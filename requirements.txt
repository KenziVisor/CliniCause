# Canonical project-wide installation entry point.
# Parent/router dependencies are declared here; component dependencies remain
# maintained in their respective directories and are included recursively.

pandas>=2.0
numpy>=1.9
tqdm>=4.0
scikit-learn>=1.0
matplotlib>=3.0
seaborn>=0.12

-r causal-irregular-time-series/requirements.txt
-r STraTS/requirements.txt
