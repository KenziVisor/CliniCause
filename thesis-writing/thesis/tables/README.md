# Thesis Table Directory

Store thesis-local generated or hand-checked table artifacts here only after a later stage validates the source data and table plan.

Do not fill missing numeric values with invented values. Current planned tables and source artifacts are tracked in `thesis-writing/planning/table_plan.md`; chapter files contain `[TABLE REQUIRED]` comments where later drafting may attach approved tables.
