You are performing Step 3 of the approved CliniCause thesis–paper story-alignment mission.

MODEL AND EFFORT
- Use GPT-5.6.
- Use High reasoning.
- This is a bounded scientific-writing, LaTeX-editing, figure-integration, and validation task.
- Perform only Step 3.
- Stop after the Step 3 report and validation.
- Do not begin Step 4.

CONTROLLING DOCUMENTS

Read these files completely before editing:

thesis-writing/THESIS_TESTBED_STORY_ALIGNMENT_PLAN.md
thesis-writing/logs/testbed_story_alignment_step_1_audit.md
thesis-writing/logs/testbed_story_alignment_step_2_intellectual_reframe.md

Also read the current active versions of:

thesis-writing/thesis/chapters/01_introduction.tex
thesis-writing/thesis/chapters/02_background_related_work.tex
thesis-writing/thesis/chapters/03_problem_definition_study_design.tex

These three chapters are read-only in Step 3. Read them to maintain continuity with the testbed-first intellectual hierarchy established in Step 2.

The canonical plan controls the overall mission.

The Step 1 audit controls repository-specific source authority, frozen content, figure provenance, and terminology boundaries.

The Step 2 report controls the current post-reframe state, build procedure, protected-file baseline, and deferred issues.

MISSION

Reframe Chapters 4–8 so that their detailed technical content is clearly presented as the construction and converging-evidence characterization of two dataset-specific observational causal-analysis testbeds.

Do not rewrite these chapters from scratch.

The current technical detail is an important thesis strength and must be preserved. The required change is primarily conceptual framing:

- preprocessing instantiates source-specific resource contracts;
- proxy construction defines and deterministically instantiates the representation layer;
- predictive modeling evaluates proxy recoverability;
- aggregation provides one fixed analytical exposure interface;
- DAGs and adjustment sets make representation-layer causal assumptions explicit;
- matching and causal estimators characterize behavior under those assumptions;
- robustness, sensitivity, support, clinical comparison, and provenance form non-equivalent converging-evidence axes.

The chapters must remain explicit that:

- patient records and mortality outcomes are source-observed;
- proxy labels are deterministically derived analytical constructs;
- model predictions are learned annotations of those proxy labels;
- aggregation is algorithmic and is not clinical consensus;
- DAGs and adjustment sets are project-specified assumptions;
- causal estimators do not have access to known causal truth;
- agreement, prediction, matching, permutation, and sensitivity do not establish clinical or causal correctness.

EXPECTED BASELINE

The expected committed baseline is:

80b2169fcc0282ef12277e2a4fa88e0592fccaaf

Work from the actual current HEAD.

If HEAD is newer, inspect and classify every intervening commit. Do not reset, overwrite, or silently reinterpret later work.

REPOSITORY STARTUP

Work from the CliniCause repository root.

Before editing, run and record:

git status --short
git branch --show-current
git rev-parse HEAD
git log -10 --oneline --decorate

Read applicable README, AGENTS.md, figure, table, thesis, literature, and reproducibility instructions.

Preserve every pre-existing modified or untracked file.

Do not:

git reset --hard
git clean -fd
git stash
git add .
git add -A
git commit
git push

Do not repair unrelated whitespace, normalize line endings, remove generated artifacts broadly, or clean unrelated directories.

SOURCE AUTHORITY

Use this scientific authority order:

1. thesis-writing/CausalDataGeneration.pdf
   - authority for the final visible paper story and the final shock-proxy figure;

2. thesis-writing/aaai27-submission.tex
   - authority for the finished paper’s method framing and figure references;

3. thesis-writing/supp.pdf
   - authority for finished supplementary descriptions of proxy definitions, clinical comparisons, DAG documentation, and evaluation scope;

4. thesis-writing/paper-aaai/aaai27-submission.tex
   - byte-identical supporting final source, read-only;

5. thesis-writing/paper-aaai/supplementary1.tex
   - supporting source for the finished supplement, read-only;

6. active thesis source under thesis-writing/thesis/
   - authority for the detailed implemented workflow, exact rules, model descriptions, estimands, estimator hierarchy, diagnostics, and qualifications;

7. checked thesis result and reproducibility material
   - may be inspected to verify terminology or methodology, but Step 3 must not add new empirical results.

Explicitly exclude as scientific authorities:

thesis-writing/paper-aaai/paper.tex
thesis-writing/paper-aaai/paper.pdf
thesis-writing/ignore-paper-aaai/**
thesis-writing/paper-aaai/AuthorKit27/**

Do not conduct internet research.

PROTECTED MATERIAL

Do not modify:

thesis-writing/CausalDataGeneration.pdf
thesis-writing/aaai27-submission.tex
thesis-writing/supp.pdf
thesis-writing/paper-aaai/**
thesis-writing/ignore-paper-aaai/**
thesis-writing/true-figure-1.png

Do not modify any code, dataset, result CSV, table source, reproducibility record, paper report, model artifact, existing paper figure, or existing thesis figure.

The only allowed operation involving a protected paper figure is reading and byte-for-byte copying the verified shock-proxy image to a new thesis-specific destination.

PERMITTED SOURCE EDITS

You may modify only:

thesis-writing/thesis/chapters/04_data_preprocessing.tex
thesis-writing/thesis/chapters/05_proxy_state_construction.tex
thesis-writing/thesis/chapters/06_predictive_modeling.tex
thesis-writing/thesis/chapters/07_causal_methodology.tex
thesis-writing/thesis/chapters/08_robustness_sensitivity_validation.tex

You may create only:

thesis-writing/thesis/figures/clinicause_shock_proxy_example.png
thesis-writing/logs/testbed_story_alignment_step_3_methodology_reframe.md

After a complete successful validation, you may replace only the generated artifact:

thesis-writing/thesis/main.pdf

with the validated post-Step-3 PDF.

No other repository file may change.

READ-ONLY THESIS FILES

Do not modify:

thesis-writing/thesis/main.tex
thesis-writing/thesis/frontmatter/**
thesis-writing/thesis/chapters/01_introduction.tex
thesis-writing/thesis/chapters/02_background_related_work.tex
thesis-writing/thesis/chapters/03_problem_definition_study_design.tex
thesis-writing/thesis/chapters/10_results.tex
thesis-writing/thesis/chapters/11_discussion.tex
thesis-writing/thesis/chapters/12_conclusions_future_work.tex
thesis-writing/thesis/appendices/**
thesis-writing/thesis/tables/**
thesis-writing/thesis/figures/clinicause_testbed_pipeline.png
all other existing thesis figures

Do not update the abstracts, keywords, discussion, conclusions, results, or appendices in this step.

STEP 3 MUST NOT ADD NEW EMPIRICAL RESULTS

Do not add:

- new prediction metrics;
- new mortality-prediction values;
- new estimator agreement values;
- new rank correlations;
- new RMSE values;
- new clinical-literature effect comparisons;
- new permutation counts;
- new omitted-variable sensitivity thresholds or counts;
- new matching results;
- new cohort counts;
- new effect estimates.

You may describe evaluation methods and evidential roles conceptually.

Numerical synthesis belongs to Step 4.

PAPER PROTECTION BASELINE

Before editing, record SHA-256 hashes for:

thesis-writing/CausalDataGeneration.pdf
thesis-writing/aaai27-submission.tex
thesis-writing/supp.pdf
thesis-writing/true-figure-1.png
thesis-writing/paper-aaai/figures/figure3_shock_proxy_example.png
thesis-writing/thesis/figures/clinicause_testbed_pipeline.png

Create a deterministic protected-paper manifest:

find thesis-writing/paper-aaai -type f -print0 \
  | sort -z \
  | xargs -0 sha256sum \
  > /tmp/testbed_alignment_step3_paper_aaai_before.txt

Create a baseline hash file for every permitted existing source:

sha256sum \
  thesis-writing/thesis/chapters/04_data_preprocessing.tex \
  thesis-writing/thesis/chapters/05_proxy_state_construction.tex \
  thesis-writing/thesis/chapters/06_predictive_modeling.tex \
  thesis-writing/thesis/chapters/07_causal_methodology.tex \
  thesis-writing/thesis/chapters/08_robustness_sensitivity_validation.tex \
  thesis-writing/thesis/main.pdf \
  > /tmp/testbed_alignment_step3_allowed_before.sha256

Record before-edit inventories for:

- chapter and section headings;
- labels;
- references;
- citation keys;
- figures;
- tables;
- all integers, decimals, percentages, thresholds, mathematical constants, and reported counts in Chapters 4–8;
- proxy names;
- model names;
- estimator names;
- DAG and adjustment-set descriptions.

BASELINE PDF BUILD BEFORE EDITING

The tracked `thesis-writing/thesis/main.pdf` may not contain the Step 2 source changes because Step 2 validated its PDF out of tree.

Before editing Chapters 4–8, build the current Step 2 source into a temporary directory and treat that PDF as the visual baseline for this task.

Use the successful explicit XeLaTeX/Biber procedure recorded in the Step 2 report rather than assuming that `latexmk` alone invokes Biber correctly.

Create a clean temporary build preserving all relative source paths. A valid sequence is:

xelatex -interaction=nonstopmode -halt-on-error -output-directory=<BEFORE_DIR> main.tex
biber --input-directory=<BEFORE_DIR> --output-directory=<BEFORE_DIR> main
xelatex -interaction=nonstopmode -halt-on-error -output-directory=<BEFORE_DIR> main.tex
xelatex -interaction=nonstopmode -halt-on-error -output-directory=<BEFORE_DIR> main.tex

Use a temporary copy of the necessary `thesis-writing` structure if required to preserve bibliography and figure paths.

Do not modify repository sources merely to make an out-of-tree build convenient.

Record:

- baseline PDF path;
- page count;
- SHA-256;
- `qpdf --check` result;
- citation/reference status;
- existing warnings.

Do not proceed with editing if the Step 2 baseline source cannot be built successfully.

TERMINOLOGY REQUIREMENT

Use:

- “source-observed records”;
- “source-observed measurements and outcomes”;
- “source-observed measurement, missingness, care-process, and support patterns”;
- “observational data-generating and measurement processes remain unknown”;
- “empirical proxy prevalence induced by fixed rules on source-observed data” where relevant.

Do not use the phrase:

source-observed mechanisms

The causal and assignment mechanisms are not observed.

Do not imply that a derived proxy exposure is itself source-recorded. The underlying measurements and outcomes are source-observed; the proxy exposure is representation-defined and deterministically instantiated.

Use:

- “resource” for the constructed dataset/artifact package;
- “testbed” for its methodological evaluation role;
- “proxy recoverability” for prediction against rule-derived labels;
- “model-estimated CATE” for estimator outputs;
- “clinical-literature comparison” or “contextual corroboration” for external comparison.

Do not use:

- ground-truth benchmark for CliniCause;
- validated diagnosis;
- validated DAG;
- clinical consensus for the aggregate;
- causal accuracy for estimator agreement;
- clinical validation for proxy prediction;
- robust without naming the exact comparison or perturbation.

CHAPTER 4 — DATA PREPROCESSING AS TESTBED INSTANTIATION

Edit:

thesis-writing/thesis/chapters/04_data_preprocessing.tex

Preserve all existing:

- source descriptions;
- cohort definitions;
- identifier rules;
- time-window definitions;
- variable contracts;
- preprocessing transformations;
- missingness representations;
- dataset-specific differences;
- artifact schemas;
- exclusions;
- limitations;
- provenance statements;
- numerical values.

Do not alter code-level descriptions unless they are factually inconsistent with the current implementation evidence already used by the thesis.

Reframe only where necessary so that the chapter explains how preprocessing instantiates two separate source-specific testbed contracts.

The chapter should communicate:

1. MIMIC-III and PhysioNet remain separate resources.

2. Preprocessing does not generate synthetic patients or mortality outcomes.

3. Preprocessing selects, normalizes, aligns, summarizes, masks, imputes, or transforms source observations exactly as documented by the existing implementation.

4. Do not claim that every raw missingness pattern or every raw event is preserved unchanged if the implemented pipeline transforms it.

5. Distinguish:
   - source-observed events and outcomes;
   - extraction choices;
   - cohort restrictions;
   - preprocessing transformations;
   - analysis-ready representations;
   - downstream derived labels.

6. Explain that these choices are part of the explicit resource contract because they determine:
   - which records enter;
   - which measurements are available;
   - how time is represented;
   - how absence is encoded;
   - which identifiers survive;
   - which downstream constructs can be instantiated.

7. Treat cross-dataset heterogeneity as a feature of having two observational testbeds, not as a preprocessing defect.

8. Preserve the no-pooling policy.

Prefer limited edits to:

- the chapter opening;
- transition paragraphs;
- chapter summary or closing interpretation.

Do not rewrite technical subsections that are already correct.

CHAPTER 5 — REPRESENTATION DESIGN AND DETERMINISTIC PROXY INSTANTIATION

Edit:

thesis-writing/thesis/chapters/05_proxy_state_construction.tex

Preserve every:

- proxy name;
- dataset assignment;
- threshold;
- inequality;
- temporal rule;
- missingness rule;
- fallback rule;
- feature requirement;
- source-code authority statement;
- aggregation definition;
- numerical value;
- table;
- label;
- cross-reference.

The chapter should make the following sequence explicit:

schema and literature
→ LLM-assisted candidate representation
→ project selection
→ deterministic source encoding
→ patient-level proxy instantiation from available observations
→ optional model-generated proxy annotations
→ fixed downstream aggregate where used

Clarify:

1. The LLM proposed designs but did not execute on patient records.

2. Active source code, not prompt text, defines the implemented proxy rule.

3. Proxy labels are representation-defined analytical constructs.

4. A positive label means the encoded rule was satisfied by available evidence.

5. A zero label means the rule did not fire on the available evidence.

6. A zero label does not establish clinical absence.

7. Missingness can prevent a clause from firing and can therefore affect the instantiated proxy.

8. Rule-derived labels are not diagnoses, expert adjudication, or causal truth.

9. Similarly named proxies may differ across datasets.

10. The majority-vote aggregate is a deterministic analytical interface formed from:
    - one rule-derived source;
    - STraTS;
    - GRU;
    - GRU-D;
    - TCN.

11. The aggregate is not clinical consensus, five independent ground-truth labels, or a validated phenotype.

Keep the aggregation material in this chapter, but subordinate it to representation instantiation rather than presenting aggregation as the primary scientific object.

SHOCK-PROXY FIGURE

The finished paper’s visible shock example is stored under a stale filename:

thesis-writing/paper-aaai/figures/figure3_shock_proxy_example.png

The Step 1 audit verified that it corresponds to the finished paper’s Figure 2.

Before copying:

1. inspect `thesis-writing/CausalDataGeneration.pdf` and confirm that this image matches the visible shock-proxy figure;
2. inspect its current SHA-256;
3. compare it to the Step 1 recorded hash:

b7d7b3b5f1c52506442d847c99795d4f195f144cf19a7c61478225c64469f295

If the current source hash differs or the image does not match the finished paper, stop and report the discrepancy. Do not substitute another image automatically.

If verified, copy exact bytes:

cp \
  thesis-writing/paper-aaai/figures/figure3_shock_proxy_example.png \
  thesis-writing/thesis/figures/clinicause_shock_proxy_example.png

Do not crop, resize, recolor, recompress, annotate, sharpen, convert, or regenerate it.

Verify source and destination hashes are identical.

Insert the figure in Chapter 5 near the MIMIC-III shock operational definition or immediately after the design-to-instantiation explanation, choosing the location that best connects the figure to the exact rule.

Use label:

fig:clinicause-shock-proxy-example

Use a caption that states:

- it is an illustrative deterministic instantiation of the project-selected shock proxy;
- observed-evidence clauses feed the encoded rule;
- the label is derived from available observations;
- a non-firing clause can reflect absent qualifying evidence;
- proxy value zero does not prove absence of clinical shock;
- the figure illustrates the representation contract rather than validating the clinical construct.

Reference the figure in the prose before or immediately after it.

Use a width that makes every label legible at 100% PDF zoom.

Do not introduce a new rule, threshold, or interpretation from the graphic if it is not already supported by the active thesis source.

CHAPTER 6 — PREDICTIVE MODELING AS PROXY RECOVERABILITY

Edit:

thesis-writing/thesis/chapters/06_predictive_modeling.tex

Preserve all existing:

- architecture descriptions;
- model scope;
- STraTS details;
- GRU details;
- GRU-D details;
- TCN details;
- input representations;
- splits;
- loss functions;
- training procedures;
- thresholding;
- metrics;
- export formats;
- numerical values;
- dataset-specific conclusions;
- limitations.

Reframe the chapter so that its methodological role is proxy recoverability.

State clearly:

1. The predictive target is the deterministic rule-derived proxy label.

2. Predictive performance measures whether the rule-derived construct corresponds to learnable patterns in the irregular source-derived representation.

3. Recoverability does not establish:
   - clinical validity;
   - diagnostic accuracy;
   - causal validity;
   - correctness of the proxy definition;
   - correctness of the DAG;
   - usefulness of an intervention.

4. Dataset-specific differences in leading model performance support dataset-specific model choice and characterize the resources; they do not establish universal model superiority.

5. Model outputs are learned annotations, not source-observed exposures.

6. Probability exports and thresholded binary labels have distinct roles.

7. Prediction errors can propagate into the aggregate and downstream observational analysis.

8. The chapter should connect its outputs to the testbed contract defined in Chapter 3 and to the converging-evidence framework in Chapter 8.

Prefer localized changes to the chapter introduction, interpretation paragraphs, and closing summary.

Do not rewrite architecture subsections unnecessarily.

CHAPTER 7 — CAUSAL REPRESENTATION, ADJUSTMENT, AND ESTIMATOR INTERFACES

Edit:

thesis-writing/thesis/chapters/07_causal_methodology.tex

Preserve all existing:

- DAG nodes;
- DAG edges;
- graph figures;
- graph labels;
- adjustment sets;
- formulas;
- estimands;
- covariate lists;
- treatment/exposure definitions;
- estimator configurations;
- matching method;
- CausalForestDML description;
- LinearDML description;
- CausalPFN description;
- uncertainty descriptions;
- assumptions;
- limitations;
- numerical values.

Do not recopy or duplicate the MIMIC or PhysioNet DAG figures. The Step 1 audit established that the active thesis copies already match the relevant paper/support assets.

Reframe the chapter so that:

1. DAGs are explicit representation-layer assumptions included with each testbed.

2. The DAGs are:
   - project-specified;
   - source-encoded;
   - not learned from the study data;
   - not validated edge by edge;
   - not guaranteed complete.

3. Exposure-specific adjustment sets operationalize the project graphs.

4. Graph transparency makes assumptions inspectable and replaceable; it does not make them correct.

5. Proxy-state exposures often do not correspond to simple, well-specified interventions.

6. Model-estimated CATE summaries remain conditional on:
   - proxy measurement;
   - temporal ordering;
   - graph adequacy;
   - observed adjustment;
   - support;
   - model specification;
   - estimand definition.

7. Matching is a secondary descriptive/support-oriented comparison and must not be upgraded to a definitive treatment-effect estimator.

8. Preserve the estimator hierarchy exactly:
   - CausalForestDML: primary;
   - LinearDML: secondary comparator;
   - CausalPFN: exploratory complementary estimator.

9. Do not give CausalPFN diagnostic or provenance support that the archived evidence does not contain.

10. Agreement among estimators belongs to converging-evidence characterization, not causal accuracy.

Prefer localized changes to:

- chapter opening;
- DAG-role explanation;
- transitions into adjustment;
- estimator-role framing;
- chapter conclusion.

Do not modify mathematical definitions merely for rhetorical alignment.

CHAPTER 8 — CONVERGING-EVIDENCE EVALUATION

Edit:

thesis-writing/thesis/chapters/08_robustness_sensitivity_validation.tex

This chapter should become the methodological center for evaluating observational testbeds without a causal answer key.

You may revise the visible chapter title and selected section headings if doing so improves precision. Keep the filename unchanged.

A preferred chapter title is:

Converging-Evidence Evaluation, Robustness, and Sensitivity

Use another closely equivalent title only if it better matches the existing internal structure.

Avoid using unqualified “validation” for the whole resource.

The chapter must explain that the evaluation axes are non-equivalent. They investigate different failure modes and cannot be added together as proof of truth.

Organize the existing material around the following axes.

1. CONSTRUCT PLAUSIBILITY

Describe:

- literature-grounded representation design;
- bounded informal clinician consultation, where supported;
- inspectability of rules and rationale.

State that there was no:

- blinded formal clinician-rating study;
- chart adjudication;
- inter-rater reliability analysis;
- clinical construct validation.

Do not invent consultation details absent from the finished paper or thesis evidence.

2. PROXY RECOVERABILITY

Reference Chapter 6.

Explain that predictive performance evaluates learnability of rule-derived labels, not clinical correctness.

3. MORTALITY-RELEVANT PREDICTIVE INFORMATION

Describe the methodological role of predicting mortality from proxy representations.

Do not add disputed or newly synthesized AUROC values.

State that prognostic information is associational and does not establish causal relevance.

4. CROSS-ESTIMATOR STABILITY

Define distinct comparison dimensions:

- sign agreement;
- rank agreement;
- magnitude agreement.

State that these dimensions are not interchangeable.

Agreement means a pattern is not unique to one fitted estimator under the fixed representation and population. It does not establish estimator accuracy, estimand equivalence, identification, or truth.

Do not add new rank, RMSE, or agreement numbers in Step 3.

5. CLINICAL-LITERATURE COMPARISON

Add a bounded methodology subsection based only on:

thesis-writing/CausalDataGeneration.pdf
thesis-writing/supp.pdf
thesis-writing/paper-aaai/supplementary1.tex

Explain that the comparison extracts, where available:

- represented clinical construct;
- operational definition;
- study population;
- comparison group;
- mortality horizon;
- effect scale;
- adjustment method;
- reported mortality contrast;
- major comparability limitations.

Explain that external studies can differ in:

- clinical definition;
- severity;
- eligibility;
- data source;
- calendar period;
- treatment context;
- observation window;
- mortality horizon;
- matching or adjustment;
- estimand;
- support;
- residual confounding.

State that the comparison is:

- contextual corroboration;
- a way to identify broad compatibility and discrepancies;
- not a meta-analysis;
- not calibration against causal truth;
- not external validation of the proxy;
- not proof that a CliniCause estimate is correct.

Do not add the numerical comparison table or numerical synthesis in Step 3. Those belong to Step 4.

6. MATCHING AND EMPIRICAL SUPPORT

Preserve the current matching and support methods.

Clarify that:

- matching availability and balance diagnostics reveal empirical support constraints;
- matched outcome differences remain descriptive and estimator-dependent;
- failure to find adequate matches is informative;
- successful matching does not prove positivity or identification.

7. PERMUTATION OR DISRUPTION CHECKS

Preserve the implemented treatment and outcome permutation methods.

Explain what disrupted-label or disrupted-outcome behavior tests.

Do not describe permutation checks as causal validation or conventional inferential p-values unless the implementation and existing text explicitly support that interpretation.

A successful disruption response is a sanity check on pipeline behavior, not proof of correct causal estimation.

8. OMITTED-VARIABLE SENSITIVITY

Preserve the exact implemented sensitivity method and all existing qualifications.

State that sensitivity analysis asks how conclusions change under a specified model of unobserved confounding.

It does not:

- establish the actual amount of confounding;
- prove that no confounding exists below a threshold;
- validate the DAG;
- determine whether an estimate is true or false.

Do not add the paper’s concise unified sensitivity numbers in Step 3.

9. POPULATION PERTURBATION AND OUTCOME DOWNSAMPLING

Preserve the original-versus-downsampled design.

Explain that it studies population and outcome-prevalence dependence.

Do not treat it as a replacement for external validation or as evidence that the original cohort is unbiased.

10. PROVENANCE AND EVIDENCE ADMISSION

Preserve and strengthen the distinction among:

- current source behavior;
- archived numerical artifacts;
- checked result rows;
- reconstructed summaries;
- unresolved producing lineage;
- full clean-checkout reproducibility.

Explain that provenance controls what may be claimed from the testbed, but provenance quality does not itself establish clinical or causal validity.

CHAPTER 8 EVALUATION-AXIS SUMMARY

Add a concise synthesis paragraph, or a compact non-numerical table only if necessary, explaining for every axis:

- what question it asks;
- what evidence it supplies;
- what it cannot establish.

Prefer prose or a short structured description over a large new table.

Do not add an evaluation score, total evidence score, or rank the testbeds.

CHAPTER-LEVEL CONTINUITY

Across Chapters 4–8:

- preserve existing section labels whenever possible;
- add new labels only for genuinely new sections;
- preserve all existing cross-references;
- update opening and closing paragraphs so each chapter’s role in the Design–Instantiate–Evaluate story is clear;
- avoid repeating the full Chapter 1 argument in every chapter;
- use short governing statements and precise transitions;
- retain technical detail rather than replacing it with high-level paper prose.

FROZEN SCIENTIFIC CONTENT

The following must not change:

- cohort definitions;
- record counts;
- proxy names;
- proxy thresholds;
- temporal rules;
- missingness logic;
- preprocessing transformations;
- model scope;
- model architecture facts;
- dataset split facts;
- metric values;
- aggregation composition;
- aggregation threshold;
- DAG nodes and edges;
- adjustment sets;
- estimator formulas;
- estimator hyperparameters;
- estimator hierarchy;
- matching method;
- estimands;
- sampling definitions;
- sensitivity method;
- permutation method;
- support definitions;
- result-admission rules;
- provenance limitations.

Any apparent need to change one of these is blocking and must be reported rather than silently repaired.

POST-EDIT NUMERICAL AND STRUCTURAL AUDIT

Compare the pre-edit and post-edit Chapters 4–8.

Extract and compare:

- all integers;
- decimals;
- percentages;
- inequalities;
- ranges;
- variable names;
- proxy names;
- model names;
- estimator names;
- treatment/exposure names;
- DAG edge descriptions;
- adjustment-set content;
- formula environments;
- figure labels;
- table labels;
- citation keys;
- cross-reference targets.

Every difference must be categorized as:

- intended prose framing;
- new shock-figure reference;
- intended heading/label addition;
- formatting-only;
- unexpected scientific drift.

Unexpected scientific drift is blocking.

SHOCK-FIGURE VALIDATION

Verify exact equality:

sha256sum \
  thesis-writing/paper-aaai/figures/figure3_shock_proxy_example.png \
  thesis-writing/thesis/figures/clinicause_shock_proxy_example.png

The hashes must match.

Verify:

- figure appears in the list of figures;
- label resolves;
- first prose reference is close to placement;
- caption is accurate;
- all text is readable at normal thesis page width;
- image is not clipped or blurred;
- it does not create an excessive blank area or isolated caption page.

POST-EDIT BUILD

Build the complete post-Step-3 thesis into a new clean temporary directory using the explicit XeLaTeX/Biber sequence proven in Step 2.

Do not rely solely on `latexmk` unless you independently verify that Biber ran.

Required checks:

- all XeLaTeX passes succeed;
- Biber succeeds;
- no undefined citations;
- no undefined references;
- no duplicate labels;
- no missing figures;
- no missing glyphs;
- no new fatal or severe overfull boxes;
- `qpdf --check` succeeds;
- resulting PDF opens correctly;
- page count is recorded;
- SHA-256 is recorded.

Compare the pre-edit Step 2 baseline PDF and post-edit Step 3 PDF.

Use text extraction and page rendering to identify changed pages.

Do not use OCR.

TRACKED MAIN PDF

Only after the post-edit PDF has passed every build and visual check, replace:

thesis-writing/thesis/main.pdf

with the validated post-Step-3 PDF.

Copy only the PDF.

Do not copy:

- `.aux`;
- `.bcf`;
- `.bbl`;
- `.blg`;
- `.fdb_latexmk`;
- `.fls`;
- `.log`;
- `.out`;
- `.run.xml`;
- `.toc`;
- page renderings;
- temporary comparison files.

Record:

- previous tracked `main.pdf` SHA-256 and page count;
- Step 2 baseline temporary PDF SHA-256 and page count;
- final Step 3 PDF SHA-256 and page count;
- confirmation that tracked `main.pdf` equals the validated final PDF.

VISUAL REVIEW

Render every changed post-edit PDF page at no less than 180–200 DPI.

Inspect changed pages individually.

A contact sheet may be used only for navigation.

At minimum inspect:

- table of contents if the Chapter 8 title changed;
- list of figures;
- every changed page in Chapter 4;
- every changed page in Chapter 5;
- the shock-proxy figure page at 100% zoom;
- every changed page in Chapter 6;
- every changed page in Chapter 7;
- every changed page in Chapter 8;
- all pages affected by float movement;
- bibliography pages if pagination moved them;
- Hebrew cover and title pages for regressions caused by pagination or rebuild;
- the pipeline figure page to confirm it remains intact.

Check for:

- clipped text;
- unreadable figure labels;
- low-resolution rendering;
- wrong RTL behavior;
- broken headings;
- orphaned headings;
- widowed captions;
- excessive whitespace;
- floats far from references;
- duplicate figures;
- broken list-of-figures entries;
- unresolved references;
- page-number anomalies;
- accidental blank pages;
- overfull boxes introduced by edited prose.

PAPER PROTECTION VALIDATION

After all edits and PDF generation, recompute:

sha256sum \
  thesis-writing/CausalDataGeneration.pdf \
  thesis-writing/aaai27-submission.tex \
  thesis-writing/supp.pdf \
  thesis-writing/true-figure-1.png \
  thesis-writing/paper-aaai/figures/figure3_shock_proxy_example.png \
  thesis-writing/thesis/figures/clinicause_testbed_pipeline.png

Recreate:

find thesis-writing/paper-aaai -type f -print0 \
  | sort -z \
  | xargs -0 sha256sum \
  > /tmp/testbed_alignment_step3_paper_aaai_after.txt

Compare:

diff -u \
  /tmp/testbed_alignment_step3_paper_aaai_before.txt \
  /tmp/testbed_alignment_step3_paper_aaai_after.txt

Any task-caused change under the protected paper paths is blocking.

GIT AND DIFF VALIDATION

Run a task-scoped check:

git diff --check -- \
  thesis-writing/thesis/chapters/04_data_preprocessing.tex \
  thesis-writing/thesis/chapters/05_proxy_state_construction.tex \
  thesis-writing/thesis/chapters/06_predictive_modeling.tex \
  thesis-writing/thesis/chapters/07_causal_methodology.tex \
  thesis-writing/thesis/chapters/08_robustness_sensitivity_validation.tex \
  thesis-writing/thesis/figures/clinicause_shock_proxy_example.png \
  thesis-writing/thesis/main.pdf \
  thesis-writing/logs/testbed_story_alignment_step_3_methodology_reframe.md

Also run:

git diff --check
git diff --stat
git diff --name-only
git status --short

If the global whitespace check fails only because of documented pre-existing files, do not modify those files. Record the distinction.

Inspect the full diff of every task-modified text file.

Confirm no unexpected file changed.

REPORT

Create:

thesis-writing/logs/testbed_story_alignment_step_3_methodology_reframe.md

Use this structure:

# Testbed Story Alignment — Step 3 Methodology Reframe

## 1. Baseline
- expected baseline
- actual HEAD
- branch
- recent commits
- pre-existing modified and untracked files
- applicable instructions read

## 2. Scope and Source Authority
- objective
- permitted files
- protected files
- sources inspected
- superseded sources excluded
- files actually changed

## 3. Step 2 Baseline Build
- commands
- temporary build path
- PDF path
- page count
- SHA-256
- qpdf result
- citations and references
- pre-existing warnings

## 4. Chapter 4 Changes
- framing changes
- technical content preserved
- source-observed versus transformed-data distinctions
- no-pooling preservation

## 5. Chapter 5 Changes
- representation-design hierarchy
- deterministic instantiation language
- missingness semantics
- aggregation role
- exact rules deliberately preserved

## 6. Shock-Proxy Figure
- finished-paper verification
- source path
- destination path
- expected source hash
- actual source hash
- destination hash
- equality result
- placement
- caption
- label
- visual readability
- confirmation that source paper files remained unchanged

## 7. Chapter 6 Changes
- proxy-recoverability framing
- preserved model details
- clinical-validity boundary
- export and aggregation boundaries

## 8. Chapter 7 Changes
- DAG role
- adjustment-set role
- estimator hierarchy
- matching role
- preserved formulas and assumptions
- no graph or estimator changes

## 9. Chapter 8 Changes
- chapter title decision
- converging-evidence structure
- role and limit of every evaluation axis
- clinical-literature comparison methodology
- claims deliberately deferred to Step 4
- unqualified validation language removed or bounded

## 10. Evaluation-Axis Coverage Matrix
For each axis:
- method;
- source chapter;
- evidential role;
- prohibited inference;
- later result location.

## 11. Frozen-Content Verification
- numeric-token comparison
- proxy and threshold comparison
- preprocessing-contract comparison
- model comparison
- aggregation comparison
- DAG and adjustment-set comparison
- estimator and estimand comparison
- diagnostics comparison
- labels and citations
- unexplained differences

## 12. Post-Edit Build
- exact commands
- temporary build path
- PDF path
- page count
- SHA-256
- qpdf result
- Biber result
- citation/reference result
- warnings
- comparison with Step 2 baseline

## 13. Visual Review
- changed page ranges
- rendered pages
- Chapter 4 findings
- Chapter 5 findings
- shock-figure findings
- Chapter 6 findings
- Chapter 7 findings
- Chapter 8 findings
- table-of-contents findings
- list-of-figures findings
- float movement
- title/Hebrew regression check
- unresolved layout defects

## 14. Tracked Main PDF
- old tracked PDF hash and page count
- Step 2 baseline temporary PDF hash and page count
- final Step 3 PDF hash and page count
- equality between validated final PDF and tracked main.pdf
- confirmation that no auxiliary files were copied

## 15. Paper Protection
- hashes before
- hashes after
- paper-aaai tree comparison
- changed protected-file count
- pipeline figure preservation
- shock source/copy equality

## 16. Git Diff Summary
- task-scoped git diff --check
- global git diff --check
- git diff --stat
- changed file list
- unexpected changes
- final git status
- confirmation that nothing was staged, committed, or pushed

## 17. Deferred Issues
- Step 4 empirical synthesis
- disputed or gated numerical claims
- abstracts and keywords
- discussion and conclusions
- any Step 5 terminology or layout item
- Hebrew human-review item

## 18. Readiness

End with exactly one of:

READY FOR STEP 4

or:

BLOCKED BEFORE STEP 4

If blocked, list:

- exact defect;
- exact file;
- exact page where relevant;
- whether the problem is scientific, numerical, source-authority, build, visual, or protection related;
- minimum repair required.

READINESS IS BLOCKED IF

Do not report `READY FOR STEP 4` if:

- any proxy rule or threshold changed;
- any preprocessing contract changed;
- any model scope or metric changed;
- aggregation composition changed;
- any DAG edge or adjustment set changed;
- estimator hierarchy changed;
- any new empirical result was added;
- the shock figure is not byte-identical to the verified source;
- the shock figure is unreadable;
- a paper file changed;
- the build fails;
- citations or references are unresolved;
- tracked main.pdf does not equal the validated final PDF;
- unexpected repository files changed;
- Chapters 4–8 remain predominantly pipeline-first rather than testbed-construction/evaluation-first;
- predictive recoverability is described as clinical validation;
- estimator agreement is described as causal accuracy;
- clinical comparison is described as causal validation;
- sensitivity is described as proof against confounding.

STOP CONDITION

Stop after:

- the bounded Chapter 4–8 edits;
- exact shock-figure integration;
- baseline and post-edit builds;
- complete changed-page visual comparison;
- replacement of tracked main.pdf with the validated post-Step-3 PDF;
- Step 3 report;
- final protected-file and Git verification.

Do not edit Chapters 1–3.
Do not edit Chapters 10–12.
Do not edit abstracts, keywords, or appendices.
Do not add empirical synthesis.
Do not add clinical-comparison numbers.
Do not add mortality-prediction numbers.
Do not add estimator rank or RMSE numbers.
Do not add omitted-variable numerical summaries.
Do not replace the estimator-agreement figure.
Do not stage, commit, or push.
Do not begin Step 4.