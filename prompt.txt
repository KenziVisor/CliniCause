# Stage 5.1 — Final Claim, Number, Citation, Terminology, and Front-Matter Audit

## Model

GPT-5.6 Sol

## Reasoning

Extra High

## Task identity

Implement this task as:

```text
Stage 5.1 — Whole-Thesis Claim, Number, Citation, Terminology, and Front-Matter Audit
```

The current repository head is expected to be:

```text
4f69df9 step 4.10C
```

Its immediate parent is expected to be:

```text
405f907 step 4.10B
```

Stage 4.10C completed the evidence-supported front matter and retained one concise reproducibility appendix. It reported:

```text
READY FOR STAGE 5.1 WITH NON-BLOCKING ADMINISTRATIVE WARNINGS
```

No figure was inserted, copied, generated, modified, or newly referenced during Stage 4.10C.

The next planned task after this one is:

```text
Stage 5.2 — Figure, Table, Appendix, and Reproducibility-Artifact Audit
```

Do not begin Stage 5.2.

---

# 1. Objective

Perform a final, independent, claim-level audit of the complete reader-facing thesis.

Audit:

* every empirical claim;
* every repeated number;
* every contribution claim;
* every causal or clinical statement;
* every citation;
* every bibliography key used in the thesis;
* terminology consistency;
* research-question consistency;
* estimator and cohort hierarchy;
* English and Hebrew abstract equivalence;
* title consistency;
* keywords;
* abbreviations;
* notation;
* front-matter administrative boundaries;
* reader-facing placeholders;
* cross-references;
* conclusion consistency;
* appendix claims;
* accidental repository-process language.

Repair only issues supported by authoritative evidence.

Do not perform a broad stylistic rewrite.

Do not conduct new research.

Do not add new experiments.

Do not create or modify figures in this stage.

---

# 2. Mandatory repository verification

From the repository root, run:

```bash
git status --short
git branch --show-current
git log -10 --oneline

git show --stat --oneline 4f69df9
git diff --stat 405f907..4f69df9
```

Confirm:

1. `4f69df9` exists.
2. Its message is `step 4.10C`.
3. Its immediate parent is `405f907`.
4. Stage 4.10C changed only its authorized front-matter, appendix, log, PDF, prompt, and generated-build paths.
5. No body chapter changed.
6. No checked result source changed.
7. No figure, figure generator, or figure register changed.
8. The Stage 4.10C report records:

   * 115 pages;
   * zero unresolved citations;
   * zero unresolved references;
   * zero duplicate labels;
   * zero fatal errors;
   * clean Hebrew and bidirectional rendering;
   * no newly included figure;
   * readiness for Stage 5.1 with non-blocking administrative warnings.

Record the complete initial worktree state.

Do not reset, clean, switch branches, discard unrelated changes, stage, amend, commit, or push.

If the baseline differs materially, stop and report:

```text
BLOCKED BEFORE STAGE 5.1 — REPOSITORY BASELINE MISMATCH
```

---

# 3. Mandatory generated-file cleanup

The Stage 4.10C commit includes LaTeX auxiliary files that its own report describes as generated and untracked.

These files are not authorized source deliverables:

```text
thesis-writing/thesis/main.aux
thesis-writing/thesis/main.bbl
thesis-writing/thesis/main.bcf
thesis-writing/thesis/main.blg
thesis-writing/thesis/main.fdb_latexmk
thesis-writing/thesis/main.fls
thesis-writing/thesis/main.lof
thesis-writing/thesis/main.lot
thesis-writing/thesis/main.out
thesis-writing/thesis/main.run.xml
thesis-writing/thesis/main.toc
thesis-writing/thesis/main.xdv
```

Before substantive auditing:

1. verify that each file is a generated LaTeX artifact;
2. verify that none is intentionally required as a tracked source file;
3. remove these files from the working tree;
4. do not remove `main.pdf`;
5. do not remove any `.tex`, `.bib`, source CSV, source image, manifest, or log;
6. record the cleanup in the Stage 5.1 evidence report.

Inspect `.gitignore`.

If the thesis auxiliary files are not already ignored, add narrowly scoped ignore rules for only the corresponding generated LaTeX artifacts.

Do not add a broad rule that could hide source files, result artifacts, figures, bibliography files, or evidence logs.

A suitable narrowly scoped policy may include thesis-local patterns such as:

```gitignore
thesis-writing/thesis/*.aux
thesis-writing/thesis/*.bbl
thesis-writing/thesis/*.bcf
thesis-writing/thesis/*.blg
thesis-writing/thesis/*.fdb_latexmk
thesis-writing/thesis/*.fls
thesis-writing/thesis/*.lof
thesis-writing/thesis/*.lot
thesis-writing/thesis/*.out
thesis-writing/thesis/*.run.xml
thesis-writing/thesis/*.toc
thesis-writing/thesis/*.xdv
```

Do not ignore:

```text
thesis-writing/thesis/main.tex
thesis-writing/thesis/main.pdf
thesis-writing/thesis/**/*.tex
thesis-writing/thesis/figures/
thesis-writing/literature/
thesis-writing/results/
```

---

# 4. Protected-file baseline

Before making content edits, calculate SHA-256 hashes for:

```text
thesis-writing/results/checked_*.csv
thesis-writing/results/results_manifest.csv
thesis-writing/results/results_manifest.md
thesis-writing/results/results_source_packet.md
thesis-writing/results/results_decision_register.md
thesis-writing/results/results_checksums.sha256
thesis-writing/results/figure_selection_register.md
thesis-writing/literature/metadata/references.bib
thesis-writing/literature/metadata/catalog.csv
```

These are immutable evidence sources in Stage 5.1.

Also hash every figure and figure-generation script currently used by the thesis.

At completion, prove these files are byte-for-byte unchanged.

Body and front-matter `.tex` files may be edited only through the repair policy below.

---

# 5. Baseline build

From:

```text
thesis-writing/thesis/
```

run:

```bash
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

Record:

* return codes;
* page count;
* PDF hash;
* unresolved citations;
* unresolved references;
* duplicate labels;
* Biber warnings and errors;
* fatal errors;
* missing glyphs;
* bidi warnings;
* `xdvipdfmx` warnings;
* overfull boxes;
* underfull boxes.

The Stage 4.10C report recorded 115 pages. Verify the actual current source after auxiliary-file cleanup.

---

# 6. Authoritative evidence hierarchy

Use this hierarchy.

## 6.1 Numerical authority

```text
thesis-writing/results/checked_*.csv
thesis-writing/results/results_manifest.md
thesis-writing/results/results_source_packet.md
thesis-writing/results/results_decision_register.md
thesis-writing/results/results_checksums.sha256
thesis-writing/thesis/chapters/10_results.tex
```

Checked structured sources take precedence over prose summaries.

Chapter 10 is the reader-facing numerical authority.

## 6.2 Interpretation authority

```text
thesis-writing/thesis/chapters/11_discussion.tex
thesis-writing/thesis/chapters/12_conclusions_future_work.tex
```

Use these for:

* interpretation;
* limitation;
* clinical meaning;
* causal boundaries;
* future work;
* conclusion strength.

## 6.3 Objective and contribution authority

```text
thesis-writing/thesis/chapters/01_introduction.tex
```

Use Chapter 1 for:

* thesis gap;
* objective;
* main research question;
* SRQ-1 through SRQ-7;
* contribution hierarchy.

Actual evidence in Chapters 10–12 overrides an earlier unsupported framing.

## 6.4 Methods authority

Use Chapters 3–9 and active source only where needed to validate:

* a workflow statement;
* implemented terminology;
* data contracts;
* proxy construction;
* prediction behavior;
* graph terminology;
* estimator description;
* diagnostic coverage;
* experimental design.

## 6.5 Literature authority

Use:

```text
thesis-writing/literature/metadata/references.bib
thesis-writing/literature/metadata/catalog.csv
thesis-writing/literature/README.md
```

Do not browse the web.

Do not add literature.

Do not repair a citation gap by inventing a source.

---

# 7. Create the master Stage 5.1 claim ledger

Create:

```text
thesis-writing/logs/stage_5_1_master_claim_audit.csv
```

Use one row per materially distinct reader-facing claim.

Required columns:

```text
claim_id
file
chapter_or_section
line_or_excerpt
claim_type
claim_summary
numeric_expression
citation_keys
authoritative_source
exact_source_location
support_status
qualification_status
terminology_status
hierarchy_status
cross_language_status
required_action
notes
```

Claim types should include:

```text
MOTIVATION
OBJECTIVE
RESEARCH_QUESTION
WORKFLOW
IMPLEMENTATION
CONTRIBUTION
LITERATURE_SYNTHESIS
PREDICTIVE_RESULT
CAUSAL_MODEL_RESULT
MATCHING_RESULT
DIAGNOSTIC_RESULT
ROBUSTNESS_RESULT
CROSS_DATASET_RESULT
EXPLORATORY_RESULT
LIMITATION
ETHICS_GOVERNANCE
FUTURE_WORK
NEGATIVE_BOUNDARY
ABSTRACT_SYNTHESIS
APPENDIX_PROVENANCE
```

Support statuses:

```text
SUPPORTED
SUPPORTED_WITH_QUALIFICATION
EXPLORATORY_CORRECTLY_BOUNDED
LIMITATION_SUPPORTED
ADMINISTRATIVE_GATE
UNSUPPORTED
INCONSISTENT
SOURCE_AMBIGUOUS
```

Required actions:

```text
RETAIN
REPAIR
REMOVE
MOVE_TO_TRACKING
BLOCKED_EXTERNAL_INPUT
```

---

# 8. Numerical audit

Programmatically extract numerical expressions from all reader-facing `.tex` files:

```text
thesis-writing/thesis/main.tex
thesis-writing/thesis/frontmatter/
thesis-writing/thesis/chapters/
thesis-writing/thesis/appendices/
```

Classify each expression as:

```text
RESULT_VALUE
RESULT_COUNT
DATASET_YEAR
METHOD_PARAMETER
EQUATION_OR_SYMBOL
SECTION_IDENTIFIER
CITATION_YEAR
ADMINISTRATIVE_TEXT
EDITORIAL_ORDERING
```

Every result value and result count must have an exact authoritative source.

At minimum independently revalidate:

* predictive model table values;
* MIMIC and PhysioNet causal-analysis population counts;
* all primary CausalForestDML table values;
* matching pair counts, rates, distances, and differences;
* all LinearDML comparison values;
* all CausalPFN comparison values;
* sensitivity and permutation values appearing in prose or tables;
* downsampling robustness counts;
* 9/9 MIMIC primary positive summaries;
* 9/10 PhysioNet primary positive summaries;
* negative PhysioNet shock;
* 19/19 CausalForestDML–LinearDML directional agreement;
* 18/19 all-three directional agreement;
* abstract repetitions of those values.

Check:

* sign;
* rounding;
* dataset;
* estimator;
* sampling condition;
* exposure;
* source row;
* displayed precision;
* table/prose agreement.

Do not calculate a new aggregate for inclusion.

A validation-only computation may be used internally and recorded in the report, but it must not enter reader-facing prose unless already authorized.

---

# 9. Result hierarchy audit

Verify throughout the thesis:

```text
Original cohorts = primary
Outcome-downsampled cohorts = robustness only
CausalForestDML = primary
LinearDML = secondary comparator
CausalPFN = exploratory
Matching = descriptive empirical-support baseline
No normalized-CATE main result
No numerical pooling across datasets
Every prespecified dataset-specific exposure retained
InterpNet excluded
```

Audit:

* Chapter 1;
* Chapters 7–12;
* both abstracts;
* keywords;
* nomenclature;
* appendix.

No text may imply that directional agreement establishes:

* estimator equivalence;
* correctness;
* superiority;
* interchangeable uncertainty;
* valid causal identification;
* treatment priority.

PhysioNet shock must remain the visible disagreement where all-three agreement is summarized.

---

# 10. Causal and clinical language audit

Scan every reader-facing `.tex` file for high-risk language.

Audit uses of:

```text
cause
causal effect
effect
treatment
intervention
diagnosis
phenotype
ground truth
validated
clinical
actionable
protective
recommendation
deployment
positivity
exchangeability
confounding
proof
prove
superior
expert
discovery
```

Required boundaries:

* proxy states are rule-derived analytical constructs;
* positive tags are not diagnoses;
* negative tags do not prove absence;
* project DAGs are specified assumptions;
* DAGs were not learned from data;
* DAGs were not clinically validated;
* matching is descriptive;
* model-estimated CATE remains conditional;
* mean estimated CATE is not automatically an ATE;
* agreement is not causal validation;
* a negative estimate is not a protective effect;
* no treatment recommendation follows;
* no clinical deployment readiness is established;
* no global positivity or exchangeability is proven;
* LLM prompts are design provenance only;
* an LLM did not act as a medical expert or causal-discovery method.

Use the smallest repair needed.

---

# 11. Research-question and contribution audit

Create a matrix for:

* main research question;
* SRQ-1 through SRQ-7;
* each contribution in Chapter 1.

For every item record:

```text
question_or_contribution
Chapter 1 wording
methods coverage
results evidence
discussion answer
conclusion coverage
abstract coverage
status
action
```

Check that:

* every RQ receives an evidence-bounded answer;
* no abstract introduces an eighth secondary question;
* the conclusion does not omit a limitation that changes an answer;
* contributions remain integration/application/evidence-tracking contributions;
* no component method is claimed as invented;
* no novelty is inferred from absence of literature;
* evidence infrastructure is not equated with full reproducibility.

---

# 12. Citation audit

## 12.1 Citation-key integrity

Extract every citation key used in all thesis `.tex` files.

Verify:

* every used key exists exactly once in `references.bib`;
* no malformed key;
* no unresolved citation;
* no duplicate bibliography entry for the same cited work unless intentionally distinct;
* no citation to an optional or excluded source contrary to corpus policy;
* no bibliography key appears in reader-facing text.

## 12.2 Claim-to-citation alignment

For each cited sentence or paragraph, determine whether the cited source supports:

* the method;
* dataset;
* clinical definition;
* literature synthesis;
* causal assumption;
* limitation.

Do not treat a general clinical guideline as validation of the project-specific proxy rule.

Do not treat model papers as validation of local execution.

Do not treat causal-method papers as validation of the project DAG or estimates.

## 12.3 Uncited factual claims

Identify factual literature claims that need a citation.

Repair only when an already approved source and exact bibliography key clearly supports the statement.

Otherwise:

* bound or remove the claim;
* record the unresolved source requirement;
* do not browse;
* do not add a paper.

## 12.4 Citation placement

Check that citations are attached to the claim they support and are not stranded at the end of a paragraph containing several unrelated claims.

Do not add citations to abstracts or Chapter 12 unless absolutely necessary and already approved.

---

# 13. Terminology audit

Use:

```text
thesis-writing/planning/terminology_and_notation.md
thesis-writing/audit/terminology_map.md
```

Audit consistency of:

* irregular clinical time series;
* proxy state;
* rule-derived proxy label;
* predicted proxy state;
* aggregation;
* exposure;
* outcome;
* adjustment variable;
* effect modifier;
* project-specified or project-authored DAG;
* matching contrast;
* model-estimated CATE;
* mean model-estimated CATE;
* observational analysis;
* exploratory estimator;
* evidence provenance;
* clean-checkout reproducibility.

Restricted terms must remain appropriately bounded:

```text
diagnosis
ground truth
validated phenotype
causal effect
treatment
ATE
ATT
clinical actionability
latent variable
LLM-discovered DAG
unbiased
proof
```

Audit spelling and capitalization of:

```text
MIMIC-III
PhysioNet 2012
STraTS
GRU
GRU-D
TCN
SAnD
CausalForestDML
LinearDML
CausalPFN
DAG
DML
CATE
AUROC
AUPRC
minRP
```

No InterpNet match is permitted in reader-facing thesis text.

---

# 14. Abstract equivalence audit

Audit:

```text
thesis-writing/thesis/frontmatter/abstract_primary.tex
thesis-writing/thesis/frontmatter/abstract_secondary.tex
```

Create a sentence-level semantic matrix:

```text
component_id
English claim
Hebrew claim
numbers
estimator role
qualification
equivalent
repair
```

Verify exact equivalence for:

* objective;
* datasets;
* workflow;
* five-family predictive evaluation;
* proxy-state boundary;
* DAG boundary;
* 9/9;
* 9/10;
* negative shock;
* 19/19;
* 18/19;
* CausalPFN exploratory status;
* matching descriptive status;
* no pooling;
* contribution;
* causal limitations;
* clinical/deployment limitations;
* reproducibility boundary;
* future needs.

Check:

* each abstract is under 500 words;
* no citations;
* no footnotes;
* no extra claim in one language;
* no mistranslated causal strength;
* no broken Hebrew/Latin direction;
* no use of a Hebrew term that implies clinical diagnosis or confirmed causality more strongly than the English version.

Use professional academic Hebrew, but repair only factual or materially unclear language—not stylistic preference.

---

# 15. Front-matter and administrative audit

Audit:

```text
thesis-writing/thesis/frontmatter/administrative_metadata.tex
thesis-writing/thesis/frontmatter/title_pages.tex
thesis-writing/thesis/frontmatter/keywords.tex
thesis-writing/thesis/frontmatter/acknowledgements.tex
thesis-writing/thesis/frontmatter/nomenclature.tex
thesis-writing/thesis/main.tex
```

Verify:

* the English title is identical everywhere;
* the title remains explicitly provisional where tracked;
* no Hebrew title is fabricated;
* no author identity is inferred;
* no supervisor identity is inferred;
* no department or degree wording is invented;
* no approval wording is invented;
* no institutional ethics number is invented;
* English-thesis authorization remains unresolved;
* acknowledgements are omitted when no approved text exists;
* keywords contain 10–15 entries in each language;
* bilingual keyword sets are semantically aligned;
* abbreviations are actually used;
* notation entries are actually used;
* unresolved CausalPFN naming/citation status is not concealed;
* title-page gates are precise rather than generic drafting placeholders.

Administrative gates must not be “repaired” without authoritative input.

---

# 16. Appendix audit

Audit the retained appendix:

```text
thesis-writing/thesis/appendices/appendices.tex
```

Confirm that it:

* has a scholarly reader purpose;
* accurately distinguishes available traceability from unavailable rerun provenance;
* contains no internal Codex chronology;
* contains no raw absolute paths that should not be exposed;
* contains no unsupported exact configuration;
* contains no claim of clean-checkout reproducibility;
* contains no newly introduced number without evidence;
* contains no figure;
* contains no empty placeholder chapter;
* does not duplicate Chapters 3–11 unnecessarily.

Check that omitted appendices are not still referenced from:

* body chapters;
* table of contents;
* figure/table lists;
* front matter;
* planning comments that render into the PDF.

---

# 17. Existing-figure preservation check

Stage 5.1 must not create, regenerate, edit, rename, copy, or newly reference any figure.

Identify every figure currently included by the thesis.

For each record:

```text
figure_id
path
SHA-256
source register entry
chapter
caption
changed_since_stage_4_10C
```

Confirm:

* hashes match the Stage 5.1 baseline;
* no blocked or excluded figure is included;
* no figure changed during Stage 5.1;
* no caption change alters the interpretation;
* no newly included figure exists.

The detailed value-to-image validation belongs to Stage 5.2.

If any figure changes unexpectedly, stop and report:

```text
BLOCKED BEFORE STAGE 5.2 — UNAUTHORIZED FIGURE CHANGE
```

---

# 18. Reader-facing process-language audit

Scan for:

```text
Codex
prompt.txt
Stage 4
Stage 5
checked packet
claim audit
evidence report
commit SHA
git commit
author decision freeze
repository head
worktree
```

Internal repository or provenance terminology may remain only when necessary for reproducibility and expressed in reader-facing scholarly language.

Remove internal workflow jargon that does not help the reader.

Do not remove legitimate methodological uses of:

* source code;
* source version;
* repository;
* manifest;
* checksum;
* configuration;
* provenance.

---

# 19. Repair policy

A repair is allowed when the audit identifies:

* incorrect number;
* incorrect sign;
* incorrect estimator;
* incorrect cohort;
* incorrect dataset;
* inconsistent terminology;
* missing qualification;
* unsupported claim;
* citation mismatch;
* broken citation;
* research-question drift;
* contribution overclaim;
* abstract mismatch;
* stale internal-process language;
* broken cross-reference;
* malformed LaTeX;
* nomenclature inconsistency;
* appendix overclaim.

Use the smallest sufficient edit.

Do not broadly rewrite a chapter.

Do not alter the frozen result packet.

Do not change research code.

Do not change a figure.

If an issue requires:

* new literature;
* new experiment;
* new result;
* supervisor decision;
* clinician decision;
* institutional documentation;
* official administrative details;
* new figure generation;

do not resolve it in prose.

Track it and classify it as external.

---

# 20. Allowed edit scope

You may edit reader-facing thesis `.tex` files only where required by a documented audit repair:

```text
thesis-writing/thesis/main.tex
thesis-writing/thesis/frontmatter/*.tex
thesis-writing/thesis/chapters/*.tex
thesis-writing/thesis/appendices/appendices.tex
```

You may edit:

```text
.gitignore
```

only for the narrow LaTeX-generated-file cleanup.

You may update:

```text
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

only for a new issue, materially changed status, or exact Stage 5.1 disposition.

You may create:

```text
thesis-writing/logs/stage_5_1_master_claim_audit.csv
thesis-writing/logs/stage_5_1_numeric_audit.csv
thesis-writing/logs/stage_5_1_citation_audit.csv
thesis-writing/logs/stage_5_1_rq_contribution_matrix.csv
thesis-writing/logs/stage_5_1_abstract_equivalence.csv
thesis-writing/logs/stage_5_1_evidence_report.md
```

You may regenerate:

```text
thesis-writing/thesis/main.pdf
```

Do not retain auxiliary build files after final validation.

---

# 21. Forbidden edit scope

Do not edit:

```text
prompt.txt
thesis-writing/results/checked_*.csv
thesis-writing/results/results_manifest.csv
thesis-writing/results/results_manifest.md
thesis-writing/results/results_source_packet.md
thesis-writing/results/results_decision_register.md
thesis-writing/results/results_checksums.sha256
thesis-writing/results/figure_selection_register.md
thesis-writing/thesis/figures/
thesis-writing/results/*.py
thesis-writing/literature/
thesis-writing/audit/
thesis-writing/planning/
router.py
causal-irregular-time-series/
STraTS/
requirements*.txt
tests/
```

Do not browse the web.

Do not add citations from outside the approved corpus.

Do not rerun research experiments.

Do not generate a figure.

---

# 22. Final validation

Run:

```bash
cd thesis-writing/thesis
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

After validation, remove auxiliary files while retaining `main.pdf`.

Then rebuild once more if necessary to prove that the repository source remains buildable without tracked auxiliaries.

Validate:

* page count;
* PDF SHA-256;
* zero unresolved citations;
* zero unresolved references;
* zero duplicate labels;
* zero fatal errors;
* zero Biber errors;
* zero missing glyphs;
* zero bidi errors;
* stable title;
* both abstracts;
* keyword counts;
* TOC;
* nomenclature;
* lists;
* appendix;
* bibliography.

Render the final PDF and inspect:

* title pages;
* Hebrew abstract;
* English abstract;
* keyword page;
* TOC;
* nomenclature;
* Chapter 1 opening;
* Chapter 10 result pages;
* Chapter 11 limitation pages;
* Chapter 12;
* appendix;
* bibliography transition.

---

# 23. Required scans

Run:

```bash
grep -Rni "InterpNet\|interpnet" thesis-writing/thesis \
  --include='*.tex' || true

grep -RniE \
'Stage [0-9]|Codex|prompt\.txt|claim audit|evidence report|author decision freeze|repository head|worktree' \
thesis-writing/thesis \
--include='*.tex' || true

grep -RniE \
'diagnosis|ground truth|validated phenotype|caused mortality|protective effect|treatment recommendation|deployment ready|clinical decision support|confounding eliminated|positivity established|medical expert|causal discovery|unbiased' \
thesis-writing/thesis \
--include='*.tex' || true

find thesis-writing/thesis -maxdepth 1 -type f \
  \( -name '*.aux' -o -name '*.bbl' -o -name '*.bcf' -o -name '*.blg' \
     -o -name '*.fdb_latexmk' -o -name '*.fls' -o -name '*.lof' \
     -o -name '*.lot' -o -name '*.out' -o -name '*.run.xml' \
     -o -name '*.toc' -o -name '*.xdv' \) -print
```

The final generated-file scan must return no tracked or retained auxiliary file.

Manual review is required for every language-risk match.

---

# 24. Stage 5.1 evidence report

Create:

```text
thesis-writing/logs/stage_5_1_evidence_report.md
```

Use this structure.

## 24.1 Repository baseline

* head;
* parent;
* branch;
* initial status;
* Stage 4.10C readiness;
* no-new-figure verification.

## 24.2 Generated-file cleanup

* files removed;
* why they were generated artifacts;
* `.gitignore` action;
* confirmation that `main.pdf` remains tracked;
* final auxiliary-file scan.

## 24.3 Baseline build

* commands;
* page count;
* PDF hash;
* citation/reference/label status;
* warnings.

## 24.4 Evidence hierarchy and sources inspected

List exact files.

## 24.5 Master claim audit

Report counts by support status and required action.

## 24.6 Numerical audit

List:

* result expressions checked;
* exact source;
* discrepancies;
* repairs;
* final validation.

## 24.7 Result hierarchy

Report every frozen hierarchy item.

## 24.8 Research questions and contributions

Summarize the matrix and any repairs.

## 24.9 Citation audit

Report:

* used keys;
* missing keys;
* duplicate keys;
* unsupported citation placements;
* uncited claims;
* repairs.

## 24.10 Terminology audit

Report inconsistent terms and repairs.

## 24.11 Causal and clinical language audit

Report every material match and disposition.

## 24.12 Abstract equivalence

Report:

* English word count;
* Hebrew word count;
* numerical equivalence;
* semantic-equivalence result;
* repairs.

## 24.13 Front matter

Report:

* title consistency;
* keyword counts;
* nomenclature;
* administrative gates;
* acknowledgements.

## 24.14 Appendix

Report support and any repair.

## 24.15 Existing figures

Report hashes and confirm no changes.

## 24.16 Files changed

List exact paths.

## 24.17 Protected-file validation

Report hash comparison for frozen evidence and figures.

## 24.18 Final build and render

Report:

* page count;
* PDF hash;
* citations;
* references;
* labels;
* fatal errors;
* Biber;
* glyph/bidi status;
* warning counts;
* visual-inspection result.

## 24.19 Remaining gates

Carry forward:

* supervisor ratification;
* clinical review;
* CausalPFN source/version;
* raw/configuration/split/checkpoint/source/archive provenance;
* overlap/balance;
* fairness;
* external/prospective validation;
* LLM human-review provenance;
* ethics/governance wording;
* title and English-thesis approval;
* official administrative details;
* acknowledgements;
* current faculty forms;
* detailed external figure correctness review in Stage 5.2.

## 24.20 Readiness decision

Use exactly one:

```text
READY FOR STAGE 5.2 WITH NON-BLOCKING WARNINGS
BLOCKED BEFORE STAGE 5.2
```

---

# 25. Readiness rules

Use:

```text
READY FOR STAGE 5.2 WITH NON-BLOCKING WARNINGS
```

only if:

* all reader-facing claims are supported or correctly qualified;
* every repeated number matches;
* citation keys resolve;
* citation placement is defensible;
* terminology is consistent;
* result hierarchy is unchanged;
* abstracts are equivalent;
* no figure changed;
* no auxiliary build files remain tracked or retained;
* the thesis builds;
* protected evidence is unchanged;
* remaining gates require external human, clinical, institutional, provenance, or later figure review.

Use:

```text
BLOCKED BEFORE STAGE 5.2
```

if:

* an unsupported reader-facing claim remains;
* a number cannot be reconciled;
* a citation is materially wrong and no approved source supports repair;
* an abstract differs materially from the thesis or its translation;
* result hierarchy drift remains;
* a body repair requires new research;
* a figure changed;
* an excluded figure entered the thesis;
* auxiliary generated files remain committed or retained;
* frozen evidence changed;
* the thesis does not build.

---

# 26. Final Codex response

At completion report:

1. verified head and parent;
2. readiness status;
3. generated files removed;
4. `.gitignore` change;
5. total claims audited;
6. numerical discrepancies and repairs;
7. citation issues and repairs;
8. terminology issues and repairs;
9. abstract-equivalence result;
10. research-question and contribution result;
11. causal/clinical-language result;
12. appendix result;
13. figure-preservation result;
14. files changed;
15. protected-file result;
16. final page count and PDF hash;
17. build and render result;
18. remaining gates;
19. exact next authorized stage.

Do not commit or push.

Do not edit `prompt.txt`.

Do not begin Stage 5.2.
