You are working in the unified repository `CliniCause`, which orchestrates two sibling repositories: `causal-irregular-time-series` and my forked `STraTS`.

Goal: fix the integration so the unified router is safe to run on the server. Do not run the full heavy pipeline. Make code changes only, plus lightweight validation where possible.

Context:
`CliniCause/router.py` is the top-level orchestrator. It should support this pipeline:

1. preprocessing
2. tagging latent variables
3. decision-tree plots
4. prepare STraTS inputs
5. run STraTS
6. collect STraTS predictions
7. normalize prediction/voter CSVs
8. thesis main pipeline

Fix all integration blockers below.

Required fixes:

1. Fix MIMIC preprocessing argument mismatch.
   Currently `router.py` passes `--processed-dir` to both PhysioNet and MIMIC preprocessing. `preprocess_physionet_2012.py` supports it, but `preprocess_mimic_iii_large.py` does not.
   Change `router.py` so `--processed-dir` is passed only to PhysioNet, or add proper support to the MIMIC preprocessing script. Prefer the minimal safe router-side fix.

2. Fix STraTS processed pickle format.
   `router.py` currently builds the STraTS pickle as a dict:
   `{"data": ts, "oc": oc, "train_ids": ..., "val_ids": ..., "test_ids": ...}`.
   But STraTS expects:
   `data, oc, train_ids, val_ids, test_ids = pickle.load(...)`.
   Change `build_strats_pickle()` to serialize a 5-item list/tuple:
   `[ts, oc, train_ids, val_ids, test_ids]`.
   Update `validate_strats_pickle()` so it rejects dicts and validates the actual STraTS-compatible 5-item structure.

3. Fix PhysioNet decision-tree pickle path.
   The router expects `latent_decision_trees.pkl`, but the PhysioNet tagger writes `latent_tags_trees.pkl` because it derives the tree path from `output_csv_path.replace(".csv", "_trees.pkl")`.
   Standardize this. Prefer making the PhysioNet tagger write `latent_decision_trees.pkl` in the same directory as `latent_tags.csv`, or update the router’s PhysioNet-specific path. The final result must make the `trees` stage work for both datasets.

4. Make `--run-strats false` actually skip STraTS execution.
   Currently it only affects validation but `run_strats_script()` can still execute the bash script.
   If `context.args.run_strats` is false, the router must print a clear skip message and return without running STraTS.

5. Fix single-dataset behavior.
   `run_full_main.sh` currently runs both datasets unconditionally, and `collect_strats_outputs()` loops over both datasets unconditionally.
   Make the unified router respect `--dataset physionet`, `--dataset mimic`, and `--dataset both`.
   At minimum:

* `collect_strats_outputs()` must iterate only over `context.datasets`.
* STraTS execution must not require inputs for a dataset that was not selected.
  Prefer adding dataset-aware STraTS runner support, for example by allowing the router to pass selected dataset(s) to a script or by creating separate STraTS scripts/functions for PhysioNet and MIMIC.
  Do not break the full `both` run.

6. Fix STraTS training/export checkpoint logic.
   The current STraTS bash script trains models into auto-generated output directories but exports predictions from different generic directories with `--max_epochs 0`. This can export random/untrained predictions if no checkpoint exists in the export directory.
   Ensure each model’s export uses the trained checkpoint from the same run.
   Acceptable solution:

* For each dataset/model, define a stable `output_dir`.
* Train with that `--output_dir`.
* Export with the same `--output_dir`, `--max_epochs 0`, and load the trained checkpoint if needed.
* For STRATS, distinguish pretrain and finetune correctly: finetune should load the pretrain checkpoint, and export should use the finetuned model checkpoint, not only the pretrain checkpoint.
  Make sure GRU, GRUD, TCN, SAND, and STRATS all export trained predictions.

7. Make STraTS use the router’s Python executable.
   The router has `--python-executable`, but `run_full_main.sh` uses plain `python`.
   Update the STraTS script to use `${PYTHON:-python}` and update `router.py` so when it launches the STraTS bash script it passes an environment variable `PYTHON=<context.args.python_executable>`.
   If needed, extend `run_command()` to accept an optional `env` dict.

8. Improve raw data validation.
   If the selected stages include `preprocessing`, require the relevant raw data path explicitly:

* `--physionet-raw-data-path` for PhysioNet
* `--mimic-raw-data-path` for MIMIC
  Do not treat empty string/current directory as valid.
  Fail early with a clear error message.

9. Improve `--validate-only`.
   `--validate-only` should not only check that top-level scripts exist. It should also validate selected-stage prerequisites as much as possible without running heavy computation:

* selected raw data paths are present if preprocessing is selected
* dataset config files exist and load
* expected child scripts exist
* STraTS runner exists if STraTS is selected
* STraTS input files exist if running only later stages without `prepare-strats`
* stage selection and dataset selection are coherent
  Do not load huge raw datasets in validation.

10. Improve `--skip-existing` validation.
    When skipping existing tagging outputs, validate against the full `LATENT_ORDER` from the resolved dataset config, not hard-coded columns.
    For normalized voter CSVs, ensure all required latent columns exist, are binary, and `ts_id` is present and unique.

11. Update requirements/docs.
    The top-level `requirements.txt` is too small for a full run.
    Do one of:

* create `requirements-router.txt` for the lightweight router and `requirements-full.txt` that references/combines the thesis and STraTS dependencies, or
* update README/SCRIPTS clearly explaining that full runs require installing both child repositories’ requirements.
  Prefer the clear two-file approach if practical.

12. Make logs/manifests useful.
    Keep or improve the existing manifest/log behavior.
    When a stage is skipped because of `--run-strats false`, `--skip-existing`, or dataset selection, record a clear status/reason in the manifest if that is currently supported.

Important constraints:

* Do not run the full pipeline.
* Do not require access to the real raw datasets to complete the code changes.
* Preserve existing CLI names where possible.
* Keep the router simple and explicit.
* Prefer clear failures over silent fallbacks.
* Avoid changing scientific logic unless needed for integration correctness.
* Make the code robust for Linux server execution.
* Do not over-engineer.

After making the changes, perform lightweight checks only:

* `python -m py_compile router.py`
* If possible, `python router.py --help`
* If possible, run `--validate-only` or `--dry-run` with dummy paths only where it does not require real data.
* Inspect that the STraTS script uses `${PYTHON:-python}` consistently.
* Inspect that train/export output directories are consistent.

Return:

1. A concise summary of files changed.
2. A list of the integration bugs fixed.
3. Exact commands I should run on the server for:

   * validation/dry-run
   * front-half run up to `prepare-strats`
   * full run
4. Any remaining assumptions or TODOs.
