# Stage 4.6B-R — Regenerate and Insert the Three Validated Main-Results Figures

## Model

Use:

```text
GPT-5.6 Sol
Reasoning: High
```

## Task Declaration

Implement this task as:

```text
Stage 4.6B-R — Corrected Main-Results Figures and Chapter 10 Readiness Repair
```

The latest approved commit is expected to be:

```text
fb40839 step 4.6B
```

Its parent is expected to be:

```text
5e6477a step 4.6A repair
```

Stage 4.6B successfully drafted and numerically validated Chapter 10, but its evidence report concluded:

```text
BLOCKED BEFORE STAGE 4.7
```

The only blocking issue is that the three author-selected optional figures failed numerical or semantic validation.

This repair must:

1. preserve the validated Chapter 10 tables and prose;
2. generate three corrected figures directly from the frozen checked CSV;
3. insert those corrected figures into Chapter 10;
4. preserve the conflicting optional PNGs unchanged as excluded provenance artifacts;
5. update the figure and result manifests;
6. rerun complete numerical and LaTeX validation;
7. change readiness to Stage 4.7 only when the repair passes.

Do not begin Stage 4.7.

---

# 1. Verify Repository State

From the repository root, run:

```bash
git status --short
git branch --show-current
git log -9 --oneline
git show --stat --oneline fb40839
git diff --stat 5e6477a..fb40839
```

Confirm:

* `fb40839` exists;
* its message is `step 4.6B`;
* it follows `5e6477a step 4.6A repair`;
* Chapter 10 has eight major result sections;
* `stage_4_6B_evidence_report.md` exists;
* the final Stage 4.6B build reports 87 pages;
* no InterpNet occurrence exists;
* no main-results figure is currently inserted;
* the Stage 4.6B readiness status is blocked only by figure conflicts.

Record initial and final worktree states.

Do not:

* reset;
* clean;
* checkout another branch;
* discard unrelated changes;
* stage;
* amend;
* commit;
* push.

---

# 2. Frozen Numerical Source

Use only:

```text
thesis-writing/results/checked_cate_candidates.csv
thesis-writing/results/figure_selection_register.md
thesis-writing/results/checked_figure_candidates.csv
thesis-writing/results/results_manifest.csv
thesis-writing/results/results_checksums.sha256
thesis-writing/logs/stage_4_6B_evidence_report.md
```

The numerical source for all three corrected plots is:

```text
thesis-writing/results/checked_cate_candidates.csv
```

Do not read values from:

* figure labels;
* the seminar presentation;
* the conflicting optional PNGs;
* reduced manager summaries;
* raw causal output summaries;
* normalized-CATE columns.

Do not alter any checked CATE row or numerical field.

---

# 3. Preserve the Conflicting Optional Figures

Do not modify, overwrite, rename, or delete:

```text
thesis-writing/figures-options/mimic_non_downsampled_cate_ranking.png
thesis-writing/figures-options/physionet_non_downsampled_cate_ranking.png
thesis-writing/figures-options/cross_model_direction_counts.png
```

They must remain recorded as:

```text
BLOCKED_VALUE_CONFLICT
EXCLUDED_FROM_THESIS
```

Their conflicts are:

1. The MIMIC and PhysioNet ranking plots use values corresponding to a two-DML average while their labeling describes another aggregation.
2. The direction-count plot combines original and outcome-downsampled analyses rather than reporting the approved original-cohort comparison.

The corrected figures must use new filenames.

---

# 4. Create a Reproducible Figure Script

Create:

```text
thesis-writing/results/generate_stage_4_6B_main_figures.py
```

The script must:

* read `checked_cate_candidates.csv`;
* validate required columns before plotting;
* use exact numeric values from the CSV;
* reject duplicate dataset–estimator–sampling–exposure rows;
* reject missing values in plotted fields;
* use only the explicit filters defined below;
* generate deterministic figures;
* write PNG files directly to `thesis-writing/thesis/figures/`;
* print the selected rows and derived counts;
* exit nonzero if any validation fails.

Do not embed numerical values manually in the script.

Do not read or edit the original optional PNGs.

Use Matplotlib.

Use readable thesis-scale typography and sufficient resolution for printing.

Do not create decorative or illustrative elements unsupported by the data.

---

# 5. Corrected Figure 1 — MIMIC Primary CausalForestDML Ranking

Generate:

```text
thesis-writing/thesis/figures/results_mimic_forest_original_cate_ranking.png
```

Filter the checked CSV exactly by:

```text
dataset = mimic
estimator = CausalForestDML
sampling_condition = original
selection_status = PRIMARY_MAIN_TEXT
```

Validation requirements:

* exactly nine rows;
* nine unique proxy-state exposures;
* no missing `mean_cate`;
* no normalized-CATE use;
* values match the exact rows used in Chapter 10’s MIMIC primary table.

Plot specification:

* horizontal bar chart;
* one bar per prespecified MIMIC proxy-state exposure;
* sort by exact `mean_cate`, largest at the top;
* display all nine exposures;
* use readable human-facing labels while preserving the exact `LAT_*` identifier;
* include a vertical zero reference line;
* label the horizontal axis:

```text
Mean model-estimated CATE
```

* title or subtitle must clearly state:

```text
MIMIC-III
CausalForestDML
Original cohort
```

* annotate displayed values using the same rounding policy as Chapter 10;
* do not include error bars because checked intervals are unavailable;
* do not label the values as ATE, risk ratio, relative risk, or percentage change.

Required caption boundary for Chapter 10:

```text
Original-cohort MIMIC-III mean model-estimated CATE values from
CausalForestDML. All nine prespecified proxy-state exposures are shown.
The ordering is descriptive and does not establish statistical
significance, clinical importance, or population-average causal effects.
```

---

# 6. Corrected Figure 2 — PhysioNet Primary CausalForestDML Ranking

Generate:

```text
thesis-writing/thesis/figures/results_physionet_forest_original_cate_ranking.png
```

Filter exactly by:

```text
dataset = physionet
estimator = CausalForestDML
sampling_condition = original
selection_status = PRIMARY_MAIN_TEXT
```

Validation requirements:

* exactly ten rows;
* ten unique proxy-state exposures;
* no missing `mean_cate`;
* values match the exact rows used in Chapter 10’s PhysioNet primary table;
* the negative PhysioNet shock result remains negative.

Plot specification:

* horizontal bar chart;
* sort by exact `mean_cate`, largest at the top;
* show all ten prespecified exposures;
* retain the exact `LAT_*` identifiers;
* include a zero reference line that makes the negative shock value visible;
* use the same axis wording and visual structure as the MIMIC figure;
* annotate values using Chapter 10’s display-rounding policy;
* do not use normalized CATE or unavailable intervals.

Required caption boundary:

```text
Original-cohort PhysioNet mean model-estimated CATE values from
CausalForestDML. All ten prespecified proxy-state exposures are shown.
The ordering is descriptive; the negative shock estimate is retained.
The figure does not establish statistical significance, clinical
importance, or population-average causal effects.
```

---

# 7. Corrected Figure 3 — Original-Cohort Direction Agreement

Generate:

```text
thesis-writing/thesis/figures/results_original_three_estimator_direction_agreement.png
```

Use only rows satisfying:

```text
sampling_condition = original
```

Use the three estimators:

```text
CausalForestDML
LinearDML
CausalPFN
```

For each dataset–exposure pair:

1. require exactly one row for each estimator;
2. compare the sign of exact `mean_cate`;
3. classify the pair as:

   * directionally concordant across all three; or
   * not concordant across all three.

Validation must reproduce:

```text
MIMIC: 9 concordant, 0 discordant, total 9
PhysioNet: 9 concordant, 1 discordant, total 10
Overall: 18 concordant, 1 discordant, total 19
```

The sole discordant pair must be:

```text
PhysioNet LAT_SHOCK
```

Its exact direction pattern must be:

```text
CausalForestDML: negative
LinearDML: negative
CausalPFN: positive
```

Plot specification:

* stacked horizontal bars or another simple count-based design;
* one bar for MIMIC and one for PhysioNet;
* visually distinguish concordant and discordant counts;
* display count labels;
* include `9/9` for MIMIC and `9/10` for PhysioNet;
* state `18/19 overall` in the title, subtitle, or annotation;
* identify PhysioNet `LAT_SHOCK` as the sole exception;
* do not include outcome-downsampled rows;
* do not compare magnitudes;
* do not show normalized CATE;
* do not imply estimator equivalence.

Required caption boundary:

```text
Directional concordance of the original-cohort mean model-estimated CATE
across CausalForestDML, LinearDML, and CausalPFN. All three estimators
shared the same direction for 18 of 19 dataset–exposure comparisons:
9 of 9 in MIMIC-III and 9 of 10 in PhysioNet. PhysioNet shock was the
sole exception. Direction agreement does not imply equal magnitude,
uncertainty, estimator equivalence, or causal validity.
```

---

# 8. Execute and Validate the Figure Script

Run the script from the repository root.

For each generated PNG, record:

* source checked file;
* exact selection filter;
* number of source rows;
* output path;
* width;
* height;
* file size;
* SHA-256;
* plotted values or counts;
* validation result.

Programmatically verify:

## MIMIC ranking

* nine bars;
* exact source order after sorting;
* every exact mean CATE matches the selected row;
* displayed labels use the intended rounding.

## PhysioNet ranking

* ten bars;
* exact source order;
* shock remains negative;
* every exact value matches the source.

## Agreement plot

* MIMIC 9/9;
* PhysioNet 9/10;
* total 18/19;
* only PhysioNet shock is discordant;
* no downsampled row was used.

Do not rely solely on visual inspection.

---

# 9. Update Chapter 10

Edit:

```text
thesis-writing/thesis/chapters/10_results.tex
```

Remove the paragraph stating that the three figures were blocked and not inserted.

Insert:

1. the corrected MIMIC ranking figure in the primary CausalForestDML section, near the MIMIC table;
2. the corrected PhysioNet ranking figure near the PhysioNet table;
3. the corrected direction-agreement figure in the CausalPFN exploratory section.

Use unique labels such as:

```latex
\label{fig:results-mimic-forest-ranking}
\label{fig:results-physionet-forest-ranking}
\label{fig:results-three-estimator-direction}
```

Use the required caption boundaries above.

Cross-reference each figure in the surrounding prose.

Do not change:

* any validated Chapter 10 numerical table value;
* the section order;
* estimator hierarchy;
* population hierarchy;
* 18-of-19 conclusion;
* PhysioNet shock exception;
* downsampling interpretation;
* predictive results;
* matching values;
* robustness counts.

Make only small prose edits needed to introduce the figures and remove the former blocked-figure explanation.

---

# 10. Update Figure and Result Registers

Update:

```text
thesis-writing/results/figure_selection_register.md
thesis-writing/results/checked_figure_candidates.csv
thesis-writing/results/results_manifest.csv
thesis-writing/results/results_manifest.md
thesis-writing/results/results_source_packet.md
thesis-writing/results/results_checksums.sha256
```

For the three original conflicting optional figures:

* retain their original hashes;
* retain `BLOCKED_VALUE_CONFLICT`;
* retain `EXCLUDED_FROM_THESIS`;
* state that corrected generated replacements now exist.

Add the three corrected generated figures with:

```text
artifact role = generated checked result figure
source = checked_cate_candidates.csv
validation = SOURCE_EXACT_GENERATED
selection = MAIN_RESULTS
admission = ADMISSIBLE_WITH_QUALIFICATIONS
```

Record:

* relative path;
* SHA-256;
* dimensions;
* generating script;
* selection filter;
* source row count;
* chapter placement;
* caption boundary;
* provenance notes.

Regenerate the checksum file.

Do not hash the checksum file into itself.

---

# 11. Update Logs

Update:

```text
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

Resolve or update:

```text
DF-4.6B-001 — ranking-figure conflicts
```

Mark it resolved only when:

* all three corrected figures exist;
* all pass numerical validation;
* all are inserted;
* the original conflicting files remain excluded.

Retain:

* supervisor ratification;
* CausalPFN primary citation;
* missing numbered causal configs;
* predictive split and checkpoint lineage;
* raw cohort-total limitations;
* appendix placement;
* final causal-language review.

Do not mark unrelated limitations resolved.

---

# 12. Repair Evidence Report

Create:

```text
thesis-writing/logs/stage_4_6B_figure_repair_report.md
```

Include:

## 12.1 Commit and worktree

* verified `fb40839`;
* branch;
* initial worktree;
* final worktree;
* files changed.

## 12.2 Blocking issue

Explain separately:

* ranking-plot aggregation conflict;
* direction-count sampling-mode conflict;
* why the original PNGs remain excluded.

## 12.3 Generation script

Record:

* path;
* input file;
* required columns;
* filters;
* duplicate checks;
* missing-value checks;
* deterministic behavior.

## 12.4 MIMIC ranking figure

Record:

* exact nine source rows;
* exact mean-CATE values;
* sorted order;
* display labels;
* output hash;
* dimensions;
* Chapter 10 label.

## 12.5 PhysioNet ranking figure

Record:

* exact ten source rows;
* exact values;
* sorted order;
* negative shock validation;
* output hash;
* dimensions;
* Chapter 10 label.

## 12.6 Direction-agreement figure

Record:

* all 19 joined original-cohort comparisons;
* MIMIC 9/9;
* PhysioNet 9/10;
* total 18/19;
* PhysioNet shock exception;
* output hash;
* dimensions;
* Chapter 10 label.

## 12.7 Chapter changes

Record:

* removed blocked-figure paragraph;
* inserted figure locations;
* captions;
* cross-references;
* confirmation that validated table values did not change.

## 12.8 Manifest updates

Record:

* new manifest rows;
* original blocked rows retained;
* checksum regeneration.

## 12.9 Numerical integrity

Compare all Chapter 10 table values before and after.

Result must be:

```text
0 numerical table changes
```

## 12.10 Final build and readiness

Record the final build, warnings, and readiness.

---

# 13. Allowed Edit Scope

You may create:

```text
thesis-writing/results/generate_stage_4_6B_main_figures.py
thesis-writing/thesis/figures/results_mimic_forest_original_cate_ranking.png
thesis-writing/thesis/figures/results_physionet_forest_original_cate_ranking.png
thesis-writing/thesis/figures/results_original_three_estimator_direction_agreement.png
thesis-writing/logs/stage_4_6B_figure_repair_report.md
```

You may edit:

```text
thesis-writing/thesis/chapters/10_results.tex
thesis-writing/results/figure_selection_register.md
thesis-writing/results/checked_figure_candidates.csv
thesis-writing/results/results_manifest.csv
thesis-writing/results/results_manifest.md
thesis-writing/results/results_source_packet.md
thesis-writing/results/results_checksums.sha256
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

A build may update:

```text
thesis-writing/thesis/main.pdf
```

Do not edit:

```text
checked_cate_candidates.csv
other checked numerical CSVs
Chapters 1–9
Chapters 11–12
front matter
appendix prose
research code
causal configs
source result artifacts
the 16 optional source PNGs
bibliography files
planning files
audit files
```

---

# 14. Final Validation

## 14.1 Source-value integrity

Confirm:

* `checked_cate_candidates.csv` is byte-for-byte unchanged;
* all numerical checked CSVs are semantically unchanged;
* no Chapter 10 table value changed;
* no normalized CATE was used.

## 14.2 Figure integrity

Confirm:

* exactly three corrected generated figures are inserted;
* all three are generated from the checked CSV;
* no conflicting optional figure is inserted;
* no downsampled row appears in an original-cohort figure;
* figures contain all required exposures or comparisons;
* values and counts are exact before display rounding.

## 14.3 Estimator-agreement integrity

Recalculate:

```text
MIMIC = 9/9
PhysioNet = 9/10
Overall = 18/19
```

Confirm the sole exception is PhysioNet shock.

## 14.4 Causal-language scan

Search Chapter 10 and captions for:

```text
ATE
ATT
risk ratio
relative risk
percent reduction
statistically significant
caused mortality
proves causality
validated causal effect
clinically actionable
positivity established
confounding eliminated
superior estimator
equivalent estimators
```

Review every occurrence.

Remove or qualify inappropriate uses.

## 14.5 InterpNet scan

Run:

```bash
grep -Rni "InterpNet\|interpnet" thesis-writing/thesis \
  --include='*.tex' || true
```

There must be no occurrence.

## 14.6 LaTeX build

Run:

```bash
cd thesis-writing/thesis
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

Scan for:

```text
LaTeX Error
Undefined control sequence
Citation ... undefined
Reference ... undefined
There were undefined references
multiply defined
Biber error
Emergency stop
Fatal error
```

Record layout warnings separately.

Inspect the rendered pages containing all three figures for:

* cropping;
* unreadable labels;
* text outside page bounds;
* distorted aspect ratios;
* captions separated incorrectly from figures.

---

# 15. Readiness Decision

Use:

```text
READY FOR STAGE 4.7
```

only when:

* all three corrected figures pass numerical validation;
* all are inserted;
* original conflicting files remain excluded;
* Chapter 10 tables remain unchanged;
* 18-of-19 is reproduced;
* the thesis builds successfully;
* no prohibited causal language appears.

Use:

```text
BLOCKED BEFORE STAGE 4.7
```

when any generated figure:

* uses an incorrect source row;
* uses downsampled data;
* omits a required exposure;
* has a value mismatch;
* misstates its aggregation;
* cannot be rendered readably;
* or causes a fatal thesis-build error.

Supervisor ratification remains pending but is non-blocking for drafting Stage 4.7.

Do not begin Stage 4.7.

---

# 16. Completion Response

Report:

1. Verified Stage 4.6B commit.
2. Original blocking issue.
3. Figure-generation script path.
4. MIMIC figure path, hash, dimensions, and nine-row validation.
5. PhysioNet figure path, hash, dimensions, and ten-row validation.
6. Direction-agreement figure path, hash, dimensions, and 18-of-19 validation.
7. PhysioNet shock exception.
8. Original conflicting-figure status.
9. Chapter 10 figure labels and placements.
10. Confirmation that Chapter 10 table values did not change.
11. Manifest and checksum updates.
12. InterpNet scan.
13. Causal-language scan.
14. Final build result and page count.
15. Remaining warnings.
16. Stage 4.7 readiness.

Do not commit or push.

Do not begin Stage 4.7.
