# Stage 4.9A — Draft Chapter 2: Background and Related Work

## Model

Use:

```text
GPT-5.6 Terra
Reasoning: High
```

## Task Declaration

Implement this task as:

```text
Stage 4.9A — Draft Chapter 2: Background and Related Work
```

The latest approved commit is expected to be:

```text
4fa3f4f step 4.8
```

Its immediate predecessor is expected to be:

```text
12acb9a step 4.7
```

Target:

```text
thesis-writing/thesis/chapters/02_background_related_work.tex
```

This stage drafts Chapter 2 only.

The next substep is:

```text
Stage 4.9B — Chapter 2 Literature, Citation, and Cross-Chapter Consistency Audit
Model: GPT-5.6 Terra
Reasoning: High
```

Do not begin Stage 4.9B.

Do not edit bibliography or literature-corpus files in Stage 4.9A.

---

# 1. Objective

Write a rigorous Background and Related Work chapter that:

1. introduces the two ICU datasets and their research context;
2. explains the methodological challenges of sparse and irregular clinical time series;
3. reviews the five predictive model families included in the thesis;
4. positions rule-derived proxy states within electronic phenotyping and weak supervision;
5. introduces causal diagrams, identification assumptions, double machine learning, causal forests, heterogeneous-effect estimation, overlap, and sensitivity analysis;
6. distinguishes prior literature from the implementation and findings of this thesis;
7. explains how the literature motivates the integrated pipeline;
8. identifies the limitations of applying existing methods to the thesis’s proxy-state exposures;
9. prepares the reader for Chapters 3–9 without repeating their implementation detail;
10. uses only verified citations from the canonical local literature corpus.

Chapter 2 should synthesize literature rather than present a sequence of disconnected paper summaries.

It must show:

* what the main methodological traditions contribute;
* where they differ;
* why none alone supplies the complete thesis workflow;
* how the current thesis combines them;
* which assumptions and gaps remain.

Do not report the thesis’s numerical findings in Chapter 2.

---

# 2. Mandatory Initial Verification

From the repository root, run:

```bash
git status --short
git branch --show-current
git log -10 --oneline
git show --stat --oneline 4fa3f4f
git diff --stat 12acb9a..4fa3f4f
```

Verify:

* `4fa3f4f` exists;
* its message is `step 4.8`;
* it follows `12acb9a step 4.7`;
* `stage_4_8_evidence_report.md` exists;
* Stage 4.8 reports a successful 107-page build;
* Stage 4.8 reports readiness for Stage 4.9A;
* Chapter 1 is complete;
* Chapter 2 remains a four-section placeholder skeleton;
* Chapters 3–11 remain completed;
* Chapter 12 remains undrafted;
* no InterpNet occurrence exists in any thesis `.tex` file.

Run:

```bash
grep -Rni "InterpNet\|interpnet" thesis-writing/thesis \
  --include='*.tex' || true
```

There must be no match.

Record initial and final worktree states.

Do not:

* reset;
* clean;
* checkout another branch;
* discard unrelated changes;
* stage;
* amend;
* commit;
* push.

---

# 3. Baseline Thesis Build

Run:

```bash
cd thesis-writing/thesis
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

Record:

* return status;
* PDF path;
* page count;
* unresolved citations;
* unresolved references;
* duplicate labels;
* Biber errors;
* fatal errors;
* layout warnings.

The expected baseline is approximately 107 pages, but verify it directly.

---

# 4. Canonical Literature Corpus

Use the following as the sole literature source of truth:

```text
thesis-writing/literature/README.md
thesis-writing/literature/metadata/catalog.csv
thesis-writing/literature/metadata/references.bib
thesis-writing/literature/metadata/checksums.sha256
thesis-writing/literature/papers/
thesis-writing/literature/optional/
```

At the approved commit, the documented corpus contains:

```text
35 core catalog entries
5 optional catalog entries
38 valid local PDFs
2 missing PDFs
```

Verify these counts rather than assuming that the dirty working tree still matches them.

Inspect:

```bash
find thesis-writing/literature/papers \
  -maxdepth 1 -type f -name '*.pdf' | sort

find thesis-writing/literature/optional \
  -maxdepth 1 -type f -name '*.pdf' | sort

wc -l thesis-writing/literature/metadata/catalog.csv
grep -c ',core,' thesis-writing/literature/metadata/catalog.csv
grep -c ',optional,' thesis-writing/literature/metadata/catalog.csv
grep -c ',present,' thesis-writing/literature/metadata/catalog.csv
grep -c ',missing,' thesis-writing/literature/metadata/catalog.csv
```

Where practical, validate the literature checksums without changing any file.

Do not:

* browse the web;
* download a paper;
* add a citation;
* edit BibTeX;
* edit the catalog;
* change a PDF;
* move a PDF;
* regenerate corpus checksums.

Any literature gap discovered in this stage must be logged for Stage 4.9B.

---

# 5. Reading the Literature

Do not draft Chapter 2 solely from citation titles or planning summaries.

For each paper materially cited in Chapter 2:

1. verify the citation key in `references.bib`;
2. verify its catalog row;
3. inspect its local PDF when available;
4. read enough of the abstract, introduction, methods, discussion, and limitations to support the proposed claim;
5. record the exact conceptual role and usage boundary in the evidence report.

Temporary text extraction may be written outside the repository, for example:

```text
/tmp/clinicause_stage_4_9A_literature/
```

Do not commit extracted text.

When a PDF is missing:

* use only the verified bibliographic metadata and previously approved limited role;
* do not make detailed claims about methods or findings that were not verified;
* record the limitation.

Paraphrase the literature.

Do not insert long quotations.

---

# 6. Required Thesis Inspection

Read the complete current versions of:

```text
thesis-writing/thesis/chapters/01_introduction.tex
thesis-writing/thesis/chapters/02_background_related_work.tex
thesis-writing/thesis/chapters/03_problem_definition_study_design.tex
thesis-writing/thesis/chapters/04_data_preprocessing.tex
thesis-writing/thesis/chapters/05_proxy_state_construction.tex
thesis-writing/thesis/chapters/06_predictive_modeling.tex
thesis-writing/thesis/chapters/07_causal_methodology.tex
thesis-writing/thesis/chapters/08_robustness_sensitivity_validation.tex
thesis-writing/thesis/chapters/09_experimental_design.tex
thesis-writing/thesis/chapters/10_results.tex
thesis-writing/thesis/chapters/11_discussion.tex
```

Use the completed chapters to prevent Chapter 2 from:

* contradicting thesis terminology;
* claiming a method that was not implemented;
* restoring an excluded model;
* duplicating methods prose;
* introducing unsupported results;
* overstating clinical or causal interpretation.

Chapter 2 supplies conceptual background.

Chapters 3–9 define the actual project design and implementation.

Chapter 10 supplies results.

Chapter 11 supplies interpretation and limitations.

---

# 7. Required Planning and Audit Inspection

Inspect:

```text
thesis-writing/planning/thesis_story.md
thesis-writing/planning/thesis_outline.md
thesis-writing/planning/chapter_evidence_map.md
thesis-writing/planning/citation_plan.md
thesis-writing/planning/table_plan.md
thesis-writing/planning/terminology_and_notation.md
thesis-writing/planning/writing_order.md
thesis-writing/planning/stage4_prompt_queue.md
```

Inspect:

```text
thesis-writing/audit/claim_evidence_ledger.csv
thesis-writing/audit/evidence_inventory.md
thesis-writing/audit/unresolved_questions.md
thesis-writing/audit/terminology_map.md
```

Inspect:

```text
thesis-writing/logs/stage_4_7_evidence_report.md
thesis-writing/logs/stage_4_8_evidence_report.md
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

Old planning entries may contain superseded statements.

Follow the latest approved thesis state.

In particular:

* InterpNet is not part of the thesis pipeline;
* it must not appear in Chapter 2;
* it must not appear in a model table;
* it must not appear as future work;
* it must not appear as a missing result;
* the predictive comparison contains exactly STraTS, GRU, GRU-D, TCN, and SAnD;
* CausalForestDML is primary;
* LinearDML is secondary;
* CausalPFN is exploratory;
* original cohorts are primary;
* outcome-downsampled analyses are robustness analyses;
* the result manifest now exists;
* Chapter 10 and Chapter 11 are complete.

Do not edit stale planning files during this task.

---

# 8. Required Chapter Structure

Preserve exactly these four top-level sections:

```latex
\section{ICU EHR Datasets and Irregular Sampling}

\section{Irregular Time-Series Representation Models}

\section{Proxy Phenotyping and Weak Supervision}

\section{Causal Diagrams, Identification, HTE, Overlap, and Sensitivity}
```

Minor polishing of capitalization is permitted, but retain:

* the four-section structure;
* existing labels;
* section order.

Subsections may be added.

Recommended structure:

```text
2.1 ICU EHR Datasets and Irregular Sampling
    2.1.1 Critical-care EHR time series
    2.1.2 PhysioNet Challenge 2012
    2.1.3 MIMIC-III and benchmark research
    2.1.4 Dataset differences relevant to this thesis

2.2 Irregular Time-Series Representation Models
    2.2.1 Representation challenges
    2.2.2 Recurrent baselines: GRU and GRU-D
    2.2.3 Temporal convolution: TCN
    2.2.4 Attention-based dense modeling: SAnD
    2.2.5 Sparse-event transformer modeling: STraTS
    2.2.6 Relationship among the five included families

2.3 Proxy Phenotyping and Weak Supervision
    2.3.1 Electronic phenotyping and rule-derived labels
    2.3.2 Clinical-definition sources and approximation
    2.3.3 Weak supervision and label aggregation
    2.3.4 Implications for this thesis’s proxy states

2.4 Causal Diagrams, Identification, HTE, Overlap, and Sensitivity
    2.4.1 Observational causal questions and target-trial thinking
    2.4.2 DAGs and adjustment
    2.4.3 Double machine learning
    2.4.4 Causal forests and heterogeneous effects
    2.4.5 ICU and EHR HTE challenges
    2.4.6 Overlap and empirical support
    2.4.7 Omitted-variable sensitivity and diagnostic workflows
    2.4.8 Positioning of the present study
```

Do not create a fifth top-level section.

---

# 9. Section 2.1 — ICU EHR Datasets and Irregular Sampling

## 9.1 Critical-care data characteristics

Explain why ICU EHR data are challenging:

* nonuniform observation times;
* variable-specific sampling rates;
* informative measurement;
* missingness;
* heterogeneous illness trajectories;
* high-dimensional measurements;
* differences in care processes;
* administrative and physiological data mixed in one record;
* repeated records or stays;
* temporal and cohort-definition ambiguity.

Use careful wording.

Do not imply that missingness is always informative or always clinically meaningful.

Explain that the observation process may carry information, while also creating bias and representation challenges.

Relevant literature may include:

```text
sun_2026_review_irregular_medical_timeseries
lipton_kale_wetzel_2016_missingness_rnns
che2018grud
```

## 9.2 PhysioNet Challenge 2012

Use:

```text
silva2012physionet
```

Discuss:

* its role as a critical-care time-series mortality challenge;
* early ICU measurements;
* structured critical-care research use;
* why it provides a useful smaller benchmark-style data source.

Do not claim that the local preprocessing exactly reproduces every original challenge rule.

Do not give a local cohort count in Chapter 2.

## 9.3 MIMIC-III

Use:

```text
johnson2016mimiciii
harutyunyan_2019_mimiciii_benchmark
```

Discuss:

* MIMIC-III as a large, openly accessible critical-care database under controlled access requirements;
* its use in clinical time-series research;
* benchmark-task development;
* the difference between the database itself and any particular benchmark extraction.

Do not say that this thesis uses the Harutyunyan benchmark tasks unchanged.

## 9.4 Dataset comparison

Compare PhysioNet and MIMIC conceptually across:

```text
data-source role
scale
data era
available variables
identifier structure
measurement process
preprocessing needs
benchmark versus database character
```

Do not use unverified raw counts.

Do not imply that similarly named variables or proxy states are identical.

End the section by explaining that the thesis uses both datasets to evaluate methodological portability, not to pool them into one population.

## 9.5 Dataset table

Create a concise conceptual table:

```latex
\label{tab:background-datasets}
```

Suggested columns:

```text
Dataset
Research role
Longitudinal structure
Strength for this thesis
Important limitation
```

Do not include a row claiming an exact local cohort size.

Do not include a raw variable count unless verified from the cited paper and needed.

---

# 10. Section 2.2 — Irregular Time-Series Representation Models

Review exactly these five model families:

```text
STraTS
GRU
GRU-D
TCN
SAnD
```

No other predictive architecture should be included as part of the thesis comparison.

## 10.1 General representation choices

Explain the main representation strategies:

* recurrent state updates;
* explicit missingness and elapsed-time features;
* temporal convolution;
* attention over dense or regularly represented sequences;
* sparse event or triplet representations;
* masking and positional or temporal encoding;
* self-supervised pretraining.

Do not imply that every listed property is present in every implementation.

Distinguish the literature’s canonical model from this thesis’s local adaptation.

## 10.2 GRU

Use:

```text
cho2014gru
```

Explain:

* recurrent gated state updates;
* reset and update gates at a conceptual level;
* strength as a general sequence baseline;
* limitation that ordinary GRU modeling does not inherently solve irregular observation and informative missingness.

Do not turn the section into a full derivation.

## 10.3 GRU-D

Use:

```text
che2018grud
```

Explain:

* masks;
* elapsed-time information;
* trainable decay toward reference values or hidden states;
* the motivation for modeling missingness explicitly.

State that GRU-D provides one approach to irregularity and missingness, not a universal solution.

## 10.4 TCN

Use:

```text
bai2018tcn
```

Explain:

* causal or temporally ordered convolution;
* dilation;
* receptive fields;
* residual blocks;
* parallel sequence computation;
* the need for an appropriate regular sequence representation in this thesis’s setting.

Do not call the generic TCN paper an ICU-specific model paper.

## 10.5 SAnD

Use:

```text
song2018sand
```

Explain:

* masked self-attention for clinical time series;
* dense sequence representation;
* positional encoding;
* long-range dependencies;
* differences from sparse-event modeling.

Do not imply that SAnD directly consumes the same raw representation as STraTS.

## 10.6 STraTS

Use:

```text
tipirneni2022strats
```

Explain:

* sparse and irregular event representation;
* measurement-value-time triplets;
* transformer-style contextualization;
* self-supervised pretraining;
* suitability for sparse clinical event streams.

Separate:

* the published STraTS method;
* the local multi-label adaptation described later in Chapter 6.

Do not insert local performance values.

## 10.7 Comparative synthesis

Compare the five families by:

```text
input representation
temporal mechanism
explicit missingness handling
irregular-time handling
pretraining role
main inductive bias
role in the thesis
```

Avoid ranking them.

State that architecture suitability can depend on:

* dataset structure;
* missingness;
* measurement density;
* preprocessing;
* task definition;
* label construction;
* sample size.

Do not discuss which model led the local results; that belongs in Chapters 10 and 11.

## 10.8 Model-family table

Create:

```latex
\label{tab:background-model-families}
```

Include exactly five rows.

Suggested columns:

```text
Model
Core temporal mechanism
Input/irregularity handling
Role in this thesis
Primary reference
```

The primary-reference column may contain textual citation commands or citations in the row description.

Do not include InterpNet.

---

# 11. Absolute InterpNet Exclusion

Run before and after drafting:

```bash
grep -Rni "InterpNet\|interpnet" thesis-writing/thesis \
  --include='*.tex' || true
```

There must be no occurrence.

This applies even though:

* the literature corpus retains historical InterpNet papers;
* old planning files mention it;
* source code may contain an implementation.

Do not mention InterpNet in:

* prose;
* tables;
* captions;
* footnotes;
* future work;
* limitations;
* a missing-results statement;
* a comparison of excluded models.

Do not edit or delete the historical literature metadata in this stage.

---

# 12. Section 2.3 — Proxy Phenotyping and Weak Supervision

## 12.1 Electronic phenotyping

Use:

```text
banda_2018_electronic_phenotyping
essay_mosier_subbian_2020_acute_respiratory_failure_phenotyping
```

Explain that electronic phenotyping may use:

* codes;
* measurements;
* medications;
* temporal criteria;
* deterministic rules;
* statistical models;
* machine learning;
* combinations of sources.

Discuss advantages of rule-based phenotypes:

* transparency;
* reproducibility;
* domain interpretability;
* direct inspectability.

Discuss limitations:

* threshold sensitivity;
* missing variables;
* site dependence;
* temporal ambiguity;
* label noise;
* incomplete clinical equivalence;
* limited transportability.

## 12.2 Clinical-definition literature

Use clinical sources only to provide conceptual grounding for rule families.

Potential keys include:

```text
singer_2016_sepsis3
kdigo_2012_acute_kidney_injury
taylor_et_al_2001_isth_dic
vincent_et_al_1996_sofa
ranieri_et_al_2012_berlin_ards
```

Use them carefully.

Do not state that the project exactly implements:

* Sepsis-3;
* KDIGO staging;
* ISTH DIC scoring;
* SOFA;
* Berlin ARDS criteria;

unless the active rule code demonstrably implements the complete definition.

Preferred wording:

```text
clinically informed by
draws on concepts from
approximates part of
uses measurements related to
```

Avoid:

```text
implements the clinical definition
diagnoses
confirms
meets the formal criteria
```

## 12.3 Weak supervision

Use:

```text
ratner_et_al_2020_snorkel
```

Explain:

* the motivation for combining imperfect labeling sources;
* correlated and noisy labeling functions;
* the distinction between majority voting and a learned label model;
* why source quality and dependence matter.

State explicitly that this thesis uses deterministic majority voting rather than a Snorkel generative label model.

Do not describe the vote as clinical consensus.

## 12.4 Proxy-state terminology

Use consistently:

```text
clinically inspired proxy state
rule-derived proxy label
proxy phenotype
weak clinical label
```

Explain that repository identifiers such as `LAT_*` are historical software names.

They do not establish a formally identified latent clinical variable.

State that proxy-state validity must be separated into:

```text
implementation validity
schema consistency
predictive learnability
clinical construct validity
causal measurement validity
```

The thesis supports the first three more strongly than the last two.

## 12.5 Relationship to this thesis

Explain that the proxy layer serves three connected roles:

1. supervised prediction target;
2. algorithmic aggregation input;
3. downstream exposure representation.

Discuss the benefit of the shared interface.

Also discuss the risk that error can propagate from:

```text
rule design
→ model prediction
→ vote aggregation
→ DAG-guided causal analysis
```

Do not present predictive accuracy against rule labels as clinical validation.

Do not report local proxy prevalence or mortality associations here.

---

# 13. Section 2.4 — Causal Diagrams, Identification, HTE, Overlap, and Sensitivity

This section should build a coherent conceptual progression:

```text
causal question
→ intervention and estimand definition
→ DAG and identification
→ estimation
→ heterogeneity
→ support
→ sensitivity
→ diagnostic interpretation
```

## 13.1 Observational causal questions

Use:

```text
hernan_robins_2016_target_trial
hernan_taubman_2008_well_defined_interventions
smit_2023_causal_inference_icu_scoping_review
```

Discuss:

* the importance of defining a causal question before choosing an estimator;
* eligibility, time zero, exposure, outcome, follow-up, and analysis;
* well-defined intervention concerns;
* challenges specific to retrospective ICU data;
* time-varying clinical processes;
* confounding by indication;
* measurement and selection bias.

State that this thesis does not implement a complete target-trial emulation.

Mention marginal structural models only as optional longitudinal background when useful, using:

```text
robins_hernan_brumback_2000_msm
```

Clearly state that marginal structural modeling is not the implemented point/binary exposure estimator.

## 13.2 DAGs and adjustment

Use:

```text
pearl_1995_causal_diagrams
```

Explain:

* DAGs as explicit representations of causal assumptions;
* paths;
* confounding;
* colliders;
* backdoor reasoning;
* adjustment-set selection;
* distinction between graphical assumptions and empirical evidence.

State that:

* a DAG does not prove its arrows;
* an adjustment set does not guarantee exchangeability;
* an observed-data restriction can leave required confounders unavailable;
* temporal order must be scientifically justified.

Relate this to the thesis only at a high level.

Do not repeat dataset-specific node lists from Chapter 7.

## 13.3 Double machine learning

Use:

```text
chernozhukov2018dml
```

Explain conceptually:

* nuisance functions;
* outcome and treatment models;
* residualization or orthogonal scores;
* cross-fitting;
* reduction of first-order sensitivity to nuisance-estimation error;
* flexibility to use machine-learning nuisance estimators.

Clarify that DML does not solve:

* unmeasured confounding;
* positivity failure;
* exposure misclassification;
* incorrect DAGs;
* ill-defined interventions.

## 13.4 EconML implementation context

Use when relevant:

```text
oprescu_et_al_2019_econml
```

Separate:

* theoretical method references;
* software implementation references.

Do not use a software paper as the sole foundation for DML or causal forests.

## 13.5 Causal forests and generalized random forests

Use:

```text
wager2018causalforest
athey2019grf
```

Explain:

* recursive partitioning for treatment-effect heterogeneity;
* forest aggregation;
* adaptive subgroup structure;
* distinction between outcome prediction and effect estimation;
* heterogeneous conditional effects.

Do not imply that high-dimensional effect estimates are automatically validated or clinically actionable.

## 13.6 Heterogeneous effects in EHR and critical care

Use:

```text
bica_2021_individualized_treatment_effects_ehr_ml
lipkovich_2024_modern_hte_methods
curth_2024_ml_individualized_treatment_effects
iwashyna_2015_hte_critical_care
```

Discuss:

* interest in individual or conditional treatment-effect estimation;
* challenges of validation;
* model dependence;
* treatment-effect heterogeneity versus prognostic heterogeneity;
* covariate shift;
* subgroup instability;
* clinical interpretation;
* the difficulty of evaluating individual effects when counterfactual outcomes are unobserved.

Do not imply that this thesis validates individualized clinical treatment effects.

## 13.7 CausalPFN boundary

The thesis includes CausalPFN as an exploratory estimator, but the approved literature corpus does not contain a verified primary CausalPFN method source.

During Stage 4.9A:

* do not invent a citation;
* do not infer architecture details from its name;
* do not give a theoretical description unsupported by a primary source;
* do not claim that it belongs to a specific established method family without evidence;
* do not remove it from later completed chapters;
* do not edit bibliography files.

Chapter 2 may omit a technical CausalPFN subsection.

When a transition requires acknowledging the estimator set, use only a restrained project-scope sentence such as:

```text
The empirical study additionally evaluates CausalPFN in an exploratory
role; its detailed theoretical positioning remains limited pending a
verified primary method reference.
```

This sentence is optional.

Retain the citation gap in the tracking log for Stage 4.9B.

## 13.8 Overlap and support

Use:

```text
crump_et_al_2009_limited_overlap
```

Explain:

* positivity;
* common support;
* poor overlap;
* extrapolation;
* instability;
* implications for heterogeneous-effect estimation;
* distinction between sample overlap and identification assumptions.

Explain that matching rates or distances can provide indirect information but do not replace:

* propensity-score inspection;
* support plots;
* covariate-balance assessment.

Do not imply that the thesis established positivity.

## 13.9 Sensitivity analysis

Use as primary sources:

```text
cinelli_hazlett_2020_sensitivity
chernozhukov_et_al_2026_ovb_causal_ml
```

Explain:

* omitted-variable-bias sensitivity;
* robustness values or related strength-of-confounding summaries;
* benchmarking against observed covariates;
* the role of sensitivity analysis in quantifying assumptions.

Clarify that sensitivity analysis:

* does not prove no unmeasured confounding;
* depends on its model and scale;
* does not validate the DAG;
* does not repair exposure misclassification.

Optional E-value background may be mentioned using:

```text
vanderweele_ding_2017_evalue
ding_vanderweele_2016_sensitivity_without_assumptions
```

only when it adds genuine conceptual value.

Do not imply that E-values are the implemented method.

## 13.10 Diagnostic or refutation workflows

Use when appropriate:

```text
sharma_kiciman_2020_dowhy
```

Discuss the general value of:

* assumption checks;
* refutation or perturbation diagnostics;
* placebo or permutation-style reasoning;
* transparent modeling workflows.

Do not claim that every DoWhy refuter was implemented.

Do not describe the thesis permutation procedure as a formal randomization test.

## 13.11 Positioning the present study

End Section 2.4 with a synthesis that explains the gap addressed by this thesis:

* irregular-time-series models address representation and prediction;
* phenotyping literature addresses label construction;
* weak supervision addresses combining imperfect sources;
* DAG literature addresses identification assumptions;
* DML and forest literature address flexible adjusted estimation;
* HTE literature addresses conditional-effect modeling;
* overlap and sensitivity literature address fragility and assumptions.

The present study integrates these layers into one auditable pipeline.

State that the contribution is the integration and evaluation of the workflow, not invention of every component.

State that the integration creates compounded risks:

```text
measurement error
model error
aggregation error
DAG misspecification
support limitations
unmeasured confounding
provenance gaps
```

This synthesis should connect naturally to Chapter 3.

---

# 14. Results Boundary

Chapter 2 must not report thesis-specific results.

Do not state:

* which predictive model led either dataset;
* any AUROC, AUPRC, minRP, or loss value;
* any CATE value;
* any exposure ranking;
* 19-of-19 agreement;
* 18-of-19 agreement;
* matching agreement counts;
* downsampling agreement counts;
* population counts;
* sensitivity results;
* permutation results.

Permitted thesis-specific statements are limited to design scope, such as:

```text
the thesis compares five predictive model families
the thesis analyzes two ICU datasets
the thesis uses project-specified DAGs
the thesis evaluates DML, forest-based, matching, and exploratory
estimator outputs
```

Reserve findings for Chapters 10 and 11.

---

# 15. Literature Synthesis Standards

For every substantive paragraph:

* identify the concept being synthesized;
* cite the most appropriate primary or authoritative source;
* compare related methods when useful;
* state limitations;
* explain relevance to this thesis;
* avoid citation dumping.

Do not write:

```text
Paper A did X. Paper B did Y. Paper C did Z.
```

Prefer thematic synthesis:

```text
Recurrent, missingness-aware, convolutional, attention-based, and
sparse-event transformer approaches encode distinct assumptions about
how irregular clinical observations should be represented...
```

Then support the synthesis with the relevant sources.

Use original method papers as primary references.

Use reviews for:

* field-level framing;
* taxonomy;
* limitations;
* broader context.

Do not use a review as the sole source for a method when the primary source exists in the corpus.

---

# 16. Citation Density and Placement

Aim for citations that are sufficient but not excessive.

General guidance:

* one citation cluster may support several closely related sentences;
* cite primary papers near their first substantive description;
* repeat a citation only when needed for clarity;
* do not attach five or more citations to every sentence;
* avoid unsupported paragraphs;
* avoid paragraphs consisting mostly of citations.

All citation keys must exist exactly in:

```text
thesis-writing/literature/metadata/references.bib
```

Do not cite filenames or catalog rows in reader-facing prose.

Do not cite repository files as academic literature.

---

# 17. Tables

Chapter 2 should include exactly two main synthesis tables:

```text
tab:background-datasets
tab:background-model-families
```

A third compact causal-concepts table may be added only when it materially improves readability.

An optional third table label is:

```latex
\label{tab:background-causal-concepts}
```

Possible columns:

```text
Concept
Purpose
Key assumption or limitation
Role in this thesis
Primary reference
```

Possible rows:

```text
DAG/backdoor reasoning
Double machine learning
Causal forests/GRF
Overlap/common support
OVB sensitivity
```

Do not include a CausalPFN technical row without a primary citation.

Do not include local numerical results in any Chapter 2 table.

---

# 18. Figures

Do not insert a figure in Chapter 2 during Stage 4.9A.

Do not copy:

```text
icu_scattered_events.png
latent_state_tags.png
majority_vote_tag_cleaning.png
shock_rule_tree.png
```

Those figures remain candidates for later whole-thesis visual review or appendices.

Do not use:

```text
causal_dag_overview.png
correlation_vs_causation.png
```

They are excluded.

Do not create a literature taxonomy figure.

---

# 19. LLM Role

Do not create a dedicated LLM literature subsection.

The canonical corpus does not yet contain an approved LLM methodology or medical-expertise evidence base for a formal Chapter 2 review.

The thesis’s LLM role is limited to:

```text
candidate design provenance for proxy-state and DAG elicitation
```

It is not:

```text
an executed pipeline component
causal discovery
clinical validation
a medical expert
an estimator
```

A brief positioning statement may appear where the present study is synthesized, but it must defer details to Chapters 5 and 7.

Do not add LLM citations.

Retain any LLM literature need in the tracking logs for Stage 4.9B or later review.

---

# 20. Terminology

Use consistently:

```text
irregular clinical time series
ICU EHR data
clinically inspired proxy state
rule-derived proxy label
proxy phenotype
weak clinical label
algorithmic aggregation
project-specified DAG
DAG-guided adjustment
double machine learning
heterogeneous-effect estimation
conditional average treatment effect
overlap
common support
omitted-variable-bias sensitivity
```

Use `conditional average treatment effect` only as general literature terminology.

When describing local thesis outputs, retain the established wording in later chapters.

Avoid:

```text
true latent state
verified diagnosis
ground truth
clinical consensus
LLM-discovered DAG
learned causal graph
proven causal effect
causal certainty
validated treatment effect
clinically actionable estimator
```

---

# 21. Chapter Length and Style

Aim for approximately:

```text
4,500–7,000 words
```

excluding tables.

The chapter should be substantial enough to ground the thesis but shorter than a standalone systematic review.

Use:

* formal academic prose;
* clear transitions;
* topic sentences;
* comparative synthesis;
* explicit limitations;
* consistent terminology.

Avoid:

* extensive equations;
* detailed code behavior;
* exact local file paths;
* implementation tutorials;
* numerical results;
* repetition of the Introduction;
* repetition of Chapters 6–8;
* unstructured lists of papers.

Equations may be used sparingly when a concept cannot be explained clearly without one.

---

# 22. Generic Placeholder Removal

Remove all Chapter 2 generic placeholders:

```text
[STAGE 4 DRAFT REQUIRED]
[VALIDATION REQUIRED]
[CITATION REQUIRED]
```

Do not leave an unresolved generic stage marker in Chapter 2.

Specific unresolved issues must be recorded in the tracking logs rather than as generic reader-facing placeholders.

---

# 23. Placeholders and Deferred Fixes

Update:

```text
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

Mark the generic Chapter 2 draft, validation, and citation placeholders resolved.

Retain or add precise gates for:

```text
[CITATION REQUIRED: primary CausalPFN method source]
[CLINICAL REVIEW REQUIRED: proxy-state definitions and clinical interpretation]
[LITERATURE REVIEW REQUIRED: verify whether any rule family lacks an adequate clinical source]
[LITERATURE REVIEW REQUIRED: decide whether formal LLM-assisted-design literature is needed]
[SUPERVISOR RATIFICATION REQUIRED: final related-work framing]
```

Do not duplicate equivalent existing entries.

Add deferred fixes beginning:

```text
DF-4.9A-
```

Assess at minimum:

1. CausalPFN primary-source gap;
2. final clinical-reference coverage by proxy family;
3. LLM-assisted-design literature decision;
4. Chapter 1–2 motivation overlap;
5. Chapter 2–6 model-background overlap;
6. Chapter 2–7 causal-background overlap;
7. stale InterpNet entries in planning and corpus metadata, while retaining the no-thesis-mention decision;
8. optional versus core citation use;
9. missing-PDF citation limitations;
10. whole-thesis citation-key and bibliography audit for Stage 4.9B.

For each new item record:

```text
ID
area
path
issue
evidence
impact
recommended Stage 4.9B action
urgency
```

---

# 24. Allowed Edit Scope

You may edit:

```text
thesis-writing/thesis/chapters/02_background_related_work.tex
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

You may create:

```text
thesis-writing/logs/stage_4_9A_evidence_report.md
```

A clean build may update:

```text
thesis-writing/thesis/main.pdf
```

A narrow compilation-only correction may be made to:

```text
thesis-writing/thesis/main.tex
thesis-writing/thesis/latexmkrc
```

only when necessary to repair a fatal build error and documented.

Do not edit:

```text
Chapter 1
Chapters 3–12
front matter
appendices
planning files
audit files
results files
figures
research code
configs
literature PDFs
literature README
catalog.csv
references.bib
literature checksums
prompt-provenance artifacts
```

Do not browse the web.

Do not download literature.

Do not run an experiment.

Do not begin Stage 4.9B.

---

# 25. Stage 4.9A Evidence Report

Create:

```text
thesis-writing/logs/stage_4_9A_evidence_report.md
```

Include:

## 25.1 Git state

* verified `4fa3f4f`;
* branch;
* initial worktree;
* final worktree;
* files changed;
* confirmation that literature and bibliography files were not modified.

## 25.2 Baseline build

* commands;
* result;
* page count;
* warnings.

## 25.3 Corpus validation

Record:

```text
core entry count
optional entry count
present-PDF count
missing-PDF count
checksum-validation result
```

Record any difference from the README.

## 25.4 Literature inspected

For every paper materially used, record:

```text
citation key
tier
PDF path
PDF status
sections inspected
claim supported
usage limitation
Chapter 2 location
```

## 25.5 Chapter structure

Record:

* four top-level sections;
* subsections;
* approximate word count per section;
* total word count;
* table labels;
* figure count.

## 25.6 Dataset background

Record:

* sources used for PhysioNet;
* sources used for MIMIC;
* distinctions made;
* unsupported numerical claims avoided.

## 25.7 Model background

For each of the five model families record:

```text
primary source
core concept
irregularity handling
local-thesis relationship
limitation stated
```

Confirm exactly five model rows.

## 25.8 Proxy and weak-supervision background

Record:

* phenotyping sources;
* clinical-definition sources;
* weak-supervision source;
* distinction between majority voting and a learned label model;
* diagnosis-language boundary.

## 25.9 Causal background

Record separately:

```text
target-trial/intervention definition
DAGs and adjustment
DML
causal forests/GRF
HTE in EHR/ICU
overlap
sensitivity
diagnostic/refutation workflows
```

For each, list citations and thesis boundary.

## 25.10 CausalPFN

Record:

* whether Chapter 2 mentions CausalPFN;
* exact restrained wording when present;
* confirmation that no technical claim or invented citation was added;
* retained citation-gap status.

## 25.11 InterpNet

Record:

* thesis-wide scan result;
* confirmation that Chapter 2 contains no mention;
* confirmation that the five-row model table excludes it;
* historical metadata left unchanged.

## 25.12 Citation inventory

For every citation key inserted in Chapter 2 record:

```text
key
number of Chapter 2 uses
primary or review role
claim families supported
PDF status
```

## 25.13 Results-boundary validation

List every thesis-specific design statement in Chapter 2.

Confirm that no numerical result appears.

## 25.14 Placeholders and deferred fixes

List:

* generic placeholders removed;
* precise gates retained;
* new `DF-4.9A-*` items.

## 25.15 Final build

Run:

```bash
cd thesis-writing/thesis
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

Record:

* return status;
* PDF path;
* page count;
* unresolved citations;
* unresolved references;
* duplicate labels;
* Biber errors;
* fatal errors;
* layout warnings.

## 25.16 Readiness

Use one:

```text
READY FOR STAGE 4.9B
READY WITH NON-BLOCKING WARNINGS
BLOCKED BEFORE STAGE 4.9B
```

---

# 26. Validation Requirements

## 26.1 Structure

Confirm:

* exactly four top-level Chapter 2 sections;
* all generic placeholders removed;
* no fifth section added;
* transitions are coherent;
* the final section leads naturally to Chapter 3.

## 26.2 Literature-source validation

Confirm:

* every citation key exists in `references.bib`;
* every cited key has a catalog row;
* every detailed method claim is supported by an inspected primary PDF where available;
* missing PDFs are not used for unsupported detail;
* no web source was used;
* no literature file changed.

## 26.3 Model validation

Confirm that Chapter 2 and `tab:background-model-families` contain exactly:

```text
STraTS
GRU
GRU-D
TCN
SAnD
```

Confirm:

* no InterpNet;
* no unsupported model;
* no local performance claim;
* no universal model ranking;
* published methods are distinguished from local adaptations.

## 26.4 Proxy-language validation

Search Chapter 2 for:

```text
diagnosis
ground truth
validated phenotype
clinical consensus
true latent state
```

Review every occurrence.

These terms may appear only when:

* explaining what project proxy states are not;
* accurately describing external literature;
* discussing a missing validation standard.

## 26.5 Causal-language validation

Search Chapter 2 for:

```text
proves causality
validated causal effect
causal certainty
unbiased
confounding eliminated
positivity established
clinical recommendation
treatment benefit
protective effect
harmful effect
```

There must be no unsupported occurrence.

## 26.6 Method-implementation boundary

Confirm:

* DML literature is not used to prove local execution;
* causal-forest literature is not used to validate local estimates;
* DAG literature is not used to validate the project graphs;
* phenotyping literature is not used to validate the project rules;
* weak-supervision literature is not used to claim the project implements Snorkel;
* sensitivity literature is not used to claim unmeasured confounding is absent;
* DoWhy literature is not used to claim all refuters were implemented.

## 26.7 CausalPFN validation

Confirm:

* no citation is invented;
* no unsupported architecture description appears;
* no superiority claim appears;
* its exploratory local role is not contradicted;
* its primary-source gap remains tracked.

## 26.8 LLM validation

Search Chapter 2 for:

```text
LLM-discovered
LLM-learned DAG
LLM-validated
medical expert
clinical expert
clinical validation by LLM
```

There must be no unsupported occurrence.

## 26.9 InterpNet validation

Run:

```bash
grep -Rni "InterpNet\|interpnet" thesis-writing/thesis \
  --include='*.tex' || true
```

There must be no occurrence.

## 26.10 Results-boundary scan

Search Chapter 2 for:

```text
AUROC
AUPRC
minRP
19 of 19
18 of 19
14 of 15
55 of 57
26,845
7,993
3.36
mean CATE
matching pairs
downsampled result
```

Review every occurrence.

Chapter 2 must not report thesis-specific numerical findings.

General literature discussion of AUROC or CATE is permitted only when genuinely necessary and not tied to local results.

## 26.11 Citation build validation

Confirm:

* Biber succeeds;
* zero undefined citations;
* zero undefined references;
* zero duplicate labels;
* bibliography entries appear correctly;
* citations are not orphaned from the claims they support.

## 26.12 Scope validation

Confirm:

* only Chapter 2 received substantive thesis prose;
* no earlier or later chapter changed;
* no bibliography changed;
* no catalog changed;
* no PDF changed;
* no result file changed;
* no figure changed;
* no code changed;
* no config changed;
* no experiment ran;
* no web research occurred;
* no Stage 4.9B work occurred;
* no commit or push occurred.

---

# 27. Completion Criteria

Stage 4.9A is complete only when:

* commit `4fa3f4f` is verified;
* the canonical corpus is validated;
* relevant primary PDFs are inspected;
* Chapter 2’s four sections are fully drafted;
* the dataset section distinguishes PhysioNet and MIMIC accurately;
* the model section reviews exactly five included families;
* InterpNet remains completely absent;
* the proxy section distinguishes rules, phenotypes, weak labels, and clinical validation;
* majority voting is distinguished from a learned weak-supervision label model;
* clinical-definition sources are used cautiously;
* the causal section covers identification before estimation;
* DML and causal forests are explained from primary sources;
* HTE challenges are discussed;
* overlap and sensitivity limitations are explicit;
* no detailed unsupported CausalPFN claim appears;
* no CausalPFN citation is invented;
* the integrated research gap is synthesized;
* no thesis-specific numerical result appears;
* exactly two required synthesis tables are present;
* all citation keys resolve;
* generic Chapter 2 placeholders are removed;
* tracking logs are updated;
* the evidence report is complete;
* the thesis builds successfully;
* only allowed files were edited.

---

# 28. Final Response

Report:

1. Verified Stage 4.8 commit.
2. Baseline build result and page count.
3. Corpus counts.
4. Checksum-validation result.
5. Number of core PDFs inspected.
6. Number of optional PDFs inspected.
7. Files changed.
8. Chapter 2 word count.
9. Section and subsection structure.
10. Dataset-background synthesis.
11. Five-model synthesis.
12. Model-table label and row count.
13. Proxy-phenotyping synthesis.
14. Weak-supervision distinction.
15. Clinical-definition citation handling.
16. DAG and identification synthesis.
17. DML synthesis.
18. Causal-forest and HTE synthesis.
19. Overlap synthesis.
20. Sensitivity synthesis.
21. Present-study positioning.
22. Dataset-table label and row count.
23. Optional third-table status.
24. CausalPFN handling.
25. InterpNet scan result.
26. LLM-language scan result.
27. Thesis-result boundary validation.
28. Citation keys inserted.
29. Missing-PDF limitations.
30. Generic placeholders removed.
31. Deferred fixes added.
32. Final build result and page count.
33. Remaining warnings.
34. Readiness for Stage 4.9B.

Do not commit or push.

Do not begin Stage 4.9B.
