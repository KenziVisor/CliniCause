# Stage 5.3 — Reproducibility and Provenance Closure

## Model

GPT-5.6 Terra

## Reasoning

Extra High

## Task identity

Implement this task as:

```text
Stage 5.3 — Evidence-Constrained Reproducibility and Provenance Closure
```

The current repository head is expected to be:

```text
f71c9bab70c8fb6347c7692236cd2cfd03e84fa1 step 5.2
```

Its immediate parent is expected to be:

```text
11a2cb46a87a979677602556611a704cfbd1074a step 5.1
```

Stage 5.2 concluded:

```text
READY FOR EXTERNAL FIGURE VALIDATION
```

The independent external assistant review has now been completed.

The next planned task after this stage is:

```text
Stage 5.4 — Institutional, Ethics, Governance, and Administrative Compliance
```

Do not begin Stage 5.4.

---

# 1. Authoritative external figure-validation decision

The following decision was made externally by the assistant after independent source reconstruction and visual review.

Codex must record this decision but must not represent it as Codex’s own approval.

| Figure ID                      | External decision                                      | Mandatory retained limitation                                                                                                  |
| ------------------------------ | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ |
| `F-RESULT-MIMIC-CATE`          | `APPROVED_BY_ASSISTANT`                                | Descriptive original-cohort CausalForestDML ranking; no significance, clinical-importance, or population-average-effect claim. |
| `F-RESULT-PHYSIONET-CATE`      | `APPROVED_BY_ASSISTANT`                                | Negative PhysioNet shock must remain visible; no significance, clinical-importance, or population-average-effect claim.        |
| `F-RESULT-DIRECTION-AGREEMENT` | `APPROVED_BY_ASSISTANT_WITH_DIRECTION_ONLY_LIMITATION` | Direction agreement only; no magnitude, uncertainty, equivalence, correctness, superiority, or causal-validity conclusion.     |
| `F-DAG-PHYSIONET`              | `APPROVED_BY_ASSISTANT_WITH_READABILITY_LIMITATION`    | Dense orientation figure only; source and node/edge inventories remain the exact authority.                                    |
| `F-DAG-MIMIC`                  | `APPROVED_BY_ASSISTANT_WITH_READABILITY_LIMITATION`    | Dense orientation figure only; source and node/edge inventories remain the exact authority.                                    |

The independent review also concludes:

```text
No figure hallucination, fabricated value, hidden sign, unauthorized aggregation,
invented graph element, or estimator substitution was found.
```

Do not modify any figure, generator, displayed value, figure caption, label, title, axis, legend, color, or placement in Stage 5.3.

---

# 2. Objective

Close as much reproducibility and provenance uncertainty as the repository can genuinely support.

This stage must:

1. formally record the completed external figure decision;
2. reconstruct available data, code, configuration, command, environment, split, checkpoint, export, run, result, and archive lineages;
3. search the repository and its Git history for currently missing provenance evidence;
4. distinguish exact evidence from defaults, inference, and absent records;
5. build a compact, machine-readable reproducibility package;
6. update thesis reproducibility language only where newly recovered evidence materially changes its accuracy;
7. leave every unresolved gap explicit;
8. determine which gaps are recoverable, externally dependent, or likely irrecoverable.

This stage must not claim complete reproducibility merely because:

* files exist;
* hashes match;
* current code runs;
* a current default resembles an archived result;
* a run summary contains a historical path;
* two files have the same filename;
* current source recreates an artifact;
* a requirements file lists packages;
* a commit predates an output;
* a copied artifact contains a timestamp.

---

# 3. Mandatory repository verification

From the parent repository root, run:

```bash
git status --short
git branch --show-current
git log -10 --oneline

git show --stat --oneline f71c9ba
git diff --stat 11a2cb4..f71c9ba

git submodule status
git ls-tree HEAD causal-irregular-time-series
git ls-tree HEAD STraTS
```

Confirm:

1. `f71c9ba` exists.
2. Its message is `step 5.2`.
3. Its immediate parent is `11a2cb4`.
4. Stage 5.2 added its audit ledgers and evidence reports.
5. Stage 5.2 changed no thesis PNG.
6. Stage 5.2 changed no checked CSV.
7. Stage 5.2 changed no figure generator.
8. Stage 5.2 changed no graph generator.
9. The only protected evidence-record modification was the authorized duplicate-name clarification in `figure_selection_register.md`.
10. The Stage 5.2 report records:

    * five included figures;
    * zero figure-value discrepancies;
    * 239 checked numerical table cells with zero discrepancies;
    * 1,389 manifest files present with matching hashes;
    * readiness for external figure validation.

Record the complete initial parent and nested-repository worktree states.

Do not:

* reset;
* clean;
* checkout another branch;
* discard unrelated changes;
* normalize CRLF-only changes;
* stage;
* amend;
* commit;
* push;
* update submodules;
* fetch remotes;
* browse the web.

If the baseline differs materially, stop and report:

```text
BLOCKED BEFORE STAGE 5.3 — REPOSITORY BASELINE MISMATCH
```

---

# 4. Protected baseline

Before investigation, hash:

```text
thesis-writing/results/checked_*.csv
thesis-writing/results/results_manifest.csv
thesis-writing/results/results_manifest.md
thesis-writing/results/results_source_packet.md
thesis-writing/results/results_decision_register.md
thesis-writing/results/results_checksums.sha256
thesis-writing/results/figure_selection_register.md
thesis-writing/results/generate_stage_4_6B_main_figures.py
thesis-writing/thesis/figures/*.png
thesis-writing/literature/metadata/references.bib
thesis-writing/literature/metadata/catalog.csv
causal-irregular-time-series/src/physionet2012_causal_graph.py
causal-irregular-time-series/src/mimiciii_causal_graph.py
```

Also record hashes for every thesis source file before editing.

At completion:

* all checked evidence must remain byte-identical;
* all five figures must remain byte-identical;
* all generators must remain byte-identical;
* literature metadata must remain byte-identical;
* research code must remain byte-identical.

---

# 5. Record the external figure decision

Create:

```text
thesis-writing/logs/stage_5_3_external_figure_decision.csv
```

Required columns:

```text
figure_id
stage_5_2_hash
assistant_decision
retained_limitation
decision_source
recorded_by_codex
figure_changed
caption_changed
notes
```

Use:

```text
decision_source=Independent assistant review supplied in the Stage 5.3 authorization prompt
recorded_by_codex=TRANSCRIBED_EXTERNAL_DECISION_NOT_CODEX_APPROVAL
figure_changed=NO
caption_changed=NO
```

Create:

```text
thesis-writing/logs/stage_5_3_external_figure_decision.md
```

The Markdown record must state:

* all five decisions;
* that the review was external to Codex;
* that no figure hallucination was found;
* that DAG readability remains a non-blocking limitation;
* that the direction-agreement figure is direction-only evidence;
* that no clinical, causal-identification, or statistical-significance approval follows.

Do not rewrite the historical Stage 5.2 report or its original pending-status ledger.

---

# 6. Evidence-status vocabulary

Use exactly these evidence classes throughout Stage 5.3:

```text
EXACT_ARCHIVED_ARTIFACT
EXACT_ARCHIVED_METADATA
EXACT_GIT_HISTORY_RECOVERY
EXACT_HASH_IDENTICAL_COPY
RUN_SUMMARY_RECORDED
LOG_RECORDED
CURRENT_SOURCE_REPRODUCTION
CURRENT_SOURCE_ONLY
CURRENT_DEFAULT_ONLY
INTENDED_REQUIREMENTS_ONLY
PARTIAL_LINEAGE
INFERRED_CANDIDATE_NOT_PROOF
MISSING_LOCAL_ARTIFACT
EXTERNAL_RECORD_REQUIRED
LIKELY_IRRECOVERABLE
NOT_APPLICABLE
```

Do not collapse them into a generic “available” status.

An inferred candidate must never be promoted to an exact producing record.

---

# 7. Create the reproducibility package

Create:

```text
thesis-writing/reproducibility/
```

Required files:

```text
thesis-writing/reproducibility/README.md
thesis-writing/reproducibility/artifact_index.csv
thesis-writing/reproducibility/causal_run_lineage.csv
thesis-writing/reproducibility/predictive_run_lineage.csv
thesis-writing/reproducibility/data_lineage.csv
thesis-writing/reproducibility/environment_lineage.csv
thesis-writing/reproducibility/source_version_lineage.csv
thesis-writing/reproducibility/archive_copy_lineage.csv
thesis-writing/reproducibility/provenance_gaps.csv
thesis-writing/reproducibility/checksums.sha256
```

The package must be reader-facing, concise, and independently auditable.

It must not duplicate all 1,389 manifest rows unnecessarily.

Instead, it should:

* index existing authoritative records;
* summarize lineage by run family or result family;
* point to detailed manifests;
* distinguish exact evidence from gaps;
* provide checksums for the new package itself.

---

# 8. Read-only repository forensics

Search all locally available evidence.

Permitted read-only commands include:

```bash
find
rg
grep
git log --all
git show
git diff
git ls-tree
git cat-file
git rev-list --all --objects
git fsck --full --no-reflogs --unreachable --no-progress
sha256sum
file
stat
```

Do not use `git fsck --lost-found`, because it writes recovered objects.

Search:

* tracked files;
* untracked but non-sensitive project files;
* all local branches and tags;
* reachable and dangling Git objects;
* parent-repository history;
* nested causal-repository history;
* STraTS history;
* archived logs;
* run summaries;
* command records;
* configuration basenames;
* copied result directories;
* training summaries;
* prediction exports;
* checkpoint names;
* environment metadata;
* timestamps stored inside artifacts.

Do not inspect:

* shell-history files;
* credentials;
* tokens;
* `.env` secrets;
* SSH files;
* raw protected clinical data;
* unrelated user directories.

When a historical or dangling object is inspected, record:

```text
repository
object_or_commit
path
relevance
recovery_status
```

Do not restore it into the working tree unless it is a non-sensitive provenance artifact and restoration is explicitly justified in the report.

Prefer recording recovered content in the reproducibility package rather than altering research directories.

---

# 9. Causal-run lineage

Create one row for each of the twelve archived causal run families:

```text
MIMIC / CausalForestDML / original
MIMIC / CausalForestDML / outcome-downsampled
MIMIC / LinearDML / original
MIMIC / LinearDML / outcome-downsampled
MIMIC / CausalPFN / original
MIMIC / CausalPFN / outcome-downsampled
PhysioNet / CausalForestDML / original
PhysioNet / CausalForestDML / outcome-downsampled
PhysioNet / LinearDML / original
PhysioNet / LinearDML / outcome-downsampled
PhysioNet / CausalPFN / original
PhysioNet / CausalPFN / outcome-downsampled
```

Required columns:

```text
run_id
dataset
estimator
sampling_condition
run_directory
run_summary
run_summary_sha256
recorded_command
command_status
recorded_config_path
config_basename
config_local_status
resolved_config_candidate
candidate_status
source_repository
source_version
source_version_status
graph_artifact
matching_artifact
cate_artifact
diagnostic_artifact
environment_artifact
archive_copy_status
lineage_completeness
blocking_gap
notes
```

For each run, verify:

* stage statuses;
* graph output;
* matching status;
* CATE status;
* sensitivity status;
* permutation status;
* intentionally skipped CausalPFN diagnostics;
* sampling condition;
* exact exposure coverage;
* configuration path;
* command record;
* source-version record;
* environment record;
* copy lineage.

A numbered configuration referenced through an unavailable absolute path remains missing unless the exact file is recovered by content or history.

A similar current config is not the producing config.

---

# 10. Predictive-run lineage

Create one row for each dataset–model family combination:

```text
MIMIC-III: STraTS, GRU, GRU-D, TCN, SAnD
PhysioNet 2012: STraTS, GRU, GRU-D, TCN, SAnD
```

Required columns:

```text
predictive_run_id
dataset
model_family
training_summary
training_summary_sha256
prediction_export
prediction_export_sha256
test_metrics_source
split_manifest
split_status
seed
seed_status
checkpoint
checkpoint_sha256
checkpoint_status
checkpoint_to_export_status
producing_command
command_status
producing_config
config_status
source_repository
source_version
source_version_status
environment_record
archive_copy_status
lineage_completeness
blocking_gap
notes
```

Explicitly distinguish:

* archived summary;
* archived prediction export;
* available implementation wrapper;
* current default;
* missing producing split;
* missing checkpoint;
* missing checkpoint-to-export mapping;
* missing producing command;
* missing exact source version.

Do not treat matching numerical log values as proof of checkpoint lineage.

---

# 11. Data lineage

Audit separately:

```text
raw source data
authorized access
raw-data version
raw-data hashes
extraction queries
cohort construction
processed pickles
processed hashes
patient or record identifiers
prediction inputs
proxy-label inputs
majority-vote inputs
causal-analysis inputs
mortality outcome construction
```

Required `data_lineage.csv` columns:

```text
lineage_id
dataset
stage
input_artifact
input_status
input_sha256
producer
producer_status
configuration
configuration_status
output_artifact
output_status
output_sha256
row_identifier
row_count
access_requirement
sensitive_data
lineage_completeness
gap
notes
```

Do not open protected raw data.

Use schemas, manifests, code contracts, and available hashes only.

Do not claim cross-dataset equivalence merely because both outputs use a similarly named mortality field.

---

# 12. Environment lineage

For every major family record:

```text
predictive training
prediction export
proxy construction
aggregation
matching
CausalForestDML
LinearDML
CausalPFN
figure generation
thesis build
```

Distinguish:

* package versions stored in the archived artifact;
* package versions printed in a log;
* current environment;
* intended requirements;
* unknown producing environment.

Required columns:

```text
environment_id
workflow_family
dataset
run_family
environment_source
python_version
package_versions
operating_system
cpu
ram
gpu
cuda
timestamp
evidence_class
producing_environment_supported
missing_fields
notes
```

A current successful rebuild is `CURRENT_SOURCE_REPRODUCTION`, not proof of the historical producing environment.

A requirements file is `INTENDED_REQUIREMENTS_ONLY`.

---

# 13. Source-version lineage

Audit:

* parent CliniCause source;
* causal nested repository;
* STraTS nested repository;
* gitlinks;
* active worktree differences;
* archived source paths;
* source versions recorded in result metadata;
* producing versions recorded in run summaries;
* versions used for current reproduction.

Required columns:

```text
source_id
workflow_family
repository
repository_role
current_gitlink
current_head
worktree_status
recorded_producing_version
producing_version_status
current_reproduction_version
artifact_family
lineage_status
gap
notes
```

Never substitute:

```text
current HEAD
```

for:

```text
historical producing version
```

unless exact archived evidence proves they are identical.

---

# 14. Archive-copy lineage

Group hash-identical files across:

* `final-results`;
* archived model directories;
* thesis-local figure copies;
* optional-figure directories;
* copied summaries;
* result packets.

Required columns:

```text
copy_group_id
sha256
canonical_path
copy_path
artifact_role
byte_identical
copy_direction_known
copy_timestamp_available
timestamp_evidence_class
source_version_known
selection_status
notes
```

Hash equality proves byte identity, not copy direction.

Filesystem timestamps alone must not establish historical origin.

Preserve the Stage 5.2 finding that:

```text
causal_dag_overview.png
```

is a duplicate or rename of the canonical archived MIMIC DAG and remains excluded only to prevent duplicate inclusion.

---

# 15. Provenance-gap disposition

For every unresolved item, record:

```text
gap_id
category
artifact_or_run
description
current_evidence
searches_performed
recovered
recovery_result
impact
resolution_owner
external_input_required
likely_irrecoverable
thesis_wording
next_action
```

At minimum disposition:

* numbered final causal configurations;
* producing causal commands;
* producing causal source versions;
* predictive split manifests;
* predictive seeds;
* predictive checkpoints;
* checkpoint-to-export mapping;
* predictive producing commands;
* predictive source versions;
* raw-data versions and hashes;
* processed-pickle hashes;
* cohort extraction lineage;
* archive-copy direction;
* producing environments;
* producing hardware;
* CausalPFN source/version;
* LLM prompt-run settings;
* human clinical-review record.

Use:

```text
RECOVERED
PARTIALLY_RECOVERED
NOT_FOUND_AFTER_EXHAUSTIVE_LOCAL_SEARCH
EXTERNAL_RECORD_REQUIRED
LIKELY_IRRECOVERABLE
NOT_APPLICABLE
```

Do not describe a gap as “closed” unless its exact required evidence is recovered.

---

# 16. Reproducibility claims matrix

Create:

```text
thesis-writing/logs/stage_5_3_reproducibility_claim_matrix.csv
```

Required columns:

```text
claim_id
reader_facing_claim
current_location
required_evidence
available_evidence
evidence_class
claim_supported
qualification_required
repair_required
notes
```

Audit every reader-facing claim involving:

```text
reproducible
reproduction
replication
traceable
manifest
checksum
source version
configuration
command
environment
archive
checkpoint
split
seed
provenance
clean checkout
```

The thesis may claim:

* numerical traceability where hashes and sources support it;
* current artifact reproduction where reproduced;
* documented evidence boundaries.

It may not claim:

* complete clean-checkout reproduction;
* exact historical rerun;
* complete data lineage;
* complete checkpoint lineage;
* complete producing-environment capture.

---

# 17. Permitted thesis repairs

Only make a reader-facing repair when Stage 5.3 recovers evidence or discovers a material provenance misstatement.

Potentially relevant files:

```text
thesis-writing/thesis/chapters/04_data_preprocessing.tex
thesis-writing/thesis/chapters/06_predictive_modeling.tex
thesis-writing/thesis/chapters/09_experimental_design.tex
thesis-writing/thesis/chapters/11_discussion.tex
thesis-writing/thesis/chapters/12_conclusions_future_work.tex
thesis-writing/thesis/appendices/appendices.tex
```

Allowed repairs:

* replace a now-resolved provenance gate;
* refine a gap from unknown to partially recovered;
* distinguish current reproduction from historical production;
* add a precise reference to the reproducibility package;
* correct a false claim of missing or available evidence;
* remove obsolete internal stage wording.

Do not:

* broaden prose;
* introduce new results;
* change values;
* change result interpretation;
* change citations;
* modify figures;
* modify table values;
* add administrative facts;
* claim complete reproduction.

---

# 18. Allowed edit scope

You may create:

```text
thesis-writing/reproducibility/
thesis-writing/logs/stage_5_3_external_figure_decision.csv
thesis-writing/logs/stage_5_3_external_figure_decision.md
thesis-writing/logs/stage_5_3_reproducibility_claim_matrix.csv
thesis-writing/logs/stage_5_3_recovery_search_log.csv
thesis-writing/logs/stage_5_3_evidence_report.md
```

You may edit only where supported by documented evidence:

```text
thesis-writing/thesis/chapters/04_data_preprocessing.tex
thesis-writing/thesis/chapters/06_predictive_modeling.tex
thesis-writing/thesis/chapters/09_experimental_design.tex
thesis-writing/thesis/chapters/11_discussion.tex
thesis-writing/thesis/chapters/12_conclusions_future_work.tex
thesis-writing/thesis/appendices/appendices.tex
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

You may regenerate:

```text
thesis-writing/thesis/main.pdf
```

---

# 19. Forbidden edit scope

Do not edit:

```text
prompt.txt
thesis-writing/thesis/figures/
thesis-writing/results/checked_*.csv
thesis-writing/results/results_manifest.csv
thesis-writing/results/results_manifest.md
thesis-writing/results/results_source_packet.md
thesis-writing/results/results_decision_register.md
thesis-writing/results/results_checksums.sha256
thesis-writing/results/figure_selection_register.md
thesis-writing/results/generate_stage_4_6B_main_figures.py
thesis-writing/logs/stage_5_2_*
thesis-writing/literature/
thesis-writing/audit/
thesis-writing/planning/
causal-irregular-time-series/src/
STraTS/
router.py
requirements*.txt
tests/
```

Do not modify research code.

Do not rerun training, preprocessing, matching, CATE estimation, sensitivity analysis, or permutation experiments.

Do not create a figure.

Do not overwrite an archived result.

Do not browse the web.

---

# 20. Validation

Validate the reproducibility package programmatically.

Check:

* required columns;
* unique IDs;
* no missing status field;
* all referenced local files exist or are explicitly marked missing/external;
* all recorded hashes match;
* all exact evidence classes have an exact source;
* no `CURRENT_DEFAULT_ONLY` row is labelled producing configuration;
* no `INFERRED_CANDIDATE_NOT_PROOF` row is labelled recovered;
* no current environment is labelled historical producing environment;
* no hash-identical copy is assigned a copy direction without evidence;
* every gap has an owner and next action.

Generate:

```text
thesis-writing/reproducibility/checksums.sha256
```

for the completed reproducibility package.

---

# 21. Final build

From:

```text
thesis-writing/thesis/
```

run:

```bash
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

Record:

* return codes;
* page count;
* PDF SHA-256;
* unresolved citations;
* unresolved references;
* duplicate labels;
* fatal errors;
* Biber errors and warnings;
* missing glyphs;
* bidi warnings;
* overfull boxes;
* underfull boxes;
* `xdvipdfmx` warnings.

Render and inspect every changed thesis page.

After inspection:

```bash
latexmk -c
```

Retain `main.pdf`.

Do not retain LaTeX auxiliary files.

---

# 22. Protected-file validation

Recalculate protected hashes.

Prove:

* all five externally approved PNGs are byte-identical;
* checked CSVs are unchanged;
* figure and graph generators are unchanged;
* research source is unchanged;
* result manifests and checksums are unchanged;
* literature metadata is unchanged.

If any figure changes, report:

```text
BLOCKED — EXTERNALLY APPROVED FIGURE MODIFIED
```

---

# 23. Stage 5.3 evidence report

Create:

```text
thesis-writing/logs/stage_5_3_evidence_report.md
```

Use this structure.

## 23.1 Repository baseline

* head;
* parent;
* branch;
* parent worktree;
* nested worktrees;
* Stage 5.2 readiness.

## 23.2 External figure-decision record

* five decisions;
* source of decision;
* confirmation that Codex merely transcribed it;
* confirmation of unchanged figure hashes.

## 23.3 Search methodology

* repositories;
* histories;
* files;
* Git objects;
* exclusions;
* commands.

## 23.4 Recovered evidence

For each recovered item:

```text
item
exact source
evidence class
previous status
new status
impact
```

## 23.5 Causal-run lineage

* 12 run families;
* completeness counts;
* recovered configurations;
* recovered commands;
* unresolved items.

## 23.6 Predictive-run lineage

* 10 dataset–model families;
* summaries;
* exports;
* splits;
* checkpoints;
* commands;
* source versions.

## 23.7 Data lineage

* raw;
* processed;
* prediction;
* proxy;
* causal;
* outcome;
* access boundaries.

## 23.8 Environment lineage

* historical captured;
* intended;
* current reproduction;
* missing producing environments.

## 23.9 Source-version lineage

* parent;
* causal nested repository;
* STraTS;
* producing-version findings.

## 23.10 Archive-copy lineage

* duplicate groups;
* canonical paths;
* unknown directions;
* MIMIC DAG duplicate-name status.

## 23.11 Provenance gaps

Count:

```text
RECOVERED
PARTIALLY_RECOVERED
NOT_FOUND_AFTER_EXHAUSTIVE_LOCAL_SEARCH
EXTERNAL_RECORD_REQUIRED
LIKELY_IRRECOVERABLE
```

## 23.12 Thesis repairs

* exact path;
* exact old claim;
* exact new claim;
* source;
* reason.

## 23.13 Reproducibility package

* files;
* row counts;
* validation;
* package checksum.

## 23.14 Files changed

List exact paths.

## 23.15 Protected-file validation

* protected count;
* changed count;
* figure hashes;
* frozen-evidence result.

## 23.16 Final build

* page count;
* PDF hash;
* compilation;
* warnings;
* render inspection.

## 23.17 Remaining gates

Carry forward only genuinely unresolved items.

## 23.18 Readiness decision

Use exactly one:

```text
READY FOR STAGE 5.4 WITH NON-BLOCKING PROVENANCE GAPS
BLOCKED BEFORE STAGE 5.4
```

---

# 24. Readiness rules

Use:

```text
READY FOR STAGE 5.4 WITH NON-BLOCKING PROVENANCE GAPS
```

only if:

* external figure approval is recorded accurately;
* no figure changed;
* all locally recoverable provenance has been investigated;
* every remaining gap is explicitly classified;
* no inference is presented as exact evidence;
* the reproducibility package validates;
* thesis reproducibility language is accurate;
* protected evidence is unchanged;
* the thesis builds.

Use:

```text
BLOCKED BEFORE STAGE 5.4
```

if:

* a figure changed;
* a checked source changed;
* an exact producing claim rests only on defaults or inference;
* a recovered artifact cannot be verified;
* run-lineage rows are materially inconsistent;
* a reader-facing reproducibility overclaim remains;
* sensitive data were copied into the package;
* the thesis fails to build.

---

# 25. Final Codex response

Report:

1. verified head and parent;
2. readiness status;
3. external figure decisions recorded;
4. confirmation that no figure changed;
5. recovered causal provenance;
6. recovered predictive provenance;
7. recovered data provenance;
8. recovered environment provenance;
9. recovered source versions;
10. archive-copy findings;
11. unresolved and likely irrecoverable gaps;
12. thesis repairs;
13. reproducibility-package files and row counts;
14. protected-file result;
15. final page count and PDF hash;
16. build/render result;
17. exact next authorized stage.

Do not commit or push.

Do not edit `prompt.txt`.

Do not begin Stage 5.4.
