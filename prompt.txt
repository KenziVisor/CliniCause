# Stage 4.6A-R — Author Decisions, Optional-Figure Audit, and Results-Packet Repair

## Model

Use:

```text
GPT-5.6 Terra
Reasoning: High
```

## Task

Perform a narrow repair and decision-freeze step before drafting Chapter 10.

Expected latest commit:

```text
da200b7 adding figures
```

Its parent is expected to be:

```text
b4b1212 step 4.6A
```

Do not draft Chapter 10.

The next stage after this repair is:

```text
Stage 4.6B
GPT-5.6 Sol
Reasoning: High
```

---

## 1. Verify repository state

Run:

```bash
git status --short
git branch --show-current
git log -8 --oneline
git show --stat --oneline da200b7
git diff --stat b4b1212..da200b7
```

Confirm:

* Stage 4.6A results files exist;
* the optional figures are under `thesis-writing/figures-options/`;
* there are 16 PNG files;
* Chapter 10 remains undrafted;
* the previous decision-freeze prompt was not applied.

Do not reset, clean, stage, commit, or push.

---

## 2. Record the author-approved hierarchy

Update:

```text
thesis-writing/results/results_decision_register.md
thesis-writing/results/results_source_packet.md
thesis-writing/results/results_manifest.md
```

Record these decisions:

### Primary analysis population

* Original cohort is primary.
* Outcome-downsampled analyses are robustness/sensitivity analyses.
* Discuss downsampling briefly and directly, but do not make it a dominant thesis theme.
* Detailed downsampled results belong in supplementary material.
* Never pool original and downsampled estimates.

### Estimator hierarchy

* CausalForestDML: primary.
* LinearDML: secondary.
* CausalPFN: exploratory but included in the main Results chapter after LinearDML.

Do not choose this hierarchy based on observed effect magnitudes.

### Exposure policy

Report every prespecified dataset-specific proxy-state exposure from the original-cohort runs.

Do not select exposures based on:

* magnitude;
* direction;
* apparent significance;
* estimator agreement;
* perceived clinical interest after viewing results.

### Effect wording

Use:

```text
descriptive matched-pair outcome difference
mean model-estimated CATE over the analyzed sample
```

Do not use:

```text
ATE
confirmed ATT
population-average causal effect
risk ratio
relative risk
percent reduction
```

Omit normalized CATE from the main chapter.

### Cross-dataset comparison

* Compare datasets qualitatively and through aligned concepts only.
* Do not pool estimates.
* State explicitly that the original causal analysis populations contain:

  * MIMIC: 26,845 records;
  * PhysioNet: 7,993 records;
  * difference: 18,852;
  * MIMIC is approximately 3.36 times larger.
* Label these as original causal-analysis population counts, not raw cohort totals.

---

## 3. Approved Chapter 10 order

Freeze this order:

```text
1. Analysis-population counts
2. Predictive performance
3. Primary CausalForestDML results
4. Matching and empirical support
5. LinearDML comparison
6. CausalPFN exploratory results
7. Cross-dataset comparison
8. Robustness and sensitivity
```

Downsampling belongs within the final robustness section, not as a headline result section.

---

## 4. Estimator-agreement statement

Validate the original-cohort checked CATE rows and record the following supported conclusion:

```text
The three estimators agree in mean-effect direction for 18 of 19
dataset–exposure comparisons.
```

Document:

* MIMIC: 9 of 9 exposures directionally concordant;
* PhysioNet: 9 of 10 exposures directionally concordant;
* exception: PhysioNet `LAT_SHOCK`;
* CausalForestDML and LinearDML are negative for that exposure;
* CausalPFN is slightly positive.

Approve this bounded positive statement:

```text
CausalPFN reproduced the prevailing direction in nearly every comparison,
supporting its promise as a complementary estimator within this pipeline.
```

Also require:

```text
This broad agreement is not complete agreement and does not establish
estimator equivalence, superiority, causal validity, or interchangeable
uncertainty quantification.
```

State that CausalPFN lacks the archived downstream sensitivity and permutation diagnostics available for the DML estimators.

Do not call CausalPFN failed.

---

## 5. Remove InterpNet from the thesis

Search:

```bash
grep -Rni "InterpNet\|interpnet" thesis-writing/thesis \
  --include='*.tex'
```

Remove every InterpNet mention from the thesis draft, including:

* Chapter 6 prose;
* Chapter 6 model table;
* Chapter 6 placeholders;
* Chapter 9 experiment prose;
* Chapter 9 experiment table;
* Chapter 9 provenance discussion;
* any other thesis chapter, front-matter, or appendix occurrence.

Adjust surrounding prose and tables so that the predictive comparison contains only:

```text
STraTS
GRU
GRU-D
TCN
SAnD
```

Do not describe InterpNet as missing, excluded, future work, implemented, or unavailable in the thesis.

Historical audit and literature files may retain factual records, but the Stage 4.6B source packet must instruct Codex to ignore them.

Resolve the InterpNet result decision as:

```text
NOT PART OF THE THESIS PIPELINE — EXCLUDED FROM THESIS
```

---

## 6. Optional-figure audit

Inspect every PNG under:

```text
thesis-writing/figures-options/
```

Create:

```text
thesis-writing/results/figure_selection_register.md
```

For every figure record:

```text
filename
SHA-256
width
height
figure type
underlying checked source
value-validation result
author decision
planned chapter or appendix
required caption boundary
provenance limitation
```

The author decisions are fixed as follows.

### Main Results figures

```text
mimic_non_downsampled_cate_ranking.png
physionet_non_downsampled_cate_ranking.png
cross_model_direction_counts.png
```

Validation requirements:

* compare every displayed value/count against the checked Stage 4.6A CSVs;
* confirm the ranking figures use original-cohort results;
* confirm the scale is mean model-estimated CATE, not normalized CATE;
* confirm all prespecified exposures are shown;
* confirm `cross_model_direction_counts.png` supports the 18-of-19 statement;
* confirm it does not silently mix original and downsampled populations.

If a selected image fails numerical validation, mark it:

```text
BLOCKED_VALUE_CONFLICT
```

Do not repair, redraw, or replace it during this stage.

### Appendix figures

```text
mimic_all_run_stability.png
physionet_all_run_stability.png
mortality_prediction_true_auroc_auprc.png
permutation_null_check.png
sensitivity_intervals.png
shock_rule_tree.png
```

Caption boundaries:

* all-run stability figures must separate original and downsampled populations;
* mortality prediction is predictive association, not causal evidence;
* permutation is a sanity diagnostic, not identification proof;
* sensitivity intervals do not validate the DAG or eliminate confounding;
* the shock tree is one illustrative proxy-rule example, not clinical validation.

### Retain for later methods/background placement

```text
icu_scattered_events.png
latent_state_tags.png
majority_vote_tag_cleaning.png
```

Do not insert them during this stage.

### Exclude from the thesis

```text
causal_dag_overview.png
correlation_vs_causation.png
mortality_prediction_metrics.png
sensitivity_intervals_trimmed.png
```

Reasons:

* redundant with verified dataset-specific DAGs;
* generic rather than project-specific;
* superseded by the corrected AUROC/AUPRC figure;
* truncated visual range risks selective emphasis.

Do not let Codex independently reverse these selections.

---

## 7. Update checked-table statuses

Without changing any numerical value, update selection-status fields.

### Predictive metrics

* Test rows for STraTS, GRU, GRU-D, TCN, and SAnD:
  `PRIMARY_MAIN_TEXT`.
* Validation rows:
  `SUPPORTING_APPENDIX`.

### CATE results

* CausalForestDML + original:
  `PRIMARY_MAIN_TEXT`.
* LinearDML + original:
  `SECONDARY_MAIN_TEXT`.
* CausalPFN + original:
  `EXPLORATORY_MAIN_TEXT`.
* Every outcome-downsampled row:
  `ROBUSTNESS_APPENDIX`.

Apply the status to every prespecified exposure.

### Matching

* Original CausalForestDML:
  `SUPPORTING_MAIN_TEXT`.
* Original LinearDML:
  `SECONDARY_APPENDIX`.
* Original CausalPFN:
  `EXPLORATORY_APPENDIX`.
* Downsampled:
  `ROBUSTNESS_APPENDIX`.

### Sensitivity and permutation

* Original non-PFN:
  `SUPPORTING_APPENDIX`.
* Downsampled:
  `ROBUSTNESS_APPENDIX`.
* PFN:
  `NOT_APPLICABLE` for unavailable diagnostic stages.

### Proxy and mortality-prediction tables

Use:

```text
SUPPORTING_APPENDIX
```

Do not alter row order, row count, source paths, hashes, or numerical fields.

---

## 8. Add the new figures to the manifest

Update:

```text
thesis-writing/results/results_manifest.csv
thesis-writing/results/checked_figure_candidates.csv
thesis-writing/results/results_checksums.sha256
```

Add all 16 PNGs with:

* repository-relative path;
* SHA-256;
* dimensions;
* selection status;
* source/provenance status;
* underlying checked-table relationship;
* main/appendix/excluded role.

Record the seminar PowerPoint as contextual figure provenance only:

```text
thesis-writing/prompts-and-documents/
true_final_thesis_engineering_seminar_unified_redesign.pptx
```

Do not use the presentation as numerical authority when checked CSVs are available.

---

## 9. Update placeholders and deferred fixes

Update:

```text
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

Mark result-hierarchy decisions:

```text
AUTHOR DECISION RECORDED
```

Do not state supervisor approval unless separately documented.

Retain open evidence limitations for:

* missing numbered causal configs;
* ignored/untracked result archive;
* producing commits;
* predictive split and checkpoint lineage;
* raw cohort totals;
* missing PhysioNet proxy tables;
* dedicated overlap diagnostics;
* proxy clinical validation;
* LLM prompt execution provenance.

Add a deferred fix for final supervisor ratification of the author-approved results hierarchy.

---

## 10. Allowed edits

You may edit:

```text
thesis-writing/thesis/chapters/06_predictive_modeling.tex
thesis-writing/thesis/chapters/09_experimental_design.tex
other thesis .tex files only when removing an InterpNet occurrence
thesis-writing/results/**
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

Create:

```text
thesis-writing/logs/stage_4_6A_repair_report.md
```

A clean build may update:

```text
thesis-writing/thesis/main.pdf
```

Do not edit:

* Chapter 10;
* research code;
* configs;
* source result artifacts;
* the PNG files;
* bibliography files;
* audit or planning files;
* prompt-provenance artifacts.

---

## 11. Validation

Confirm:

1. no InterpNet occurrence remains in any thesis `.tex` file;
2. the predictive model table contains exactly five model families;
3. no numerical checked value changed;
4. all original CausalForest rows are primary;
5. all original LinearDML rows are secondary;
6. all original CausalPFN rows are exploratory main-text candidates;
7. all downsampled rows are robustness material;
8. normalized CATE is not selected for main text;
9. the 18-of-19 direction count is reproduced directly;
10. the PhysioNet shock exception is documented;
11. every optional PNG is hashed and classified;
12. every selected numerical figure matches its checked source;
13. no excluded figure is selected;
14. no source figure is modified;
15. no thesis result prose is drafted.

Run:

```bash
cd thesis-writing/thesis
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

Scan for fatal LaTeX, citation, reference, label, and Biber errors.

---

## 12. Readiness

Use:

```text
READY FOR STAGE 4.6B
```

only when:

* the hierarchy is frozen;
* InterpNet is absent from the thesis;
* figure selections are recorded;
* selected result figures are numerically validated;
* checked-table statuses are updated;
* checksums are regenerated;
* the thesis builds successfully.

Also report:

```text
SUPERVISOR RATIFICATION OF THE RESULTS HIERARCHY REMAINS PENDING
```

Do not begin Stage 4.6B.

Do not commit or push.
