# Stage 4.2 — Draft the Predictive Modeling of Proxy States Chapter

## Context

Stages 0–3, Stage 4.0, and Stage 4.1 of the CliniCause thesis workflow are complete.

The latest approved commit is expected to be:

```text
d7edc78 step 4.1
```

Stage 4.1 drafted:

```text
Chapter 3 — Problem Definition and Study Design
Chapter 4 — Data and Preprocessing
```

It also verified the WSL PDF and LaTeX toolchain and produced a successful XeLaTeX/Biber build.

This task covers only:

```text
Chapter 6 — Predictive Modeling of Proxy States
```

The target file is:

```text
thesis-writing/thesis/chapters/06_predictive_modeling.tex
```

The approved section structure is:

```text
C6.1 — Self-Supervised STraTS Pretraining
C6.2 — Supervised Multi-Label Models
C6.3 — Prediction Export and Normalization
```

Do not draft Chapter 5 or any later methods, experiments, results, discussion, introduction, or background chapter.

This chapter is a methodology chapter. It must explain what the predictive implementation does, but it must not report final model performance.

---

# 1. Objective

Draft a technically accurate and academically written Chapter 6 that explains:

* the self-supervised STraTS pretraining task;
* the STraTS and iSTraTS representations;
* the common supervised multi-label proxy-prediction task;
* the GRU, GRU-D, TCN, SAnD, and InterpNet baselines;
* model-specific handling of irregular observations and missingness;
* the shared target and output interface;
* evaluation-metric definitions without numerical values;
* checkpointing and transfer/fine-tuning behavior as actually implemented;
* prediction CSV export;
* conversion of combined probability/binary exports into downstream voter inputs;
* limitations in final split, checkpoint, and archive provenance.

The chapter must distinguish clearly between:

```text
method described in a paper
method implemented in the repository
method configured in wrapper scripts
method apparently executed
method with an approved final result artifact
```

Do not collapse these evidence levels.

---

# 2. Mandatory Initial Inspection

## 2.1 Repository and Commit State

From the repository root, run:

```bash
git status --short
git branch --show-current
git log -4 --oneline
git show --stat --oneline d7edc78
```

Verify that:

* `d7edc78` exists;
* its message is `step 4.1`;
* it follows the Stage 4.0 commit;
* Chapters 3 and 4 and the Stage 4.1 evidence report are present;
* the Chapter 6 skeleton is present.

Do not:

* reset;
* checkout another branch;
* clean the working tree;
* stage files;
* amend commits;
* commit;
* push;
* discard unrelated local changes.

Record the pre-existing working-tree state.

## 2.2 Repository Instructions

Locate and inspect all applicable instruction files before editing, including:

```text
AGENTS.md
STraTS/AGENTS.md
causal-irregular-time-series/AGENTS.md
```

Apply the instructions governing each inspected path.

When instructions conflict with this prompt, stop and report the conflict rather than silently choosing one.

## 2.3 WSL Toolchain

The WSL PDF and LaTeX toolchain was successfully installed and verified during Stage 4.1.

Confirm that these commands remain available:

```bash
command -v pdfinfo
command -v pdftotext
command -v latexmk
command -v xelatex
command -v biber
```

Record only material changes from the Stage 4.1 environment.

Do not reinstall the toolchain when all commands are present.

## 2.4 Baseline Thesis Build

Before drafting, run:

```bash
cd thesis-writing/thesis
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

Confirm:

* the baseline build succeeds;
* Biber executes;
* the PDF is generated;
* no fatal LaTeX error exists;
* there are no undefined citations or references;
* chapter and section labels are unique.

Record the baseline page count.

If the baseline fails, identify and repair only a narrow thesis-workspace compilation defect before drafting.

Do not edit scientific prose in another chapter to fix a build problem.

---

# 3. Required Planning and Audit Inspection

Inspect the relevant Stage 3 planning files:

```text
thesis-writing/planning/thesis_story.md
thesis-writing/planning/thesis_outline.md
thesis-writing/planning/chapter_evidence_map.md
thesis-writing/planning/terminology_and_notation.md
thesis-writing/planning/citation_plan.md
thesis-writing/planning/table_plan.md
thesis-writing/planning/figure_plan.md
thesis-writing/planning/writing_order.md
thesis-writing/planning/stage4_prompt_queue.md
```

Pay particular attention to:

```text
C6.1
C6.2
C6.3
T-MODEL-01
T-TRAIN-CFG-01
T-PRED-TASK-01
T-PRED-EXPORT-01
T-PRED-METRICS-01
```

Inspect the relevant audit files:

```text
thesis-writing/audit/repository_map.md
thesis-writing/audit/evidence_inventory.md
thesis-writing/audit/experiment_inventory.csv
thesis-writing/audit/claim_evidence_ledger.csv
thesis-writing/audit/terminology_map.md
thesis-writing/audit/unresolved_questions.md
```

Inspect the previous Stage 4 logs:

```text
thesis-writing/logs/stage_4_0_setup_report.md
thesis-writing/logs/stage_4_1_evidence_report.md
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

Use these documents as evidence indexes, not as substitutes for current source inspection.

---

# 4. Literature Inspection

Inspect:

```text
thesis-writing/literature/README.md
thesis-writing/literature/metadata/catalog.csv
thesis-writing/literature/metadata/references.bib
```

Verify the exact citation keys before inserting them.

Expected primary method keys include:

```text
tipirneni2022strats
cho2014gru
che2018grud
bai2018tcn
song2018sand
shukla2019interpolation
```

The MIMIC benchmark citation may be used for dataset or benchmark context when relevant:

```text
harutyunyan_2019_mimiciii_benchmark
```

Do not use the optional early InterpNet workshop paper as the main InterpNet citation when the canonical 2019 source is available.

Academic citations support method definitions and context. They do not prove local execution, checkpoint use, or result values.

---

# 5. Required STraTS Source Inspection

Inspect the current STraTS repository rather than relying only on copied context documents.

At minimum inspect:

```text
STraTS/SCRIPTS.md
STraTS/requirements.txt
STraTS/src/main.py
STraTS/src/dataset_pretrain.py
STraTS/src/dataset.py
STraTS/src/models.py
STraTS/src/evaluator_pretrain.py
STraTS/src/evaluator.py
STraTS/src/learning_curves.py
STraTS/src/modeling_strats.py
STraTS/src/modeling_gru.py
STraTS/src/modeling_grud.py
STraTS/src/modeling_tcn.py
STraTS/src/modeling_sand.py
STraTS/src/modeling_interpnet.py
```

Inspect all current predictive wrapper scripts, including where present:

```text
STraTS/run_main.sh
STraTS/run_main_rest.sh
STraTS/run_main_mimic.sh
STraTS/run_full_main.sh
STraTS/run_strats_job.sbatch
```

Verify every model name and filename against the actual tree.

Do not assume that documentation filenames are correct when they conflict with current source.

---

# 6. Required Export and Orchestration Inspection

For prediction export, normalization, and downstream handoff, inspect:

```text
router.py
SCRIPTS.md
causal-irregular-time-series/src/split_predicted_latent_tags.py
causal-irregular-time-series/src/majority_vote_latents.py
causal-irregular-time-series/main.py
```

Use these files to establish:

* how STraTS predictions are exported;
* the structure of probability and binary columns;
* how combined prediction CSVs are split;
* what files are suitable as binary voter inputs;
* how the router collects or normalizes predictive artifacts;
* where majority-vote aggregation occurs.

Do not draft the detailed majority-vote methodology in Chapter 6. That belongs in Chapter 5.

Chapter 6 may explain only the predictive-output handoff and cross-reference the later or earlier proxy-aggregation section using the existing thesis structure.

---

# 7. Result-Artifact Inspection Boundary

You may inspect predictive result artifacts only to classify provenance and implementation status.

Relevant locations may include:

```text
final-results/strats-outputs/
STraTS/outputs/
training_summary.txt
log.txt
checkpoint_best.bin
pt_saved_variables.pkl
predicted*_latent_tags*.csv
learning_curve_history.csv
```

Do not extract or write performance values in Chapter 6.

Do not write:

* AUROC values;
* AUPRC values;
* minRP values;
* losses;
* comparative rankings;
* claims that one model outperformed another;
* claims of statistical significance.

Those belong to the experimental-design and results stages after a result manifest and approved source selection exist.

You may state evidence status, for example:

* implementation present;
* wrapper configuration present;
* checkpoint artifact present;
* final result artifact missing;
* final archive provenance incomplete.

Treat InterpNet carefully:

* describe the implemented method when verified;
* cite the canonical paper;
* do not claim an approved final performance result when none exists;
* retain an actionable result placeholder for Chapter 10 or the central placeholder log.

---

# 8. Allowed Edit Scope

You may edit:

```text
thesis-writing/thesis/chapters/06_predictive_modeling.tex
thesis-writing/logs/unresolved_placeholders.md
thesis-writing/logs/deferred_fixes.md
```

You may create:

```text
thesis-writing/logs/stage_4_2_evidence_report.md
```

A clean build may update the generated file:

```text
thesis-writing/thesis/main.pdf
```

Do not manually edit the PDF.

You may make a narrow compilation-only repair to:

```text
thesis-writing/thesis/main.tex
thesis-writing/thesis/latexmkrc
```

only when a fatal build error requires it. Document any such repair.

Do not edit:

```text
Chapter 1
Chapter 2
Chapter 3
Chapter 4
Chapter 5
Chapters 7–12
front matter
appendices
planning files
audit files
bibliography metadata
research code
configuration files
results
```

Do not update the stale thesis README during this task. It is already recorded as a deferred cleanup item.

---

# 9. Evidence Policy

For every technical statement, classify the basis internally as one of:

```text
PAPER-DESCRIBED
IMPLEMENTATION-CONFIRMED
CONFIGURATION-CONFIRMED
EXECUTION-SUPPORTED
RESULT-ARTIFACT-PRESENT
PROVENANCE-INCOMPLETE
BLOCKED
```

The main prose does not need to display these labels repeatedly, but the evidence report must.

Use the strongest wording justified by the evidence.

Examples:

### Acceptable

```text
The implementation represents each STraTS observation using learned embeddings of time, value, and variable identity.
```

```text
The wrapper scripts configure self-supervised pretraining before supervised proxy-label prediction.
```

```text
Archived checkpoint-related artifacts are present, although the exact provenance of the final copied outputs remains incomplete.
```

### Not acceptable without stronger evidence

```text
All final models were correctly pretrained.
```

```text
Pretraining improved predictive accuracy.
```

```text
Every exported prediction was out-of-sample.
```

```text
The pretrained weights were successfully transferred.
```

```text
The final InterpNet model achieved competitive performance.
```

---

# 10. Special Audit: Checkpoint Loading and Fine-Tuning

Inspect the complete checkpoint-loading path in:

```text
STraTS/src/main.py
STraTS/src/models.py
```

Determine separately:

1. the intended warm-start or fine-tuning behavior;
2. the state dictionary constructed from `--load_ckpt_path`;
3. which state dictionary is actually passed to `load_state_dict`;
4. how `checkpoint_best.bin` is selected and reloaded;
5. whether the pretraining checkpoint is demonstrably used to initialize the supervised model;
6. whether only STraTS/iSTraTS have compatible self-supervised pretraining paths;
7. whether wrapper scripts and archived artifacts prove successful transfer.

Do not infer successful transfer from the presence of a `--load_ckpt_path` argument.

When the suspected warm-start issue remains present:

* distinguish intended design from actual active code;
* avoid claiming successful pretrained initialization;
* log a new deferred fix;
* use conservative thesis wording;
* retain a validation placeholder if needed.

Do not repair the training code during this task.

---

# 11. Chapter 6 Requirements

## 11.1 Chapter Opening

Open the chapter with a concise explanation that:

* the prediction task uses rule-derived proxy-state vectors as supervised targets;
* the input consists of irregular ICU measurement histories and static covariates;
* the implementation compares STraTS-family models with recurrent, convolutional, attention-based, decay-based, and interpolation-based baselines;
* Chapter 6 defines model and output mechanics;
* final training configurations and numerical comparisons are deferred to Chapters 9 and 10.

Do not repeat the full dataset description from Chapter 4.

Use a cross-reference to the data-contract discussion rather than restating it at length.

---

## 11.2 Section C6.1 — Self-Supervised STraTS Pretraining

Explain the verified STraTS pretraining pipeline.

### Observation Representation

Verify and describe how a single irregular observation is represented.

The implementation is expected to combine components corresponding to:

* measurement time;
* measurement value;
* variable identity.

Explain continuous-value embeddings only to the level supported by source and the STraTS paper.

Describe:

* token construction;
* contextualization of irregular observation tokens;
* pooling into a fixed-dimensional time-series representation;
* integration of static demographic features;
* the difference between STraTS and iSTraTS when verified.

Do not describe iSTraTS as a separately pretrained architecture unless current source supports that statement.

### Forecasting Task

Verify and explain:

* how forecast anchors are chosen;
* the minimum-history condition;
* which observations form the historical context;
* the future prediction window;
* how the target for each variable is selected;
* how variables with no future observation are masked;
* dataset-specific time-horizon or context restrictions;
* training-derived value normalization;
* training-split variable vocabulary construction.

Where the implementation uses a two-hour prediction window or a 12-hour minimum anchor, state it only after confirming it in current source.

### Pretraining Objective and Evaluation

Explain:

* the forecasting head;
* the masked regression objective;
* the role of the pretraining evaluator;
* how validation windows are materialized or cached, when relevant;
* the meaning of `loss_neg` if that remains the checkpoint metric.

Do not report loss values.

### Saved Pretraining State

Verify the role and contents of:

```text
pt_saved_variables.pkl
checkpoint_best.bin
```

Explain what metadata can be reused during supervised loading, including where verified:

* variable vocabulary;
* means and standard deviations;
* maximum time.

Do not claim that a pretraining checkpoint successfully initialized final supervised models until the checkpoint-loading audit supports that statement.

### Citation

Use:

```text
tipirneni2022strats
```

for the STraTS method.

Do not cite the paper as proof of local code execution.

---

## 11.3 Section C6.2 — Supervised Multi-Label Models

### Supervised Target Definition

Explain that the supervised label matrix is loaded from a separate proxy-label CSV.

Verify and describe:

* accepted identifier column names;
* normalization to `ts_id`;
* row alignment with split IDs;
* duplicate-ID checks;
* missing-label checks;
* treatment of all non-ID columns as target columns;
* binary multi-label target structure;
* per-target positive-class weighting;
* the shared supervised loss.

Use “proxy labels” or “proxy states,” not diagnoses or ground truth.

### Shared Output Interface

Verify that the supervised backends produce one output per proxy-state target.

Explain:

* number of targets determined at runtime;
* sigmoid or probability conversion;
* shared binary or multi-label head;
* whether fine-tuning uses an intermediate forecasting head;
* how static features are combined with the time-series representation.

When behavior differs between scratch training and checkpoint-loaded training, describe each path separately.

Do not claim that every model uses identical internal architecture merely because the output head is shared.

### Model Families

Describe each implemented family accurately.

#### STraTS and iSTraTS

Cover:

* irregular observation triplets;
* learned time/value embeddings;
* variable embeddings;
* transformer-style contextualization;
* fusion attention;
* static-feature concatenation;
* verified distinction between STraTS and iSTraTS.

Use:

```text
tipirneni2022strats
```

#### GRU

Cover:

* recurrent sequence processing;
* the dense hourly representation used by this implementation;
* included observation-mask and elapsed-time channels where verified.

Use:

```text
cho2014gru
```

#### GRU-D

Cover:

* missingness masks;
* elapsed time since previous observation;
* decay-based handling of missing values;
* the actual tensors supplied by the loader.

Use:

```text
che2018grud
```

#### TCN

Cover:

* temporal convolution;
* the dense time-grid input;
* shared value, mask, and elapsed-time representation where verified.

Use:

```text
bai2018tcn
```

#### SAnD

Cover:

* attention-based sequence modeling;
* dense time-grid representation;
* positional or temporal structure as implemented.

Use:

```text
song2018sand
```

#### InterpNet

Cover:

* continuous-time interpolation or cross-channel interpolation concepts;
* irregular time, value, mask, and holdout inputs;
* the current implementation interface.

Use:

```text
shukla2019interpolation
```

State clearly that implementation presence does not establish an approved final performance artifact.

### Model Comparison Table

Create a non-numeric methods table corresponding to:

```text
T-MODEL-01
```

Suggested columns:

```text
Model
Temporal representation
Missingness or irregularity handling
Static-feature use
Self-supervised pretraining support
Primary citation
Repository evidence status
```

Verify each cell against current source.

Do not put final AUROC, AUPRC, or loss values in this table.

Use a stable label such as:

```latex
\label{tab:predictive-model-comparison}
```

after confirming that the label is unused.

### Evaluation Metrics

Define, without reporting values:

* per-target AUROC;
* per-target AUPRC;
* maximum minimum of precision and recall, where implemented;
* averaging across valid non-degenerate target columns;
* the supervised checkpoint-selection score;
* the limited `eval_train` subset behavior if it affects interpretation.

Do not call averaged multi-label metrics micro- or macro-averages unless the evaluator implementation supports that exact term.

Do not imply that validation metrics are test metrics.

### Training Mechanics

Describe only stable implementation mechanics suitable for a methods chapter, such as:

* optimizer family;
* binary multi-label objective;
* gradient clipping;
* checkpoint selection;
* deterministic seed construction;
* early or periodic validation behavior.

Move run-specific hyperparameters, selected train fractions, run identifiers, epoch counts, and final configurations to Chapter 9 unless the Stage 3 plan explicitly requires them here.

Do not infer the final experiment configuration from parser defaults alone.

---

## 11.4 Section C6.3 — Prediction Export and Normalization

### Export Trigger and Checkpoint Selection

Verify and explain:

* the flag that requests CSV export;
* whether export occurs after training or in an export-only invocation;
* whether an existing best checkpoint is reloaded;
* which data split is exported;
* how wrapper scripts set the split;
* whether final archived provenance proves those settings.

Do not describe an export as test-only or out-of-sample unless verified.

### CSV Schema

Explain the verified schema:

```text
ts_id
<label>_prob
<label>
```

for each target label.

Explain that:

* probability columns contain model outputs;
* binary columns are produced through the implemented threshold;
* the current threshold is `0.5` when verified;
* the binary columns are model predictions, not rule labels or clinical truth;
* export rows are identified using normalized `ts_id`.

Create a concise non-numeric schema table corresponding to:

```text
T-PRED-TASK-01
T-PRED-EXPORT-01
```

when useful.

Use a unique stable label such as:

```latex
\label{tab:prediction-export-schema}
```

### Downstream Normalization

Explain at a high level:

* why probability and binary columns may need to be separated;
* how `split_predicted_latent_tags.py` performs this separation;
* why majority-vote inputs must contain only `ts_id` and binary proxy columns;
* how the router prepares or collects voter CSVs;
* that majority-vote semantics are described in Chapter 5 rather than here.

Do not present majority-vote aggregation as model ensembling performance.

### Provenance Limitation

Retain a precise limitation concerning:

* the exact split exported by the final archived commands;
* the source processed-pickle hash;
* split-generation seed or ID lists;
* checkpoint-to-export mapping;
* archive-copy provenance.

Do not hide this limitation behind vague wording.

---

# 12. Citation Requirements

Use exact verified bibliography keys.

At minimum, when their methods are described, cite:

```text
tipirneni2022strats
cho2014gru
che2018grud
bai2018tcn
song2018sand
shukla2019interpolation
```

Use:

```text
harutyunyan_2019_mimiciii_benchmark
```

only for relevant benchmark context, not as the source of the local model implementation.

Do not insert:

* nonexistent citation keys;
* citations copied from comments without checking `references.bib`;
* an optional source in place of the approved primary source;
* citations for local execution claims;
* citations for numerical results.

Run Biber validation after editing.

---

# 13. Placeholder Policy

Remove the generic placeholders:

```text
[STAGE 4 DRAFT REQUIRED]
[RESULT REQUIRED]
[VALIDATION REQUIRED]
```

from Chapter 6 when the associated content has been drafted or replaced by a more precise evidence gate.

Retain precise placeholders where needed, such as:

```text
[VALIDATION REQUIRED: confirm that the final supervised checkpoints were initialized from the intended self-supervised STraTS checkpoint]
```

```text
[VALIDATION REQUIRED: record the final export split, split-generation seed or ID manifest, source checkpoint, and archive-copy provenance]
```

```text
[RESULT REQUIRED: approved final InterpNet evaluation artifact before inclusion in the predictive performance comparison]
```

Every placeholder must be added to:

```text
thesis-writing/logs/unresolved_placeholders.md
```

Record:

* file;
* section;
* exact placeholder;
* reason;
* required evidence;
* planned resolution stage;
* status.

Do not leave a generic placeholder without an actionable description.

---

# 14. Deferred-Fix Collection

Review all existing deferred issues.

The following remain open and must not be silently resolved:

```text
DF-4.1-001 — MIMIC preprocessing outcome-column mismatch
DF-4.1-002 — predictive split and archive provenance
DF-4.1-003 — stale thesis README toolchain statement
```

Stage 4.2 is directly responsible for reassessing `DF-4.1-002`.

Mark it resolved only when a complete predictive manifest or equivalent evidence is actually found.

Append newly discovered issues using IDs beginning with:

```text
DF-4.2-
```

Potential issues that must be checked include:

* ineffective or incorrect warm-start checkpoint loading;
* mismatch between wrapper intent and active checkpoint behavior;
* model documentation disagreeing with source;
* prediction export using a different split than described;
* missing mapping from checkpoint to exported CSV;
* inconsistent target ordering across latent CSVs and exports;
* unclear status of iSTraTS final experiments;
* misleading use of “fine-tuning” when initialization is unverified;
* generated `main.pdf` being tracked without an explicit repository policy;
* result summaries existing without command or source-commit provenance.

For every deferred item record:

```text
ID
area
path
issue
evidence
impact
recommended later action
urgency
```

Do not repair predictive source code during this task.

Do not untrack or ignore `main.pdf` during this task. Record the policy question for later cleanup when it remains unresolved.

---

# 15. Stage 4.2 Evidence Report

Create:

```text
thesis-writing/logs/stage_4_2_evidence_report.md
```

It must contain the following sections.

## 15.1 Git State

Record:

* branch;
* inspected commit;
* verification of `d7edc78`;
* initial status;
* final status;
* files changed by Stage 4.2;
* unrelated pre-existing modifications.

## 15.2 Toolchain and Baseline Build

Record:

* command paths;
* baseline build command;
* baseline build result;
* initial PDF page count;
* baseline warnings.

## 15.3 Files Inspected

Group by:

* thesis and logs;
* planning;
* audit;
* literature;
* STraTS data loaders;
* STraTS models;
* STraTS evaluators;
* STraTS wrappers;
* parent router;
* downstream export-normalization scripts;
* predictive artifacts.

## 15.4 Model Implementation Matrix

For each model record:

```text
model name
source file
input representation
irregularity/missingness handling
static-feature handling
pretraining support
supervised head
primary citation
implementation status
execution evidence
approved final-result status
limitations
```

Include:

```text
STraTS
iSTraTS
GRU
GRU-D
TCN
SAnD
InterpNet
```

## 15.5 Pretraining Contract

Record:

* input fields;
* forecast-anchor logic;
* context window;
* target window;
* target construction;
* masks;
* normalization;
* variable vocabulary;
* objective;
* evaluator behavior;
* saved metadata;
* checkpoint-selection rule.

## 15.6 Supervised Contract

Record:

* latent CSV schema;
* accepted ID names;
* ID normalization;
* target-column derivation;
* loss;
* class weighting;
* output shape;
* metric calculations;
* checkpoint-selection rule.

## 15.7 Checkpoint-Loading Audit

Record:

* intended warm-start flow;
* actual active code flow;
* state dictionaries loaded;
* any ignored or overwritten state dictionary;
* supported model types;
* wrapper assumptions;
* artifact evidence;
* conclusion on whether successful pretrained initialization is verified.

Do not soften an implementation defect.

## 15.8 Export Contract

Record:

* export flag;
* split selection;
* checkpoint reload behavior;
* probability columns;
* binary columns;
* threshold;
* output naming;
* normalization/splitting scripts;
* downstream voter requirements;
* provenance limitations.

## 15.9 Citations Used

List each inserted citation key and the method claim it supports.

## 15.10 Placeholders

List:

* resolved Chapter 6 placeholders;
* retained placeholders;
* newly added placeholders;
* where each will be resolved.

## 15.11 Deferred Fixes

List:

* existing predictive issues confirmed;
* issues resolved through evidence;
* new `DF-4.2-*` entries;
* generated-PDF tracking-policy status.

## 15.12 Final Build

Record:

```bash
cd thesis-writing/thesis
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

Include:

* return status;
* PDF path;
* page count;
* citation status;
* reference status;
* duplicate-label status;
* fatal errors;
* remaining nonfatal warnings.

## 15.13 Readiness

Use one of:

```text
READY FOR STAGE 4.3
READY WITH NON-BLOCKING WARNINGS
BLOCKED BEFORE STAGE 4.3
```

Stage 4.3 will draft the clinical proxy-state construction chapter.

Do not begin Stage 4.3.

---

# 16. Validation Requirements

## 16.1 Source-to-Prose Validation

For every paragraph containing implementation details, verify that the source file named in the evidence report supports it.

Specifically verify:

* STraTS versus iSTraTS pooling;
* which models support pretraining;
* each model’s input tensors;
* shared versus model-specific heads;
* class-weight calculation;
* metric averaging;
* checkpoint-selection score;
* export threshold;
* export split behavior;
* checkpoint reload behavior.

## 16.2 Risky-Wording Search

Search the edited chapter for wording including:

```text
ground truth
diagnosis
clinically validated
state of the art
outperformed
best model
significantly better
causal
treatment effect
all models were pretrained
successfully fine-tuned
out-of-sample
test-set predictions
fully reproducible
guarantees
```

Every occurrence must be:

* justified;
* qualified;
* removed;
* or recorded as an unresolved issue.

The word “causal” may appear only in a cross-reference to the downstream pipeline, not as a property of the predictive model.

## 16.3 Citation Validation

Confirm that:

* every inserted key exists;
* all model descriptions have the appropriate primary citation;
* no citation is used as proof of local performance;
* Biber reports no unresolved key.

## 16.4 Table Validation

Confirm that:

* `tab:predictive-model-comparison` is unique;
* `tab:prediction-export-schema` is unique when used;
* every table cell is source-supported;
* no numerical result is included;
* InterpNet status is not overstated;
* captions describe methods, not findings.

## 16.5 Terminology Validation

Use consistently:

```text
proxy state
proxy label
multi-label prediction
probability output
binary prediction
irregular observation
static covariate
pretraining
scratch supervised training
checkpoint-loaded training
prediction export
```

Distinguish:

```text
rule-derived proxy label
model-predicted proxy label
probability prediction
thresholded binary prediction
majority-vote proxy label
```

Do not call a thresholded model prediction a clinical label without qualification.

## 16.6 LaTeX Validation

Run a clean final build:

```bash
cd thesis-writing/thesis
latexmk -C
latexmk -xelatex main.tex
```

Scan the build logs for:

```text
LaTeX Error
Undefined control sequence
Citation ... undefined
Reference ... undefined
There were undefined references
multiply defined
Biber error
Emergency stop
Fatal error
```

Confirm:

```bash
test -f main.pdf
pdfinfo main.pdf
```

Record overfull and underfull boxes separately as nonfatal layout issues.

## 16.7 Scope Validation

Confirm that:

* only Chapter 6 contains new substantive prose;
* no performance values were inserted;
* no other chapter was drafted;
* no research code was changed;
* no configuration was changed;
* no training or prediction job was run;
* no result file was modified;
* no bibliography record was changed;
* no commit or push was performed.

---

# 17. Completion Criteria

Stage 4.2 is complete only when:

* commit `d7edc78` is verified;
* the baseline thesis compiles;
* C6.1 accurately describes the self-supervised STraTS task;
* C6.2 accurately describes every implemented supervised model family;
* pretraining support is not attributed to unsupported baselines;
* the checkpoint-loading path is audited explicitly;
* C6.3 documents export and normalization without overstating split provenance;
* method tables contain no performance values;
* all model citations are valid;
* InterpNet’s missing final result remains visible;
* predictive provenance limitations remain visible;
* new repository issues are added to the deferred-fix register;
* placeholders are centrally tracked;
* the final thesis compiles successfully;
* the evidence report is complete;
* only permitted files were edited.

---

# 18. Final Response

At the end, report:

1. Verified Stage 4.1 commit.
2. Initial and final Git status.
3. Baseline compilation result and page count.
4. Files inspected.
5. Files changed.
6. C6.1 content drafted.
7. C6.2 models documented.
8. C6.3 export and normalization documented.
9. Model-comparison and export-schema tables created.
10. Checkpoint-loading audit conclusion.
11. Citations inserted.
12. Remaining placeholders.
13. Existing and new deferred fixes.
14. Final build command and result.
15. Final PDF path and page count.
16. Remaining LaTeX warnings.
17. Stage 4.3 readiness status.

Do not commit or push.

Do not proceed to Stage 4.3.
