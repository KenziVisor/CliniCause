Stage: Complete SAnD Clearance and Quality-Preserving Thesis Compression

Model/reasoning level:
Use the strongest available reasoning mode. This task requires repository-wide consistency checking, scientific editing, LaTeX restructuring, and rendered-PDF validation.

Repository:
KenziVisor/CliniCause

Expected starting point:
The expected HEAD is:

d77fada7b63c2b0b37746302fcda1c6351fa3e9b
commit message: SAnD removal

The latest commit is authoritative for the decision that SAnD is outside the final thesis scope.

Do not assume the work is complete merely because that commit exists. Inspect its diff, inspect the current worktree, and verify that every active thesis-facing reference has been handled consistently.

Do not commit or push.

======================================================================
1. OBJECTIVES
======================================================================

Perform two connected tasks, in this order:

1. Complete the repository and thesis-facing clearance of SAnD.
2. Reduce unnecessary thesis length through a quality-preserving compression pass.

The SAnD task is a focused consistency repair and should be completed first. The larger task is the thesis compression pass.

The author’s priority is thesis quality, not an arbitrary page count.

The thesis is currently approximately 133 physical PDF pages. A result near 100–110 pages would be welcome if it follows naturally from removing repetition, audit-level detail, oversized tables, and structural duplication. However:

- 103 pages is not a mandatory target;
- do not weaken the thesis to reach a number;
- do not remove scientifically necessary material;
- do not narrow the research scope merely to save pages;
- do not force chapters into predetermined page budgets;
- do not delete useful qualifications when they are needed locally;
- do not substitute terseness for clarity.

The governing principle is:

> Produce the strongest, clearest, most coherent thesis that can be justified by the available evidence. Reduce length only where material is repetitive, misplaced, excessively operational, or more detailed than the scientific argument requires.

This is a compression and restructuring pass, not a scope-reduction exercise.

======================================================================
2. BASELINE AND REPOSITORY VERIFICATION
======================================================================

Before editing, run and record at least:

git status --short
git branch --show-current
git log -10 --oneline
git show --stat --oneline d77fada7b63c2b0b37746302fcda1c6351fa3e9b
git diff --stat d77fada7b63c2b0b37746302fcda1c6351fa3e9b^..d77fada7b63c2b0b37746302fcda1c6351fa3e9b

Read all applicable `AGENTS.md`, repository instructions, thesis build instructions, current evidence reports, and local documentation before editing.

Record:

- branch and HEAD;
- pre-existing dirty files;
- nested-repository state;
- current thesis source path;
- current PDF path and page count;
- build command;
- files changed by the SAnD-removal commit;
- any surviving active SAnD references;
- protected material.

Do not reset, clean, normalize, stage, or overwrite pre-existing unrelated work.

A material mismatch between the expected HEAD and the actual repository state must be reported before substantive editing.

======================================================================
3. AUTHORITATIVE SCIENTIFIC SCOPE
======================================================================

Treat the following as authoritative.

Predictive comparison:

- Four learned prediction models belong to the final thesis:
  - STraTS
  - GRU
  - GRU-D
  - TCN

Final aggregate:

- The archived final aggregate contains five sources:
  - one rule-derived source;
  - predicted binary labels from STraTS;
  - predicted binary labels from GRU;
  - predicted binary labels from GRU-D;
  - predicted binary labels from TCN.

The aggregate is:

- deterministic;
- mixed-source;
- based on four learned predictions plus one rule-derived source;
- not clinical consensus;
- not a Snorkel generative label model;
- not prediction-only.

SAnD:

- is outside the final thesis scope;
- is not one of the four reported learned prediction models;
- is not a final voter;
- must not appear as an evaluated final model, included family, result row, contribution, abbreviation, thesis method, thesis comparison, active evidence item, or future thesis table candidate.

Do not replace SAnD with InterpNet.

InterpNet may remain relevant only where historically or methodologically required by an authoritative retained source, but it must not be introduced as an evaluated final model. Do not manufacture a new missing-model narrative merely to fill the space left by SAnD.

======================================================================
4. TASK A — COMPLETE SAnD CLEARANCE
======================================================================

Complete this task before the general compression pass.

4.1 Inspect the previous removal

Inspect the complete diff of commit `d77fada7`. Determine:

- which SAnD references were removed;
- which files were deleted or modified;
- which references remain intentionally historical;
- which references remain accidentally active;
- whether any deleted records left broken numbering, prose, headings, citations, table schemas, or cross-references.

Do not mechanically repeat the previous edit. Audit its consequences.

4.2 Search comprehensively

Search case-insensitively for at least:

SAnD
SAND
sand
Attend and Diagnose
song2018sand
model_sand
pred_*_sand
five predictive
five model
five families
five included families
five learned
10 predictive
ten predictive
five evaluated

Search:

- all thesis `.tex` files;
- front matter;
- abstracts;
- keywords;
- abbreviations;
- notation;
- chapter text;
- tables;
- captions;
- figure descriptions;
- appendices;
- bibliography source;
- generated auxiliary lists where appropriate;
- active planning files;
- active evidence and claim matrices;
- active results manifests;
- checked predictive-result tables;
- reproducibility and release records;
- current validation ledgers;
- thesis-facing Markdown and CSV files;
- scripts that generate thesis tables or figures.

Also inspect semantically related language that may no longer contain the literal string `SAnD` but still encodes the old count or model set.

4.3 Required Chapter 2 repair

Section 2.2.5 is currently titled:

“Relationship among the five included families”

Change it to reflect the four final models, for example:

“Relationship among the four included model families”

or a clearer equivalent.

Review the section itself, its opening sentence, Table 2.2, surrounding transitions, and all references to it. Ensure that it describes exactly:

- STraTS;
- GRU;
- GRU-D;
- TCN.

The comparison should remain a comparison of inductive biases, not a sophistication ranking.

Update:

- the table of contents;
- table captions;
- prose introducing the family count;
- any chapter summaries;
- any later cross-references.

4.4 Thesis-wide consistency

The final rendered thesis must consistently report:

- four learned prediction models;
- five aggregate sources;
- four predicted voters plus one rule-derived source.

Check especially:

- Hebrew abstract;
- English abstract;
- keywords if applicable;
- Introduction;
- research questions and contributions;
- Background §2.2;
- model-family table;
- predictive-method chapter;
- proxy aggregation description;
- experimental-design chapter;
- predictive Results table;
- Discussion;
- Conclusions;
- appendix;
- list of abbreviations;
- list of tables;
- all numerical descriptions of model counts.

4.5 Bibliography and literature handling

If the SAnD paper is no longer cited anywhere in the final thesis:

- remove its active BibTeX entry from the thesis bibliography if that bibliography is intended to contain cited works only;
- remove its active literature-corpus catalog/checksum entry only if the previous commit establishes that complete removal is the intended repository policy;
- do not leave a dangling citation key or missing PDF reference;
- do not delete unrelated literature;
- do not rewrite historical Git history.

If a protected historical audit record must preserve that SAnD once existed, retain it only when its archival role is explicit and it is not treated as active thesis evidence. Do not silently falsify history. Active records and thesis-facing summaries, however, must represent the corrected four-model scope.

4.6 Results and evidence integrity

Ensure that active checked predictive-result files and thesis tables contain only the four approved learned models.

Do not:

- recompute metrics;
- replace SAnD with another model;
- change values for the retained four models;
- alter result rankings;
- infer missing results;
- claim that SAnD failed or underperformed;
- retain stale “ten runs” or “five families” counts;
- delete raw historical artifacts unless the prior commit and repository policy explicitly authorize their removal.

If a raw archive still contains SAnD artifacts for historical reasons, it must not be cited or admitted as final thesis evidence. Record that distinction.

4.7 SAnD completion criterion

Task A is complete only when:

- no active thesis prose contains SAnD;
- no active thesis table or figure contains SAnD;
- no active predictive result includes SAnD;
- no active claim/evidence record treats SAnD as part of the thesis;
- no stale five-model or five-family language remains;
- §2.2.5 and Table 2.2 correctly reflect four models;
- the PDF compiles with correct numbering and references;
- any surviving occurrence is explicitly classified as protected historical material and cannot be mistaken for current scientific scope.

Document every surviving occurrence and why it remains.

======================================================================
5. TASK B — QUALITY-PRESERVING COMPRESSION
======================================================================

After the SAnD clearance is complete, perform a thesis-wide compression and restructuring pass.

Do not begin by deleting sections. First build a compression map.

For every chapter and substantial subsection, classify content as:

- essential scientific argument;
- necessary methodological detail;
- necessary result or interpretation;
- necessary local qualification;
- repeated explanation;
- repository/audit-level implementation detail;
- evidence-management detail better kept outside the main narrative;
- table content that could be consolidated;
- transitional material that can be shortened;
- candidate for relocation to a technical supplement;
- candidate for removal with no loss of meaning.

The main goal is to reduce documentary scope while preserving scientific scope.

======================================================================
6. WHAT MUST REMAIN IN THE THESIS
======================================================================

Preserve the complete scientific chain:

- motivation for irregular ICU time-series analysis;
- both datasets;
- dataset-specific preprocessing and data-contract distinction;
- LLM-assisted design-time knowledge elicitation;
- deterministic proxy-state construction;
- conservative proxy-state terminology;
- distinction among rule-derived, predicted, and aggregated labels;
- four-model predictive comparison:
  STraTS, GRU, GRU-D, and TCN;
- five-source final aggregate:
  four predictions plus one rule-derived source;
- dataset-specific DAGs;
- adjustment assumptions;
- matching as descriptive support;
- CausalForestDML as primary;
- LinearDML as secondary;
- CausalPFN as exploratory;
- overlap/support limitations;
- sensitivity analysis;
- permutation checks;
- original versus downsampled populations;
- complete checked Results;
- cross-dataset interpretation without pooling;
- principal limitations;
- contribution and future-work conclusions.

Preserve the central contribution:

> An integrated, inspectable, evidence-tracked workflow connecting LLM-assisted design proposals to deterministic source-encoded rules and graphs, then tracing proxy constructs through irregular-time-series prediction, aggregation, and observational causal analysis.

Do not narrow the thesis to only prediction, only causal inference, only LLM elicitation, or only software integration.

======================================================================
7. PRIORITY COMPRESSION AREAS
======================================================================

Use the following diagnosis as guidance, not as a mechanical deletion quota.

----------------------------------------------------------------------
7.1 Do not target Background merely because it appears long
----------------------------------------------------------------------

The Background chapter is important and covers several necessary traditions:

- ICU data and irregular sampling;
- four irregular-time-series model families;
- proxy phenotyping;
- weak supervision;
- LLM-assisted elicitation;
- DAG reasoning;
- DML;
- causal forests;
- overlap;
- sensitivity.

Do not reduce it indiscriminately.

Compress it by:

- eliminating repeated general cautions already stated elsewhere;
- reducing long transitions;
- keeping the strongest primary citation for each concept;
- avoiding implementation detail that belongs in Methods;
- consolidating closely related paragraphs;
- ensuring each subsection directly supports a later thesis component.

A moderate reduction is appropriate only where clarity is preserved.

----------------------------------------------------------------------
7.2 Chapters 8 and 9 have structural overlap
----------------------------------------------------------------------

Current organization separates:

- robustness, sensitivity, permutation, and validation design;
- experimental matrices;
- computational environment;
- result-selection and reproducibility policy.

Inspect whether Chapters 8 and 9 should be merged into a single chapter such as:

“Experimental and Validation Design”

A good merged structure may include:

1. populations and experiment families;
2. predictive experiment design;
3. causal experiment design;
4. overlap and support diagnostics;
5. sensitivity and permutation checks;
6. result-admission and reproducibility boundaries.

Merge only when it improves the scientific narrative and reduces repetition.

Remove or relocate:

- repeated artifact-status explanations;
- configuration-contract details;
- operational field inventories;
- duplicate provenance boundaries;
- repository-level validation mechanics that do not help interpret the method.

Do not remove:

- the distinction between original and downsampled cohorts;
- estimator hierarchy;
- diagnostic purpose and interpretation;
- result-admission logic needed to understand Chapter 10;
- reproducibility qualifications that materially bound the evidence.

----------------------------------------------------------------------
7.3 Chapter 5 mixes scientific method with audit history
----------------------------------------------------------------------

Retain:

- rationale for proxy states;
- terminology;
- LLM-assisted design protocol;
- bounded authority of the LLM;
- transition from proposals to deterministic rules;
- concise definitions of PhysioNet and MIMIC proxy families;
- distinction among rule-derived, predicted, and aggregated labels;
- main construct-validity limitations.

Compress or relocate:

- exhaustive prompt inventories;
- repeated source-path descriptions;
- full artifact-provenance matrices;
- extensive version-history discussion;
- duplicated “not clinical validation” language;
- line-by-line traceability detail;
- operational descriptions of every generated file;
- complete rule clauses when a concise clinically meaningful summary is sufficient.

Do not obscure active rule behavior. A reader must still understand what each proxy means and how it is generated.

Where detailed rule tables are too large for the main text:

- create or use a technical supplement;
- preserve a concise main-text summary;
- add a precise reference to the supplement;
- keep enough detail in the thesis to make the method scientifically understandable.

----------------------------------------------------------------------
7.4 Reduce repeated cautionary language
----------------------------------------------------------------------

The thesis correctly emphasizes boundaries such as:

- proxy state is not diagnosis;
- rule-derived label is not clinical ground truth;
- prediction is not clinical validation;
- DAG is not learned truth;
- matching is not CATE;
- estimator agreement is not causal validity;
- numerical traceability is not clean-checkout reproducibility;
- the framework is not deployment ready.

Retain these distinctions, but organize them hierarchically.

Recommended placement:

- Introduction:
  one concise global boundary paragraph.
- Methods:
  local qualifications only where they affect the specific method.
- Results:
  short reminders only where a result could otherwise be seriously misread.
- Discussion:
  authoritative complete limitations treatment.
- Conclusion:
  concise synthesis rather than another full limitations chapter.

Avoid repeating the same multi-sentence disclaimer in every chapter.

A qualification may be shortened or replaced with a cross-reference when:

- the complete statement already appears in an authoritative location;
- the local reader cannot reasonably misinterpret the claim without it;
- the cross-reference remains clear.

Do not remove a local caution when its absence would materially strengthen a claim.

----------------------------------------------------------------------
7.5 Consolidate conceptual and audit-style tables
----------------------------------------------------------------------

The thesis has many tables, including several that describe process, evidence status, assumptions, provenance, and experiment design rather than results.

Review every table against this test:

> Does the table materially help a thesis reader understand the scientific method, result, or interpretation better than concise prose or a smaller table would?

Potential consolidation:

- contribution hierarchy and study-boundary material;
- prompt-provenance and design-to-source traceability tables;
- proxy source-type tables;
- DAG node-family and adjustment-logic tables;
- causal assumptions and estimator-limit tables;
- overlap, sensitivity, and permutation design tables;
- prediction and causal experiment matrices;
- reproducibility-status and result-admission tables;
- research-question answer and limitation summary tables.

Possible actions:

- merge two related tables;
- remove columns that repeat prose;
- replace repeated status columns with a short note;
- move exhaustive matrices to a supplement;
- convert a simple table into compact prose;
- keep one authoritative summary table instead of several overlapping ones.

Do not compress the checked numerical Results tables merely for page savings. Results readability has priority.

----------------------------------------------------------------------
7.6 Introduction
----------------------------------------------------------------------

The Introduction currently performs many functions:

- motivation;
- integration gap;
- objective;
- LLM design role;
- pipeline description;
- research questions;
- contribution hierarchy;
- empirical preview;
- boundaries;
- organization.

Retain all necessary functions but eliminate duplication.

Possible improvements:

- shorten repeated descriptions of the complete pipeline;
- state the objective once in definitive form;
- shorten the explanation of “evidence-tracked” after defining it;
- reduce overlap between prose contributions and the contribution table;
- keep the research questions but shorten surrounding commentary;
- use a concise empirical orientation rather than a second Results summary;
- shorten the thesis-organization section, especially after any chapter merge.

Do not weaken the integration contribution or delete the research questions.

----------------------------------------------------------------------
7.7 Discussion
----------------------------------------------------------------------

The Discussion should interpret, not reproduce, the Results and Introduction.

Review:

- the main research-question answer;
- seven secondary-question answers;
- relationship to prior work;
- clinical meaning;
- limitations;
- summary tables.

Compress by:

- synthesizing related secondary questions;
- referring to Results tables rather than repeating their contents;
- avoiding another description of every pipeline stage;
- combining overlapping limitations;
- retaining the most important causal, construct-validity, support, external-validity, LLM, reproducibility, and ethical limitations;
- consolidating or removing a summary table if it merely duplicates the prose.

The Discussion must remain intellectually substantial. Do not turn it into a short recap.

----------------------------------------------------------------------
7.8 Conclusions
----------------------------------------------------------------------

The Conclusions should provide:

- study summary;
- main contributions;
- strongest findings;
- decisive limitations;
- prioritized future work;
- closing perspective.

Remove repeated full-method descriptions and repeated limitation inventories already handled in the Discussion.

Do not introduce new claims.

----------------------------------------------------------------------
7.9 Appendices and technical supplement
----------------------------------------------------------------------

The main thesis need not contain every repository-audit and evidence-management detail.

Candidate supplemental material includes:

- complete prompt inventory;
- full prompt-to-source mappings;
- exhaustive proxy-rule clauses;
- full DAG node and edge inventories;
- treatment-specific adjustment-set details;
- run manifests;
- configuration field tables;
- checksums;
- complete result-admission ledgers;
- artifact-status matrices;
- environment inventories;
- extended provenance gaps;
- line-level source mappings.

Do not merely move 25 pages into the existing appendix and leave the PDF unchanged.

Prefer a clearly identified external technical supplement or repository evidence package when permitted by the current thesis workflow. The main thesis should:

- summarize what the supplemental material establishes;
- cite or reference its stable location;
- retain enough explanation to stand on its own scientifically.

Do not move required institutional content, core methods, key assumptions, or primary results outside the thesis.

======================================================================
8. EDITING RULES
======================================================================

8.1 Scientific integrity

Do not change:

- retained numerical results;
- signs or rankings;
- dataset counts;
- estimator hierarchy;
- result-selection status;
- causal wording strength;
- proxy definitions without evidence;
- citations merely to make a paragraph shorter;
- figure data;
- table values;
- checked-result source files, except removing already-authorized stale SAnD rows where required.

Do not add unsupported claims.

8.2 Compression quality

Prefer:

- synthesis over deletion;
- one definitive explanation over repeated partial explanations;
- concise prose over status-heavy tables;
- cross-references over repetition;
- scientific narrative over repository narration;
- method concepts over file-path inventories;
- clear topic sentences;
- shorter transitions;
- direct language;
- active voice where appropriate.

Avoid:

- telegraphic prose;
- unexplained acronyms;
- dense walls of text;
- excessive parenthetical qualifications;
- paragraphs containing several unrelated limitations;
- hiding important assumptions in appendices;
- cosmetic edits that do not improve meaning or length;
- reducing font size, margins, spacing, or figure readability to lower page count.

Page reduction must come from editorial improvement, not typographic compression.

8.3 No arbitrary quota

Do not force every chapter to meet a proposed page allocation.

Do not stop at 103 pages merely because that number is reached.

Do not continue cutting strong material merely because the PDF remains above 103 pages.

At completion, report:

- initial physical PDF pages;
- final physical PDF pages;
- numbered body pages if practical;
- pages saved;
- where savings came from;
- which recommended cuts were rejected because they would damage quality.

A final PDF of 108, 112, or another defensible length is acceptable when the retained material is necessary and well written.

======================================================================
8.4 LINE SPACING AND VERTICAL RHYTHM
======================================================================

The ordinary narrative text of the thesis should use approximately 1.5 line spacing, in accordance with the institutional formatting instructions.

This is not an instruction to blindly add a global `\onehalfspacing`, `\setstretch{1.5}`, or equivalent command.

Before changing anything, inspect the existing LaTeX configuration and determine the effective current spacing.

Inspect at least:

- the document class;
- template and style files;
- `setspace` or equivalent packages;
- `\onehalfspacing`, `\doublespacing`, `\singlespacing`, and `\setstretch`;
- `\baselinestretch`;
- locally scoped spacing environments;
- chapter and front-matter definitions;
- table, caption, bibliography, list, quotation, and footnote formatting;
- any language-specific Hebrew or English environments that affect baseline spacing.

Determine whether ordinary body prose is already rendered at approximately 1.5 line spacing.

If body spacing is already approximately 1.5:

- preserve the existing implementation;
- do not add a duplicate spacing package;
- do not stack another spacing command on top of it;
- do not change it merely to make the configuration look more explicit;
- record how the existing template achieves the required spacing.

If ordinary body prose is not approximately 1.5:

- make the smallest safe change that brings ordinary narrative prose to approximately 1.5 spacing;
- prefer the existing template’s mechanism over introducing a competing global mechanism;
- scope the change carefully;
- verify both English and Hebrew narrative text;
- ensure that chapter openings, paragraphs, headings, equations, tables, and floats remain visually coherent.

The approximately 1.5-spacing requirement applies primarily to normal thesis prose. It must not be applied indiscriminately to every LaTeX environment.

Use context-appropriate spacing for elements such as:

- title and approval pages;
- compact author and institutional metadata;
- table of contents;
- list of figures;
- list of tables;
- abbreviation and notation lists;
- tables and tabular cells;
- figure and table captions;
- footnotes and endnotes;
- bibliography entries;
- code listings;
- algorithms;
- displayed equations and aligned equation groups;
- long quotations;
- itemized and enumerated lists;
- source-path or artifact listings;
- other dense technical material where 1.5 spacing would reduce readability or create excessive vertical expansion.

These elements may remain single-spaced or use the template’s established compact spacing when that is academically conventional and visually appropriate.

Do not assume that “1.5 spacing” means multiplying all vertical dimensions by 1.5. Line spacing, paragraph spacing, heading spacing, float spacing, table row height, equation spacing, and list spacing are separate typographic concerns.

Do not:

- add blank vertical space manually between ordinary paragraphs;
- use repeated `\\`, `\vspace`, or empty lines to imitate line spacing;
- increase table row spacing globally without inspecting every table;
- enlarge spacing in captions, bibliography, or contents solely because body text is 1.5-spaced;
- tighten body spacing to reduce the page count;
- reduce font size, margins, or baseline spacing as part of the compression exercise;
- alter Hebrew line spacing differently from equivalent English prose without a documented typographic reason;
- introduce inconsistent spacing between chapters through ad hoc local commands.

Paragraph separation should follow the existing thesis/template convention. Do not combine enlarged paragraph gaps with 1.5 line spacing unless the template already requires both and the rendered result is appropriate.

Spacing decisions must support readability, hierarchy, and professional appearance. The compression task must reduce pages through editorial improvement, consolidation, and relocation—not through tighter line spacing.

Required spacing validation:

1. Identify the source command or template rule controlling ordinary body spacing.
2. State whether it was already approximately 1.5 before editing.
3. If no spacing change was needed, leave the configuration unchanged.
4. If a change was required, document the exact scope and rationale.
5. Inspect representative pages containing:
   - ordinary English prose;
   - ordinary Hebrew prose;
   - chapter openings;
   - dense prose;
   - tables;
   - captions;
   - equations;
   - lists;
   - bibliography;
   - contents and front matter.
6. Confirm that ordinary prose is approximately 1.5-spaced while compact environments remain contextually formatted.
7. Confirm that no spacing change was used as an artificial page-count reduction technique.

======================================================================
9. PROTECTED MATERIAL
======================================================================

Before editing, create hashes or equivalent snapshots for:

- checked numerical result CSVs;
- result manifests;
- figure source data;
- figure image files;
- literature PDFs;
- active research code;
- DAG artifacts;
- proxy-rule source;
- prior committed evidence reports;
- title-page metadata;
- abstracts’ numerical claims;
- table values;
- citation keys;
- labels and cross-references.

Also snapshot the thesis’s:

- integers;
- decimals;
- percentages;
- sample sizes;
- model and estimator names;
- dataset names;
- proxy-state names;
- equations;
- figure filenames;
- table labels;
- citation keys.

After editing, compare the protected baseline.

Expected changes:

- SAnD-related active content;
- model-count wording from five to four where required;
- chapter/section structure and numbering;
- prose compression;
- approved table consolidation;
- references to relocated supplemental material;
- resulting PDF pagination.

Unexpected changes to retained scientific numbers, models, estimators, captions, figure data, or result tables are blocking.

======================================================================
10. ALLOWED FILES
======================================================================

Edits may be made to:

- thesis `.tex` source files;
- thesis bibliography source where needed for SAnD clearance;
- thesis build/index files required by chapter merging or renumbering;
- active thesis-facing tables generated from source text;
- active claim/evidence records that must reflect the corrected four-model scope;
- current release and validation ledgers;
- an existing canonical technical-supplement file or directory;
- one new supplement index only if no canonical supplement location exists and the new file has a durable role;
- one stage evidence report under the established logs directory.

Do not create unnecessary Markdown files.

Before creating any file:

1. inspect the target folder;
2. search for an existing file with the same role;
3. update the canonical file when possible;
4. create a new file only if the content has a distinct durable purpose.

======================================================================
11. FORBIDDEN CHANGES
======================================================================

Do not modify:

- research implementation code, except no code change should be needed;
- raw data;
- processed data;
- retained model outputs;
- retained causal outputs;
- checked values for STraTS, GRU, GRU-D, or TCN;
- retained figures or their plotted values;
- institutional forms;
- author/supervisor identity;
- official titles without explicit authority;
- ethics or governance facts;
- Git history;
- prior commits;
- unrelated dirty files.

Do not run:

- model training;
- preprocessing;
- causal estimation;
- result regeneration;
- new experiments.

LaTeX/document build and static validation are authorized.

======================================================================
12. REQUIRED VALIDATION
======================================================================

12.1 Static SAnD checks

Run exhaustive searches and provide a classified occurrence ledger.

The active thesis source and rendered PDF should contain zero SAnD references.

Also verify zero stale language for:

- five evaluated predictive models;
- five predictive families;
- ten final predictive runs;
- SAnD as a non-voting evaluated model;
- SAnD bibliography citation;
- SAnD abbreviation;
- SAnD result row.

Verify exact current language for:

- four learned models;
- four predicted sources;
- one rule-derived source;
- five aggregate sources.

12.2 LaTeX build

Perform a clean thesis build using the repository’s documented process.

Check:

- successful compilation;
- no undefined citations;
- no undefined references;
- no multiply defined labels;
- no missing files;
- no stale table-of-contents entries;
- no stale list-of-table entries;
- correct chapter numbering after any merge;
- correct table and figure numbering;
- correct bibliography;
- correct Hebrew and English abstracts;
- correct PDF metadata if affected.

Do not accept a build merely because a PDF was produced.

12.3 PDF content checks

Extract or inspect PDF text and confirm:

- SAnD is absent;
- four-model wording is consistent;
- §2.2.5 has the corrected title;
- Table 2.2 has four rows;
- abstracts agree with each other;
- contribution and conclusion sections agree;
- final aggregate lineage is stated correctly;
- chapter transitions remain coherent;
- references to merged or relocated sections resolve.

12.4 Visual inspection

Inspect every rendered PDF page at adequate resolution.

Pay particular attention to:

- title page;
- Hebrew abstract;
- English abstract;
- contents;
- list of tables;
- Section 2.2 and Table 2.2;
- consolidated tables;
- chapter boundaries after merging;
- large proxy-rule tables;
- DAG pages;
- Results tables and figures;
- Discussion tables;
- bibliography;
- appendix or supplement references.

Check:

- clipping;
- overlap;
- orphaned headings;
- widows/orphans where severe;
- blank or nearly blank pages caused by restructuring;
- table readability;
- figure readability;
- broken RTL text;
- broken references;
- excessive whitespace;
- headings detached from content.

Do not shrink text below a professionally readable size to save pages.

12.5 Protected-content comparison

Report:

- number of protected files changed;
- all retained numerical-token differences;
- citation-key additions/removals;
- label/reference changes;
- figure changes;
- table-value changes.

Only expected and justified changes may remain.

======================================================================
13. REQUIRED DELIVERABLES
======================================================================

Produce:

1. Revised thesis source.
2. Rebuilt `main.pdf`.
3. A concise SAnD-clearance occurrence ledger.
4. A compression map showing, for each chapter:
   - original role;
   - identified redundancy;
   - action taken;
   - material retained;
   - material consolidated;
   - material relocated;
   - material removed;
   - justification.
5. A table-consolidation ledger:
   - old table;
   - action;
   - destination;
   - scientific information preserved.
6. A protected-content comparison.
7. A visual-review ledger covering every PDF page.
8. A stage evidence report in the established thesis logs directory.

Do not create multiple narrative reports with overlapping purposes. Combine ledgers into the stage evidence report or established canonical validation files where practical.

======================================================================
14. EVIDENCE REPORT STRUCTURE
======================================================================

The final report must include:

# Stage: SAnD Clearance and Thesis Compression

## Baseline
- branch;
- starting HEAD;
- worktree status;
- current PDF page count;
- protected paths;
- prior SAnD-removal commit inspected.

## SAnD clearance
- files searched;
- occurrences found;
- occurrences removed;
- stale count language repaired;
- bibliography action;
- surviving historical references and justification;
- confirmation that active thesis scope contains four models.

## Compression diagnosis
- chapter-by-chapter findings;
- repeated concepts;
- audit-level material;
- overlapping tables;
- structural overlap;
- supplement candidates.

## Changes made
- files changed;
- chapter merges or renames;
- sections shortened;
- tables consolidated;
- material relocated;
- material removed;
- material deliberately retained despite length.

## Scientific preservation
- scope retained;
- claims retained;
- numerical results retained;
- estimator hierarchy retained;
- qualifications retained;
- any proposed compression rejected to protect quality.

## Validation
- static search results;
- build result;
- citation/reference result;
- PDF page count;
- page-level visual review;
- protected-content comparison;
- Hebrew/English consistency;
- SAnD zero-occurrence result.

## Length outcome
- starting physical pages;
- final physical pages;
- net reduction;
- main sources of reduction;
- explanation if the thesis remains materially above 103 pages.

## Unresolved issues
- scientific;
- administrative;
- layout;
- supplement;
- provenance.

## Readiness decision

Use exactly one:

READY FOR AUTHOR REVIEW

READY FOR AUTHOR REVIEW WITH NON-BLOCKING LENGTH CONSIDERATIONS

BLOCKED FOR SCIENTIFIC CONTENT — <reason>

BLOCKED FOR DOCUMENT INTEGRITY — <reason>

BLOCKED FOR SAnD CLEARANCE — <reason>

======================================================================
15. FINAL DECISION RULE
======================================================================

The task is successful when the thesis is:

- scientifically complete;
- materially less repetitive;
- more coherent;
- easier to read;
- free of active SAnD content;
- internally consistent about the four learned models;
- internally consistent about the five-source mixed aggregate;
- visually professional;
- fully compiled and cross-referenced;
- shorter because unnecessary documentary detail was removed or relocated;
- not weakened to satisfy an arbitrary page target.

The final page count is evidence about the outcome, not the definition of success.

Do not commit or push.