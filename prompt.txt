You are working inside the local `clinicause` repository.

Your task is to reorganize and complete the thesis literature corpus located under:

`thesis-writing/literature/`

This is a literature-maintenance task only. Do not modify thesis prose, experimental code, datasets, model outputs, or any files outside `thesis-writing/literature/`.

## Objective

Turn the current literature directory into a clean, reproducible corpus with:

1. One directory for core papers.
2. One directory for optional/background papers.
3. One canonical BibTeX database.
4. One canonical metadata catalog.
5. Consistent semantic PDF filenames.
6. Valid PDFs for all newly required papers.
7. No duplicate metadata, BibTeX entries, checksums, manifests, or README files.

Do not commit or push the changes.

---

# 1. Inspect before modifying

First inspect the current contents of:

* `thesis-writing/literature/papers/`
* `thesis-writing/literature/optional/`
* all metadata files currently stored in `thesis-writing/literature/`

The current metadata is expected to include some or all of:

* `README.txt`
* `README_filter_decisions.md`
* `filter_decisions.csv`
* `literature_phase1.bib`
* `manifest.md`
* `references.bib`
* `thesis_filtered_papers_roles.md`
* `MANIFEST_sha256.txt`

Before changing anything:

1. Inventory every PDF.
2. Extract or inspect each PDF’s title, authors, and year.
3. Record its current filename and SHA-256 hash.
4. Parse all existing BibTeX entries.
5. Detect duplicate citation keys, duplicate titles, duplicate DOIs, and byte-identical PDFs.
6. Confirm that `references.bib` already contains the 29 existing unique papers before adding anything.
7. Do not assume that two similarly named PDFs are duplicates; compare hashes, titles, and publication details.

Use the current files as source material, but do not preserve their duplicated structure.

---

# 2. Final directory structure

The final structure must be:

```text
thesis-writing/literature/
├── README.md
├── papers/
│   └── core PDF files only
├── optional/
│   └── optional/background PDF files only
└── metadata/
    ├── references.bib
    ├── catalog.csv
    └── checksums.sha256
```

Do not create another nested `literature/literature` directory.

After all information has been safely merged and validated, remove the obsolete root-level metadata files listed above. Their useful content must first be preserved in the new canonical files.

Git history is sufficient provenance; do not retain duplicate “legacy” metadata files merely as backups.

---

# 3. Move these existing papers to `optional/`

Move the following five papers out of `papers/` and into `optional/`.

Rename them using the exact target filenames below.

### 3.1 Epidemiological E-value perspective

Current file:

`01_sensitivity_evalue_unmeasured_confounding_vanderweele_ding_2017.pdf`

Target:

`optional/sensitivity_evalue_vanderweele_ding_2017.pdf`

Reason:

Useful epidemiological sensitivity-analysis background, but it is not the direct methodological basis of the implemented robustness-value analysis.

### 3.2 General sensitivity bounds

Current file:

`02_sensitivity_without_assumptions_ding_vanderweele_2016.pdf`

Target:

`optional/sensitivity_bounds_ding_vanderweele_2016.pdf`

Reason:

Useful technical background, but less directly aligned with the implemented `RV`, `cf_y`, and `cf_t`/`cf_d` workflow than omitted-variable-bias sensitivity methods.

### 3.3 Marginal structural models

Current file:

`05_method_marginal_structural_models_iptw_robins_hernan_brumback_2000.pdf`

Target:

`optional/causal_marginal_structural_models_robins_et_al_2000.pdf`

Reason:

Important for time-varying treatment-confounder feedback, but the current thesis implementation primarily performs point/binary exposure estimation rather than a full longitudinal marginal structural model.

### 3.4 Early InterpNet workshop version

Current file:

`08_model_irregular_clinical_timeseries_interpnet_shukla_marlin_2018.pdf`

Target:

`optional/model_interpnet_workshop_shukla_marlin_2018.pdf`

Reason:

This is an earlier workshop version. The ICLR 2019 Interpolation-Prediction Networks paper remains the canonical core citation.

### 3.5 General personalized evidence-based medicine review

Current file:

`16_review_personalized_ebm_hte_kent_steyerberg_2018.pdf`

Target:

`optional/review_personalized_ebm_hte_kent_et_al_2018.pdf`

Reason:

Good conceptual background, but substantially overlaps with the more directly relevant EHR, ICU, and modern HTE reviews.

Keep both the Lipkovich 2024 and Curth 2024 papers in the core corpus for now. They serve different roles: one is a methods tutorial and the other focuses on identification and validation challenges.

Optional papers must remain in the canonical BibTeX file and catalog, marked as optional. Do not delete them.

---

# 4. Download and add these 11 core papers

For every paper below:

1. Search by exact title and authors.
2. Verify bibliographic metadata against an authoritative source.
3. Download a lawful publicly accessible PDF.
4. Prefer sources in this order:

   * official publisher open-access PDF;
   * official journal or conference repository;
   * official guideline organization;
   * arXiv;
   * author or university institutional repository.
5. Do not use Sci-Hub, shadow libraries, suspicious mirrors, or copyright-bypassing services.
6. Verify that the downloaded file begins with the PDF magic bytes `%PDF`.
7. Run `pdfinfo` and extract the first page with `pdftotext`.
8. Confirm that the title and authors inside the PDF match the requested paper.
9. Reject HTML error pages, login pages, supplementary files, slide decks, abstracts without the paper, and unrelated papers.
10. Store the successful source URL and access date in `catalog.csv`.
11. Add a complete, normalized BibTeX entry.
12. Save the PDF using the exact filename specified below.

When both a final peer-reviewed version and a preprint exist, prefer the final version. If the final version is not lawfully downloadable, use the author manuscript or preprint, but record the relationship in `notes`.

## 4.1 Well-defined causal interventions

Paper:

Hernán and Taubman, “Does Obesity Shorten Life? The Importance of Well-Defined Interventions to Answer Causal Questions,” 2008.

Target filename:

`papers/causal_well_defined_interventions_hernan_taubman_2008.pdf`

Role:

Supports the distinction between an intervention/treatment and a derived clinical state or exposure. This is essential because variables such as Shock, RespFail, and RenalFail may not represent directly manipulable treatments.

Suggested citation key:

`hernan_taubman_2008_well_defined_interventions`

## 4.2 Omitted-variable-bias sensitivity analysis

Paper:

Cinelli and Hazlett, “Making Sense of Sensitivity: Extending Omitted Variable Bias,” 2020.

Target filename:

`papers/sensitivity_omitted_variable_bias_cinelli_hazlett_2020.pdf`

Role:

Direct methodological foundation for robustness values and benchmark-confounder sensitivity analysis.

Suggested citation key:

`cinelli_hazlett_2020_sensitivity`

## 4.3 Omitted-variable bias in causal machine learning

Paper:

Chernozhukov, Cinelli, Newey, Sharma, and Syrgkanis, “Long Story Short: Omitted Variable Bias in Causal Machine Learning.”

Use the final published version if available; otherwise use the latest official author version.

Target filename:

`papers/sensitivity_ovb_causal_ml_chernozhukov_et_al_2026.pdf`

Role:

Connects omitted-variable sensitivity analysis directly to causal machine-learning estimators and the EconML-style workflow.

Suggested citation key:

`chernozhukov_et_al_2026_ovb_causal_ml`

## 4.4 SOFA organ-dysfunction definition

Paper:

Vincent et al., “The SOFA (Sepsis-related Organ Failure Assessment) Score to Describe Organ Dysfunction/Failure,” 1996.

Target filename:

`papers/clinical_sofa_vincent_et_al_1996.pdf`

Role:

Provides clinical grounding for organ-dysfunction proxies and the interpretation of several derived clinical state tags.

Suggested citation key:

`vincent_et_al_1996_sofa`

## 4.5 Acute kidney injury definition

Document:

Kidney Disease: Improving Global Outcomes Acute Kidney Injury Work Group, “KDIGO Clinical Practice Guideline for Acute Kidney Injury,” 2012.

Download the official guideline PDF, not a short commentary about the guideline.

Target filename:

`papers/clinical_kdigo_acute_kidney_injury_2012.pdf`

Role:

Provides clinical grounding for renal-failure or acute-kidney-injury proxy rules based on creatinine and/or urine output.

Suggested citation key:

`kdigo_2012_acute_kidney_injury`

## 4.6 Berlin definition of ARDS

Paper:

ARDS Definition Task Force / Ranieri et al., “Acute Respiratory Distress Syndrome: The Berlin Definition,” 2012.

Target filename:

`papers/clinical_ards_berlin_definition_ranieri_et_al_2012.pdf`

Role:

Provides clinical grounding for respiratory-failure rules when the implemented variables approximate oxygenation, ventilation, or ARDS criteria.

Suggested citation key:

`ranieri_et_al_2012_berlin_ards`

## 4.7 ISTH disseminated intravascular coagulation criteria

Paper:

Taylor et al., “Towards Definition, Clinical and Laboratory Criteria, and a Scoring System for Disseminated Intravascular Coagulation,” 2001.

Target filename:

`papers/clinical_dic_isth_taylor_et_al_2001.pdf`

Role:

Provides clinical grounding for hematologic/coagulation-failure proxies when the rules use platelets or coagulation-related measurements.

Suggested citation key:

`taylor_et_al_2001_isth_dic`

## 4.8 Weak supervision and label aggregation

Paper:

Ratner et al., “Snorkel: Rapid Training Data Creation with Weak Supervision.”

Prefer the final journal version; use the official preprint only when necessary.

Target filename:

`papers/phenotyping_weak_supervision_snorkel_ratner_et_al_2020.pdf`

Role:

Provides a framework for combining noisy and correlated labeling sources. It supports discussing rule-based tags, model-predicted tags, majority voting, source dependence, and silver labels.

Suggested citation key:

`ratner_et_al_2020_snorkel`

## 4.9 Limited overlap and positivity

Paper:

Crump, Hotz, Imbens, and Mitnik, “Dealing with Limited Overlap in Estimation of Average Treatment Effects,” 2009.

Target filename:

`papers/causal_limited_overlap_crump_et_al_2009.pdf`

Role:

Supports overlap/positivity diagnostics, trimming, common support, and the risks created by rare clinical-state exposures.

Suggested citation key:

`crump_et_al_2009_limited_overlap`

## 4.10 DoWhy

Paper:

Sharma and Kıcıman, “DoWhy: An End-to-End Library for Causal Inference,” 2020.

Target filename:

`papers/software_dowhy_sharma_kiciman_2020.pdf`

Role:

Documents the causal workflow used by the project: modeling assumptions, identification, estimation, and refutation.

Suggested citation key:

`sharma_kiciman_2020_dowhy`

## 4.11 EconML

Paper:

Oprescu, Syrgkanis, Battocchi, Hei, and Lewis, “EconML: A Machine Learning Library for Estimating Heterogeneous Treatment Effects,” 2019.

Download the actual workshop paper, not presentation slides and not merely a printout of the GitHub page.

Target filename:

`papers/software_econml_oprescu_et_al_2019.pdf`

Role:

Documents the software library used for heterogeneous treatment-effect estimation. It complements, rather than replaces, the theoretical DML and generalized-random-forest citations.

Suggested citation key:

`oprescu_et_al_2019_econml`

---

# 5. Do not add LLM papers in this task

Do not download Med-PaLM, medical-question-answering, general LLM reasoning, or LLM causal-graph papers as part of this cleanup.

Search the repository only to determine whether an LLM is actually used in the implemented pipeline.

Record one brief note in `README.md`:

* If no implemented LLM component is found, state that LLM literature is intentionally out of scope.
* If an implemented LLM component is found, state that its literature coverage requires a separate scope decision.

Do not expand the corpus automatically based only on comments, old plans, or speculative future work.

---

# 6. Normalize every PDF filename

Rename all remaining core PDFs, not only the newly downloaded ones.

Use lowercase ASCII snake_case with this pattern:

`<topic>_<short_description>_<first_author_or_group>_<year>.pdf`

Allowed topic prefixes include:

* `model_`
* `dataset_`
* `causal_`
* `sensitivity_`
* `clinical_`
* `phenotyping_`
* `software_`
* `review_`
* `validation_`

Examples:

```text
model_strats_tipirneni_reddy_2022.pdf
model_tcn_bai_et_al_2018.pdf
model_interpnet_shukla_marlin_2019.pdf
model_sand_song_et_al_2018.pdf
model_gru_cho_et_al_2014.pdf
model_grud_che_et_al_2018.pdf
causal_generalized_random_forests_athey_et_al_2019.pdf
causal_causal_forest_wager_athey_2018.pdf
causal_double_machine_learning_chernozhukov_et_al_2018.pdf
dataset_mimiciii_johnson_et_al_2016.pdf
dataset_physionet_challenge_silva_et_al_2012.pdf
```

Requirements:

* Remove all numeric filename prefixes.
* Do not retain two independent numbering schemes.
* Do not use spaces, parentheses, commas, accented characters, or mixed case.
* Avoid excessively long filenames.
* Preserve every old filename in the `original_filename` column of `catalog.csv`.
* Update all metadata paths after renaming.
* Prefer `git mv` for tracked files.

Do not rename existing BibTeX citation keys merely to match filenames. Preserve existing keys wherever they are unique and valid, because future thesis text may depend on them.

---

# 7. Create one canonical BibTeX file

Create:

`thesis-writing/literature/metadata/references.bib`

Use the current `references.bib` as the starting point because it should already contain the existing combined 29-paper corpus.

Merge useful information from `literature_phase1.bib`, but do not create duplicate entries.

The final BibTeX file must:

1. Contain one entry per catalog item.
2. Include both core and optional papers.
3. Contain the 29 existing unique entries plus the 11 new entries: expected total 40.
4. Preserve existing unique citation keys.
5. Contain no duplicate keys.
6. Contain no duplicate DOI values.
7. Contain no duplicate normalized titles.
8. Use the correct entry type: `@article`, `@inproceedings`, `@misc`, `@techreport`, or another justified type.
9. Include complete metadata whenever available:

   * authors;
   * title;
   * year;
   * journal/booktitle/organization;
   * volume;
   * issue;
   * pages;
   * DOI;
   * authoritative URL.
10. Protect names and acronyms that BibTeX might incorrectly lowercase, including:

    * `{MIMIC-III}`;
    * `{PhysioNet}`;
    * `{STraTS}`;
    * `{GRU-D}`;
    * `{SOFA}`;
    * `{ARDS}`;
    * `{KDIGO}`;
    * `{DoWhy}`;
    * `{EconML}`;
    * `{CATE}`;
    * `{HTE}`.
11. Include:

    * `file = {../papers/<filename>}` for core papers;
    * `file = {../optional/<filename>}` for optional papers.
12. Add `keywords = {core}` or `keywords = {optional}` consistently.
13. Avoid fabricated DOI values or guessed publication details.
14. Represent the KDIGO guideline using an appropriate corporate-author format.
15. Use the official publication year for the version actually downloaded.

Validate the completed BibTeX file with an available BibTeX parser or `biber --tool`. If neither is available, perform a structural parse in Python and report that limitation.

---

# 8. Create one canonical metadata catalog

Create:

`thesis-writing/literature/metadata/catalog.csv`

This is the single source of truth for all non-BibTeX literature metadata.

Use UTF-8 and CSV quoting where needed.

Required columns:

```text
citation_key
title
authors
year
venue
doi
url
source_pdf_url
accessed_at
tier
topic
role_in_thesis
reason_optional
original_filename
pdf_path
pdf_status
sha256
notes
```

Rules:

* `tier` must be exactly `core` or `optional`.
* `pdf_status` must be one of:

  * `present`
  * `downloaded`
  * `missing`
  * `invalid`
* `pdf_path` must be relative to the literature directory.
* `source_pdf_url` must identify the actual PDF source, not just a search-result page.
* `url` should be the canonical paper landing page, DOI resolver, guideline page, or official project page.
* `accessed_at` must use ISO date format.
* `role_in_thesis` must be concise but specific.
* `reason_optional` must be populated for optional papers and empty for core papers.
* `original_filename` must preserve the filename before this reorganization.
* For newly downloaded papers, set `original_filename` to the filename received from the source before normalizing it, when available.
* Do not describe the clinical tags as ground-truth diagnoses. Use terms such as:

  * derived clinical state;
  * proxy phenotype;
  * clinically inspired proxy label;
  * weak clinical label.
* Distinguish software-library references from theoretical estimator references.

Expected final catalog:

* 40 total entries;
* 35 core entries;
* 5 optional entries.

If an expected paper cannot be downloaded lawfully, retain its metadata entry with:

* `tier=core`
* `pdf_status=missing`
* an empty `sha256`
* a clear explanation in `notes`

Do not substitute an unrelated paper merely to satisfy the expected count.

---

# 9. Generate canonical checksums

Create:

`thesis-writing/literature/metadata/checksums.sha256`

It must contain SHA-256 checksums for every valid PDF under:

* `papers/`
* `optional/`

Use paths relative to the literature directory.

Sort entries lexicographically by path.

Do not include metadata files themselves in this checksum file.

---

# 10. Create the new README

Create:

`thesis-writing/literature/README.md`

It must be concise and derived from the canonical metadata.

Include:

1. Purpose of the corpus.
2. Final directory structure.
3. Core versus optional policy.
4. Counts of core, optional, valid, and missing PDFs.
5. A topic-level summary:

   * irregular clinical time-series models;
   * datasets and benchmarks;
   * clinical proxy phenotyping;
   * causal identification and DAGs;
   * DML and causal forests;
   * overlap and sensitivity analysis;
   * weak supervision;
   * software tooling.
6. A terminology note explaining that the clinical tags are derived proxy states, not verified diagnoses or formal latent variables.
7. A note that:

   * E-value papers are optional background;
   * Cinelli–Hazlett and Chernozhukov et al. are the primary sensitivity references for the implemented robustness-value workflow.
8. A note explaining why the early InterpNet version is optional.
9. A note explaining that marginal structural models are retained for longitudinal background but are not the implemented estimator.
10. The LLM scope note described earlier.
11. Instructions for adding a future paper:

    * place PDF in the correct folder;
    * add one BibTeX entry;
    * add one catalog row;
    * regenerate checksums;
    * run duplicate and validity checks.

Do not reproduce the full catalog as a second long Markdown table. Link readers to `metadata/catalog.csv`.

---

# 11. Validation

Before finishing, run all of the following checks.

## PDF validation

For every PDF:

* verify `%PDF` magic bytes;
* run `pdfinfo`;
* confirm it has at least one page;
* extract first-page text;
* compare title/authors to the catalog;
* detect suspiciously small files;
* detect password-protected or corrupt files;
* ensure no file is actually HTML;
* ensure no slide deck was used when a paper was requested.

## Duplicate validation

Check for:

* identical SHA-256 hashes;
* duplicate normalized titles;
* duplicate DOIs;
* duplicate BibTeX keys;
* duplicate paper identity represented by final and preprint versions.

A final and preprint version of the same work should not both be core unless there is a documented methodological reason.

## Path validation

Check that:

* every catalog `pdf_path` exists unless status is `missing`;
* every PDF appears exactly once in the catalog;
* every BibTeX entry has a matching catalog row;
* every catalog row has a matching BibTeX entry;
* every valid PDF appears in `checksums.sha256`;
* no metadata file remains loose in the literature root except `README.md`;
* `papers/` contains only core PDFs;
* `optional/` contains only optional PDFs.

## Count validation

Expected state when all downloads succeed:

* 35 core papers;
* 5 optional papers;
* 40 catalog rows;
* 40 unique BibTeX entries;
* 40 valid PDF files.

If counts differ, explain the exact reason. Do not force counts by adding duplicates or unrelated files.

---

# 12. Cleanup

Only after the new files pass validation, remove the obsolete metadata files from the literature root:

* `README.txt`
* `README_filter_decisions.md`
* `filter_decisions.csv`
* `literature_phase1.bib`
* `manifest.md`
* `references.bib`
* `thesis_filtered_papers_roles.md`
* `MANIFEST_sha256.txt`

Do not remove useful information until it has been transferred into:

* `README.md`
* `metadata/references.bib`
* `metadata/catalog.csv`
* `metadata/checksums.sha256`

Remove empty temporary directories and temporary download files.

Do not leave `.part`, `.tmp`, HTML, or failed-download files in the corpus.

---

# 13. Final response

At completion, provide a concise report containing:

1. Final directory tree.
2. Number of core and optional papers.
3. List of newly downloaded papers with their source domains.
4. Any failed downloads and why they failed.
5. Old-to-new filename mapping for moved optional papers.
6. Number of PDFs renamed.
7. Duplicate PDFs or metadata entries found and how they were resolved.
8. BibTeX validation result.
9. PDF validation result.
10. Output of `git status --short`.
11. Any unresolved issue that requires a later methodological decision.

Do not claim success for a download unless the local PDF was opened, parsed, and title-verified.
