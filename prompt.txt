Task: Update the thesis and AAAI paper reproducibility instructions for the current CliniCause launcher

Recommended model: strongest available Codex coding/reasoning model
Reasoning effort: high

Repository: KenziVisor/CliniCause
Expected task baseline when this prompt was prepared:
0fe0649f9abc2959eb9653a57379e555d7143b37

Work from the actual current HEAD. Do not reset or rewrite later commits. If HEAD is
newer than the expected baseline, inspect and classify the intervening commits before
editing.

OBJECTIVE

Revise the reproducibility material in both the thesis and AAAI paper package so that
a new authorized user can understand, from a fresh machine, how to:

1. clone CliniCause together with both Git submodules;
2. recover correctly if the repository was cloned without submodules;
3. create and activate a Conda environment using Python 3.10;
4. install the complete project dependencies;
5. obtain the restricted raw ICU datasets through the appropriate PhysioNet access
   process;
6. place the raw datasets under the repository's default data locations, or supply
   external raw-data locations through the launcher environment variables;
7. construct the CliniCause causal-analysis datasets from the authorized raw data;
8. run the complete integrated pipeline through the canonical shell launcher.

The text should be practical, positive, technically precise, and evidence-bounded.
Present the implemented workflow as a meaningful reproducibility achievement while
retaining the genuine limitations around restricted data access and incomplete
historical producing lineage.

Do not merely add commands. Explain briefly what each phase achieves and distinguish:

- current implementation reproducibility;
- reconstruction from authorized raw inputs;
- exact historical reproduction of the archived thesis results.

MANDATORY BASELINE INSPECTION

Before editing, run and record:

git status --short
git branch --show-current
git rev-parse HEAD
git log -10 --oneline --decorate

Read all applicable AGENTS.md, README.md, and local instructions before editing.

Inspect at minimum:

- .gitmodules
- run_clinicause.sh
- router.py
- requirements-full.txt
- requirements-router.txt
- causal-irregular-time-series/requirements.txt
- STraTS/requirements.txt
- the preprocessing entry points for both datasets
- thesis-writing/thesis/appendices/appendices.tex
- thesis-writing/thesis/main.tex
- thesis-writing/thesis/README.md
- thesis-writing/paper-aaai/paper.tex
- thesis-writing/paper-aaai/supplement/technical_appendix.tex
- thesis-writing/paper-aaai/ReproducibilityChecklist.tex or the actual checklist
  location, if present
- existing paper build instructions and reports
- existing reproducibility and provenance reports under thesis-writing/reproducibility/
  and thesis-writing/logs/

Confirm the exact raw-data directory contracts from source before describing their
internal contents. Do not invent expected filenames, extracted layouts, credentials,
or download commands.

PHASE 1 — CREATE ONE UNIVERSAL ROOT REQUIREMENTS FILE

Complete this phase before editing the thesis or paper.

Objective:

Create one canonical dependency installation file at the CliniCause repository root:

requirements.txt

After this phase, `requirements.txt` must be the only requirements file owned by the
top-level CliniCause repository. Remove obsolete top-level alternatives such as:

- requirements-full.txt
- requirements-router.txt
- any other root-level `requirements*.txt` file whose role is superseded by the new
  requirements.txt

Scope boundary:

- This cleanup applies only to requirements files owned by the parent CliniCause
  repository.
- Do not edit, rename, move, delete, normalize, or otherwise modify any file inside:
  - STraTS/
  - causal-irregular-time-series/
- In particular, preserve:
  - STraTS/requirements.txt
  - causal-irregular-time-series/requirements.txt
  exactly as they are.

Mandatory inspection before consolidation:

1. Enumerate all requirements files while distinguishing parent-repository files from
   submodule files:

   find . -name 'requirements*.txt' -print

2. Read:
   - requirements-full.txt
   - requirements-router.txt
   - every other parent-owned root requirements file, if present
   - STraTS/requirements.txt, read-only
   - causal-irregular-time-series/requirements.txt, read-only

3. Search the parent repository for references to the old root filenames:

   git grep -n \
     -e 'requirements-full\.txt' \
     -e 'requirements-router\.txt' \
     -e 'pip install.*requirements'

4. Inspect router.py and parent-level Python imports to determine the dependencies
   specific to the parent orchestration layer.

Required implementation:

Create root `requirements.txt` as the single supported installation entry point.

The preferred design is:

- place all parent/router-specific package requirements directly in requirements.txt;
- include the unchanged submodule requirement files using pip recursive includes:

  -r causal-irregular-time-series/requirements.txt
  -r STraTS/requirements.txt

This is considered a universal root requirements file because:

- the user runs one installation command;
- it installs the parent router dependencies;
- it recursively installs both submodules' dependencies;
- the submodules retain their own independently maintained dependency declarations;
- no duplicate copy of all submodule dependencies needs to be maintained at the
  parent level.

Use relative paths from the repository root.

Do not flatten or copy all submodule dependencies into the root file unless recursive
includes are demonstrably incompatible with the current dependency specifications.
If flattening becomes necessary, explain why in the evidence report and still do not
modify the submodule files.

Consolidation rules:

- Preserve every actual dependency required by the parent and both submodules.
- Do not silently drop packages.
- Do not invent version pins.
- Preserve existing version constraints unless a concrete incompatibility requires a
  change.
- Detect duplicate or conflicting declarations.
- Where the same dependency appears with compatible constraints, let pip consume the
  declarations normally or consolidate only when equivalence is clear.
- Where constraints conflict, do not guess. Record the conflict and mark the task
  blocked unless the repository already establishes the authoritative constraint.
- Do not use `pip freeze` as the source of requirements.
- Do not convert the file into an environment lock file.
- Do not add platform-specific packages unless already supported by repository
  evidence.
- Do not install dependencies during this task.

Required final root layout:

requirements.txt

must exist, and the superseded parent-owned root requirements files must be deleted.

Expected user installation command:

python -m pip install -r requirements.txt

Repository-reference updates:

Update active parent-repository documentation or scripts that explicitly direct users
to install:

- requirements-full.txt
- requirements-router.txt

so they refer to:

requirements.txt

However, keep this task narrow:

- update only active user-facing setup references needed to prevent broken
  instructions;
- do not rewrite historical evidence reports merely because they mention the old
  filenames as historical facts;
- do not modify copied archival documentation unless it is an active user guide;
- do not edit anything inside either submodule.

Phase 1 validation:

1. Confirm the new file exists:

   test -f requirements.txt

2. Confirm the obsolete parent-owned files are absent.

3. Confirm submodule files are byte-for-byte unchanged:

   git -C STraTS status --short
   git -C causal-irregular-time-series status --short

   Both submodule worktrees must show no changes caused by this task.

4. Confirm the recursive includes resolve to existing files.

5. Parse the root dependency graph without installing packages. Use a safe static
   method appropriate to pip requirement syntax.

6. Search for stale active installation instructions:

   git grep -n \
     -e 'requirements-full\.txt' \
     -e 'requirements-router\.txt'

Classify any remaining occurrence as:

- historical and intentionally preserved;
- generated or archival;
- active and requiring correction.

Do not begin the thesis or paper edits until Phase 1 passes.

AUTHORITATIVE IMPLEMENTATION INTERFACE

Use run_clinicause.sh as the only documented top-level launcher.

Do not recommend run_clinicause_shell.sh.

The two required user-facing launcher commands are:

STAGES=dataset-extraction bash run_clinicause.sh

and:

STAGES=all bash run_clinicause.sh

Explain them accurately:

- `dataset-extraction` constructs the dataset/resource pipeline through the implemented
  dataset-construction and aggregation boundary. Confirm the exact terminal stage from
  router.py and describe it precisely.
- `all` runs the complete integrated pipeline.
- The default dataset selection is `both`, subject to confirmation from the current
  script.
- Dataset-specific runs may be shown as optional examples only if confirmed by the
  current script, using `DATASET=physionet` or `DATASET=mimic`.

DEPENDENCY COMMAND

The current root umbrella dependency file is requirements-full.txt. It includes:

- requirements-router.txt
- causal-irregular-time-series/requirements.txt
- STraTS/requirements.txt

Therefore, unless the current checkout contains a newly added root requirements.txt
with the same umbrella role, document:

python -m pip install -r requirements-full.txt

Do not write `pip install -r requirements.txt` when that file does not exist.

Do not rename or create dependency files in this task. If the local repository differs
from the expected state, document the actual supported root installation command and
explain the difference in the evidence report.

REQUIRED FRESH-START COMMAND SEQUENCE

The detailed thesis instructions and detailed paper supplement instructions must show
a coherent sequence equivalent to the following, adjusted only where current source
inspection proves a correction is necessary:

git clone --recurse-submodules https://github.com/KenziVisor/CliniCause.git
cd CliniCause

Also explicitly include the recovery command for users who already cloned without
submodules:

git submodule update --init --recursive

Explain that an ordinary clone may leave `STraTS/` and
`causal-irregular-time-series/` absent or uninitialized and that the recursive
submodule step is mandatory.

Then show:

conda create -n clinicause python=3.10 -y
conda activate clinicause
python -m pip install --upgrade pip
python -m pip install -r requirements-full.txt

Do not claim that this dependency declaration proves the exact historical environment
that produced the archived thesis results. It is the supported setup interface for the
current repository.

RAW-DATA ACCESS AND LOCATION

State clearly and professionally that:

- MIMIC-III and PhysioNet 2012 are not redistributed with CliniCause.
- Users must obtain authorized access through PhysioNet and comply with the applicable
  credentialing, training, license, and data-use requirements.
- Because the source records are access restricted, users reconstruct CliniCause from
  their own authorized raw-data copies.
- The pipeline does not bypass, automate, or weaken those access conditions.

Document the default repository-relative locations exposed by run_clinicause.sh:

data/physionet2012
data/mimiciii

Before finalizing the prose, inspect the preprocessing code and confirm what each path
must point to. Describe the path as the raw dataset root expected by the implementation
without inventing a specific extracted file hierarchy.

Also document the supported external-location interface:

export PHYSIONET_RAW_DATA_PATH=/absolute/path/to/physionet2012
export MIMIC_RAW_DATA_PATH=/absolute/path/to/mimiciii

STAGES=dataset-extraction bash run_clinicause.sh

Alternatively, an inline form may be shown:

PHYSIONET_RAW_DATA_PATH=/absolute/path/to/physionet2012 \
MIMIC_RAW_DATA_PATH=/absolute/path/to/mimiciii \
STAGES=dataset-extraction \
bash run_clinicause.sh

Make clear that relative paths are resolved from the CliniCause repository root and
absolute paths are accepted, if that remains true in the current launcher.

RUNNING THE WORKFLOW

Include the following two primary use cases.

A. Construct the reusable CliniCause datasets from authorized raw data:

STAGES=dataset-extraction bash run_clinicause.sh

Explain what artifacts this produces, where the run output is placed by default, and
what validation/provenance records are created, but only based on current router
behavior.

B. Run the complete pipeline:

STAGES=all bash run_clinicause.sh

Explain that this includes the integrated downstream predictive and causal workflow.
Mention that the complete run is computationally heavier and that temporal-model
stages may require suitable accelerator resources. Do not state exact hardware or
runtime requirements unless verified.

Optionally document these confirmed launcher controls in a compact paragraph or table:

- DATASET
- OUTPUT_ROOT
- RUN_ID
- PHYSIONET_RAW_DATA_PATH
- MIMIC_RAW_DATA_PATH
- STRATS_MAX_CONCURRENT
- PYTHON_BIN

Do not let optional controls obscure the two canonical commands.

THESIS EDIT

Primary allowed thesis file:

thesis-writing/thesis/appendices/appendices.tex

Expand the existing “Reproducibility and Evidence Boundary” appendix into a
self-contained reproducibility appendix.

Suggested structure:

1. Current reproducibility interface
2. Repository and submodule setup
3. Python 3.10 environment and dependencies
4. Restricted raw-data access
5. Default and external data locations
6. Dataset construction command
7. Complete-pipeline command
8. Outputs, validation, and provenance
9. Reproducibility boundary and limitations

The thesis may use the exact public repository URL:

https://github.com/KenziVisor/CliniCause.git

Preserve the important existing distinction that the current launcher and dependency
files define the supported current interface but do not establish the exact historical
command, environment, source revisions, splits, checkpoints, or export lineage that
produced every archived result.

Do not leave the appendix dominated by limitations. Begin with what is now available
and usable, then close with a proportionate limitations paragraph.

PAPER EDIT

Potentially allowed paper files:

- thesis-writing/paper-aaai/paper.tex
- thesis-writing/paper-aaai/supplement/technical_appendix.tex

The main AAAI paper has strict space constraints. Keep the main-paper addition compact:
approximately one concise paragraph or a short subsection near the existing
Dataset Validation and Provenance discussion or the limitations/reproducibility
material.

The main paper should state that the repository provides:

- a single top-level launcher;
- a dedicated dataset-construction mode;
- a complete-pipeline mode;
- explicit submodule, environment, data-path, validation, and provenance interfaces;
- reconstruction from authorized source data.

Place the detailed scratch-to-run command guide in the technical appendix under an
expanded section such as:

“Code Availability and Reproduction Workflow”

or:

“Reproduction from Authorized Raw Data”

The technical appendix should contain the complete clone, submodule, Conda,
dependency, data-placement, override, dataset-construction, and full-run commands.

ANONYMITY REQUIREMENT

The paper is currently in anonymous-submission mode.

Do not place:

https://github.com/KenziVisor/CliniCause.git

or any other author-identifying URL in paper.tex or the anonymous supplement unless
the repository already contains an explicitly approved review-safe/anonymized release
URL.

Search for an existing anonymous repository or artifact URL.

If one exists, use it.

If none exists:

- keep the exact public URL in the thesis only;
- use the project's existing anonymous repository macro or neutral placeholder if one
  exists;
- otherwise use non-identifying prose in the main paper and record a human gate for
  insertion of an anonymized repository URL;
- do not invent an anonymous URL;
- do not expose author identity;
- report exactly what URL decision remains unresolved.

The commands may use:

git clone --recurse-submodules <ANONYMIZED_REPOSITORY_URL>

in the anonymous technical appendix only when a visible placeholder is compatible with
the current paper workflow. If visible placeholders are prohibited by current project
policy, keep the command sequence in source comments or the evidence report and mark
the anonymous URL as a blocking human input.

TONE AND CLAIM BOUNDARY

Use an optimistic but accurate framing similar in substance to:

“CliniCause now exposes a unified, submodule-aware execution interface that can
reconstruct the analysis resources from authorized raw inputs and execute the
integrated predictive and causal workflow.”

Emphasize achieved capabilities:

- one canonical launcher;
- reproducible repository composition through pinned submodules;
- one umbrella dependency installation;
- explicit raw-data path contracts;
- separate dataset-construction and complete-pipeline modes;
- dataset-isolated execution;
- validation, cohort, artifact, receipt, and provenance contracts;
- the ability to rebuild derived causal-analysis resources without redistributing
  restricted patient-level source data.

Retain limitations without sounding defeatist:

- raw source access remains restricted;
- users must satisfy external data-use conditions;
- full runs require appropriate compute;
- software and hardware differences may affect runtime;
- the current interface does not by itself recover every missing historical producing
  revision, split, checkpoint, configuration, or environment detail;
- current-code reproducibility is not automatically identical to exact historical
  reproduction of archived results;
- clinical validation and causal identification remain separate from software
  reproducibility.

Do not repeat the same limitation paragraph in several places. State it once clearly
in each deliverable at the appropriate level of detail.

PROTECTED CONTENT

Do not change:

- numerical results;
- record counts;
- exposure counts;
- estimator findings;
- prediction metrics;
- figures;
- tables;
- captions unrelated to reproducibility;
- causal or clinical interpretation;
- bibliography entries unless a genuinely missing citation is required;
- paper title, authorship, submission mode, or anonymity settings;
- run_clinicause.sh;
- router.py;
- requirement files;
- submodule pointers;
- research code;
- generated result artifacts;
- AAAI style or author-kit files.

Do not run:

- preprocessing;
- model training;
- inference;
- causal estimation;
- the complete pipeline;
- Slurm jobs;
- real-data commands;
- dependency installation or environment mutation.

This is a documentation and build-validation task only.

ALLOWED FILES

Edit only as necessary:

- thesis-writing/thesis/appendices/appendices.tex
- thesis-writing/paper-aaai/paper.tex
- thesis-writing/paper-aaai/supplement/technical_appendix.tex
- one new evidence report under thesis-writing/logs/, named clearly for this task

Do not edit all three scientific files merely because they are allowed. Keep the
main-paper change concise and put detailed commands in the thesis and supplement.

VALIDATION

Perform static command validation:

1. Confirm every documented path and environment-variable name against
   run_clinicause.sh and router.py.
2. Confirm requirements-full.txt exists and includes the router and both submodule
   requirement files.
3. Confirm `.gitmodules` contains both expected submodules.
4. Confirm the documentation contains:
   - `git clone --recurse-submodules`
   - `git submodule update --init --recursive`
   - Python 3.10 Conda creation
   - dependency installation
   - both default raw-data paths
   - both raw-data override variables
   - `STAGES=dataset-extraction bash run_clinicause.sh`
   - `STAGES=all bash run_clinicause.sh`
5. Confirm no changed document recommends run_clinicause_shell.sh.
6. Confirm no paper file exposes an identifying URL without explicit existing
   authorization.
7. Confirm no archived-result claim has been strengthened into an exact historical
   rerun claim.
8. Run:
   bash -n run_clinicause.sh
9. Run:
   git diff --check
10. Build the thesis using its existing canonical build command.
11. Build the AAAI paper using its existing canonical build procedure.
12. Build the technical appendix using its existing canonical build procedure.
13. Inspect the changed PDF pages for:
    - clipped command blocks;
    - overfull lines caused by URLs or shell commands;
    - unreadable code;
    - broken references;
    - undesirable page-count or float disruption.

Do not install missing LaTeX tools. If the build environment is unavailable, report
that honestly and perform all possible static checks.

EVIDENCE REPORT

Create:

thesis-writing/logs/reproducibility_execution_guide_update.md

The report must contain:

- current HEAD and branch;
- task baseline;
- worktree status before editing;
- instructions and files inspected;
- exact files changed;
- documented commands;
- confirmation of the actual umbrella requirements filename;
- confirmation of both submodules;
- raw-data path contract findings;
- paper anonymity decision;
- static validation results;
- thesis build result;
- paper build result;
- technical-appendix build result;
- protected-content confirmation;
- unresolved human gates;
- final readiness decision.

READINESS WORDING

Use exactly one of:

READY FOR AUTHOR REVIEW — REPRODUCIBILITY GUIDES UPDATED

READY FOR AUTHOR REVIEW WITH ANONYMOUS-REPOSITORY URL GATE

BLOCKED — <specific reason>

Do not commit, stage, push, reset, clean, or modify submodule revisions.