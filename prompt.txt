# CliniCause AAAI-27 — Stage P5: Write and Numerically Audit the Results Section

## Model and reasoning

* **Model:** GPT-5.6
* **Reasoning effort:** High

This stage requires exact numerical extraction, cross-source validation, result-led scientific writing, and production of a legible estimator-comparison figure. Do not use Extra High unless authoritative result sources materially conflict.

---

## Stage identity

Implement:

```text
Stage P5 — Results Draft
```

Write the complete Results section of the AAAI-27 manuscript using only approved, checked repository evidence.

This stage must deliver:

* the complete Section 5;
* the full predictive-results table;
* the cross-estimator comparison figure;
* the headline CausalPFN finding;
* the primary effect patterns;
* the matching and downsampling findings;
* a durable numerical audit in the P5 report.

---

## User-approved repository policy

The user is temporarily committing all generated and modified files and has deferred repository cleanup.

Therefore:

* do not remove tracked LaTeX auxiliary files;
* do not add or modify `.gitignore`;
* do not repair or rewrite earlier mixed-scope commits;
* do not clean the repository;
* do not treat generated build files as a blocker;
* preserve all pre-existing work.

The user will commit the work.

Do not stage, commit, push, reset, restore, revert, or clean anything.

---

## Canonical authority

Read completely before editing:

```text
thesis-writing/paper-aaai/clinicause_aaai27_paper_operational_plan.md
```

Also read:

```text
thesis-writing/paper-aaai/paper.tex
thesis-writing/paper-aaai/paper_evidence_map.md
thesis-writing/paper-aaai/aaai_structure_notes.md
thesis-writing/paper-aaai/paper_build_report.md
thesis-writing/paper-aaai/reports/P3_dataset_construction_validation_report.md
thesis-writing/paper-aaai/reports/P4_empirical_evaluation_report.md
```

The operational plan is authoritative for:

* the resource-first paper framing;
* the result hierarchy;
* the protected numerical claims;
* CausalPFN positioning;
* the original-versus-downsampled hierarchy;
* the no-pooling policy;
* claim strength;
* optimistic but precise tone;
* the page and visual budget.

Do not redesign the paper.

---

## Accepted scientific baseline

The accepted current P4 commit is:

```text
b604d68fdf85aa278d80c3f8916c9fd1ef837bcc
AAAI p4
```

The accepted P3 content commit is:

```text
60504040c86721782e6fdf8a29971c8b1e0ab9e4
paper: draft dataset construction and validation
```

Verify the repository state locally. Do not assume the commit list or worktree is unchanged.

---

## Mandatory initial inspection

Run:

```bash
git status --short
git branch --show-current
git rev-parse HEAD
git log -8 --oneline --decorate
git submodule status
```

Inspect the latest two commits:

```bash
git show --stat --oneline HEAD
git show --stat --oneline HEAD~1
git diff --name-status HEAD~2..HEAD
```

Inspect the accepted P4 manuscript changes:

```bash
git diff \
  14337a293eee03a24216c299c312ad5c7d61b3a7 \
  b604d68fdf85aa278d80c3f8916c9fd1ef837bcc \
  -- thesis-writing/paper-aaai/paper.tex
```

Record:

* current HEAD;
* branch;
* dirty files;
* relevant nested revisions;
* files changed after P4;
* whether any user changes overlap the permitted P5 files.

Do not alter unrelated work.

---

# Objective

Replace the current Results placeholders with a complete polished section:

```text
5 Results
```

Use this structure:

```text
5.1 Predictive Characterization
5.2 Effect Patterns and Estimator Agreement
5.3 Robustness and Cross-Dataset Findings
```

A short resource-oriented opening paragraph may appear directly under the Section 5 heading before Subsection 5.1.

The section must answer:

1. What resources were empirically characterized?
2. Which predictive model led each dataset?
3. What were the primary CausalForestDML patterns?
4. How strongly did LinearDML agree?
5. How strongly did CausalPFN agree?
6. What was the central disagreement?
7. What did matching show?
8. How stable were directions under outcome downsampling?
9. What cross-dataset conclusions are supported?

---

# Evidence hierarchy

## Level 1 — Checked result sources

Use the checked sources under:

```text
thesis-writing/results/
```

Locate the authoritative files for:

* primary cohort counts;
* predictive metrics;
* predictive selection status;
* original-cohort CATE values for all three estimators;
* exposure prevalence;
* matching results;
* matching failures;
* sensitivity results and provenance classes;
* treatment permutations;
* outcome permutations;
* original-versus-downsampled comparisons;
* result manifests;
* checksums;
* result-admission decisions.

Likely source families include checked CSV files and the result-source packet. Use the actual paths found in the repository.

The checked result tables override prose summaries when values differ.

## Level 2 — Decision and provenance records

Inspect:

```text
thesis-writing/planning/
thesis-writing/reproducibility/
thesis-writing/audit/
thesis-writing/logs/
```

Use these for:

* primary-versus-robustness hierarchy;
* approved result selection;
* source status;
* provenance limitations;
* display and interpretation rules.

## Level 3 — Thesis

Inspect:

```text
thesis-writing/thesis/chapters/10_results.tex
thesis-writing/thesis/chapters/11_discussion.tex
```

Use actual filenames if they differ.

The thesis can guide concise interpretation, but all numbers must be rechecked against Level 1 sources.

## Level 4 — Existing manuscript and evidence map

Use:

```text
thesis-writing/paper-aaai/paper_evidence_map.md
thesis-writing/paper-aaai/paper.tex
```

Preserve the terminology and estimator hierarchy established in Sections 3 and 4.

---

# Protected headline findings

Verify every value before inserting it.

The accepted headline findings are:

```text
Primary causal-analysis records:
- MIMIC-III: 26,845
- PhysioNet 2012: 7,993

Admitted proxy-state exposures:
- MIMIC-III: 9
- PhysioNet 2012: 10

Predictive models:
- STraTS
- GRU
- GRU-D
- TCN

MIMIC-III archived predictive leader:
- STraTS across test loss, AUROC, AUPRC, and minRP

PhysioNet archived predictive leader:
- GRU-D across test loss, AUROC, AUPRC, and minRP

CausalForestDML versus LinearDML directional agreement:
- 19/19 dataset–exposure comparisons

Agreement across CausalForestDML, LinearDML, and CausalPFN:
- 18/19 dataset–exposure comparisons

Central disagreement:
- PhysioNet shock

Original-versus-downsampled directional preservation:
- 55/57 estimator–dataset–exposure comparisons
```

Do not insert a value merely because it appears in this prompt. Revalidate it against the checked repository source.

If an authoritative conflict appears, stop that claim, document the conflict, and use the report readiness status to request a narrow repair.

---

# Result-writing rule

Every substantive Results paragraph should follow:

```text
finding
→ quantitative evidence
→ scientific meaning
→ one local boundary where necessary
```

Do not begin Results paragraphs with limitations.

Use direct and confident language.

Avoid:

```text
we merely
we only
we just
preliminary
tentative
unfortunately
```

Do not overstate clinical or causal interpretation.

---

# Section opening — Resource-oriented result context

Open Section 5 with a compact paragraph that reconnects the empirical results to the resource contribution.

State that:

* the two validated causal-analysis resources contain 26,845 MIMIC-III and 7,993 PhysioNet analysis records;
* they cover nine and ten admitted proxy-state exposures, respectively;
* the full evaluation sequence was applied separately to both;
* results remain dataset specific and are not numerically pooled.

Do not repeat the full Table 1 description.

---

# 5.1 Predictive Characterization

## Required prose

Write two compact paragraphs.

### Paragraph 1 — Headline comparison

State confidently:

> STraTS led all four archived MIMIC-III test metrics, whereas GRU-D led the corresponding PhysioNet metrics.

Explain the scientific meaning:

* the strongest representation differed by dataset;
* representation choice depends on source-specific variables, missingness, sampling, and proxy definitions;
* the experiment supports dataset-dependent architecture selection.

Do not claim statistical superiority.

### Paragraph 2 — Metric-level context

Summarize the most important metric comparisons without narrating all 32 cells.

Use exact values from Table 2.

Mention, at minimum:

* MIMIC-III STraTS AUROC and AUPRC;
* PhysioNet GRU-D AUROC and AUPRC;
* that within each dataset the same model led loss, AUROC, AUPRC, and minRP.

State once that these are archived point estimates against rule-derived targets.

Do not repeat the full limitations discussion from Section 4.

---

# Table 2 — Predictive Results

Replace the current predictive placeholder with a complete two-column table.

Use all eight model–dataset rows and all four metrics:

```text
Dataset
Model
Test loss
Test AUROC
Test AUPRC
Test minRP
```

Expected checked display values are:

```text
MIMIC-III
STraTS   0.348  0.905  0.869  0.795
GRU      0.386  0.881  0.840  0.770
GRU-D    0.404  0.884  0.841  0.773
TCN      0.414  0.867  0.823  0.758

PhysioNet 2012
STraTS   0.341  0.915  0.877  0.818
GRU      0.397  0.915  0.897  0.831
GRU-D    0.331  0.918  0.905  0.835
TCN      0.477  0.899  0.875  0.811
```

Revalidate every value.

Formatting requirements:

* full-width `table*` unless a one-column version remains fully legible;
* bold the best value in each dataset for each metric;
* for loss, smaller is better;
* for AUROC, AUPRC, and minRP, larger is better;
* use three decimal places;
* keep table text at 9 or 10 points;
* do not use `\resizebox`;
* use the AAAI caption convention;
* caption must say these are archived selected held-out test results;
* caption or nearby prose must state that no uncertainty or significance claim is implied.

Do not omit loss or minRP unless the full table cannot fit legibly. If compression is required, document the decision and move omitted columns to a clearly marked supplement plan.

---

# 5.2 Effect Patterns and Estimator Agreement

Write three or four result-led paragraphs.

## Paragraph 1 — Primary CausalForestDML pattern

Report:

* all nine MIMIC-III mean model-estimated CATE summaries were positive;
* nine of ten PhysioNet summaries were positive;
* PhysioNet shock was negative.

Use the exact checked values.

Where space permits, identify the largest primary values:

### Expected MIMIC-III leaders

```text
Cardiac strain: approximately 0.220
Inflammation/sepsis: approximately 0.161
```

### Expected PhysioNet leaders

```text
Renal dysfunction: approximately 0.120
Cardiac injury/strain: approximately 0.112
Global severity: approximately 0.108
```

### Expected central negative value

```text
PhysioNet shock: approximately -0.014
```

Revalidate all values and construct names.

Interpretation:

* the resources contain systematic mortality-related variation across the implemented proxy-state and adjustment framework;
* magnitude and ordering differ by dataset and construct.

Use the established term:

```text
mean model-estimated CATE over the analyzed sample
```

Do not call these risk ratios, odds ratios, intervention effects, or treatment recommendations.

## Paragraph 2 — DML agreement

State prominently:

> CausalForestDML and LinearDML agreed in direction for all 19 prespecified dataset–exposure comparisons.

Frame this as:

* complete directional concordance across nonlinear and linear final-stage DML models;
* evidence that the sign pattern is not unique to one DML final-stage form.

Do not claim equal magnitudes or identification.

## Paragraph 3 — CausalPFN headline result

This is a headline paragraph, not a footnote.

State prominently:

> CausalPFN reproduced the prevailing direction in 18 of 19 prespecified comparisons.

Use confident positive framing:

* it supplies strong directional triangulation from a distinct modeling perspective;
* it demonstrates promise as a complementary estimator for the constructed resources;
* the result is especially valuable because it is compared over the complete prespecified exposure set, not a favorable subset.

Use one bounded sentence noting that its archived estimator-specific diagnostic package is less complete than the DML package.

Do not repeatedly call CausalPFN exploratory.

Do not add unsupported architecture, training, theory, novelty, or method-attribution claims.

## Paragraph 4 — Informative exception

Explain the PhysioNet-shock disagreement clearly:

* CausalForestDML and LinearDML were negative;
* CausalPFN was slightly positive;
* the descriptive matching result was positive, if confirmed;
* this is the sole three-estimator sign disagreement.

Present the exception as scientifically informative:

* it reveals model and support sensitivity;
* it should remain visible rather than being resolved by estimator voting;
* it motivates deeper analysis of proxy definition, support, adjustment assumptions, and estimator behavior.

Do not describe the negative DML estimate as a clinically protective effect.

---

# Figure 2 — Cross-Estimator Agreement

Replace the current Figure 2 placeholder with an actual paper-ready figure whenever the local environment permits.

## Preferred figure

Create a full-width two-panel horizontal dot plot:

* left panel: nine MIMIC-III exposures;
* right panel: ten PhysioNet exposures;
* one row per exposure;
* three markers per row:

  * CausalForestDML;
  * LinearDML;
  * CausalPFN;
* horizontal axis: mean model-estimated CATE;
* visible zero reference line;
* exact checked full-precision values as input;
* concise human-readable proxy labels;
* PhysioNet shock clearly visible;
* magnitude differences preserved.

Do not replace the values with signs only.

## Ordering

Within each dataset, use one stable, documented ordering:

* either decreasing CausalForestDML value;
* or the checked source order.

State the ordering in the caption or generation script.

Do not omit any of the 19 comparisons.

## Visual design

The figure must be:

* readable in two-column width;
* grayscale safe;
* differentiated by both marker shape and color or line style;
* legible at normal PDF zoom;
* at least 9-point text at final placement;
* vector PDF where possible;
* free of clipped labels;
* free of Type 3 fonts;
* anonymous;
* fully reproducible from checked repository sources.

## Generation artifacts

Preferred paths:

```text
thesis-writing/paper-aaai/figures/figure2_estimator_agreement.pdf
thesis-writing/paper-aaai/figures/generate_figure2_estimator_agreement.py
```

The script must:

* read the checked result source rather than transcribing the 19×3 matrix manually;
* filter the original-sampling primary rows explicitly;
* validate that exactly 19 dataset–exposure combinations and three estimators are present;
* fail on duplicate or missing rows;
* retain full precision for plotting;
* avoid patient-level data;
* use only checked aggregate result files;
* document the input path and selection rule;
* produce deterministic output.

If Python or required plotting packages are unavailable:

1. do not install packages;
2. attempt a reproducible LaTeX-native vector implementation using already available permitted packages;
3. if that is also unavailable, produce a compact complete estimator-agreement table in the main paper and retain a fully specified Figure 2 placeholder;
4. document the exact blocker in the report.

Do not fabricate a graphical artifact.

## Caption

The caption should state:

* all 19 prespecified original-cohort comparisons are included;
* CausalForestDML and LinearDML agree in 19/19;
* all three estimators agree in 18/19;
* PhysioNet shock is the three-estimator exception;
* the figure preserves magnitude differences;
* values are mean model-estimated CATE summaries over the analyzed samples.

Do not turn the caption into a limitations paragraph.

---

# 5.3 Robustness and Cross-Dataset Findings

Write two or three concise paragraphs.

## Matching result

Verify and report:

* eight of nine MIMIC-III exposures produced a matching summary;
* seven of ten PhysioNet exposures produced a matching summary;
* therefore 15 of 19 comparisons were successful;
* 14 of those 15 successful matching summaries shared the primary CausalForestDML direction.

State that:

* failed matching rows are support information, not zero effects;
* the failures resulted from the absence of usable binary matching columns after preprocessing, if confirmed;
* detailed failures and distances can move to the supplement.

Name the four failed exposures only if space permits and all names are verified.

## Outcome-downsampling result

State prominently:

> Direction was preserved in 55 of 57 original-versus-downsampled estimator–dataset–exposure comparisons.

Explain:

* this is broad sign stability under a substantial mortality-class perturbation;
* every magnitude need not remain equal;
* the two sign changes remain explicit.

Identify the two sign-changing comparisons if the checked sources support concise naming.

Do not interpret downsampled magnitudes as estimates for the original population.

## Sensitivity and permutation evidence

Include one compact result sentence or paragraph only when the checked sources support an unambiguous result.

Possible content:

* DML sensitivity evidence exists across several provenance classes;
* treatment and outcome permutations provide disruption-based checks;
* coverage is not uniform across all exposures;
* equivalent diagnostic stages were not archived for CausalPFN.

Do not enumerate every diagnostic in the main paper.

Move full coverage tables to the supplement plan.

## Cross-dataset synthesis

End the Results section positively:

* the same resource-construction and evaluation sequence operated across both datasets;
* predictive leadership differed by dataset;
* effect rankings and at least one sign differed by dataset;
* estimator agreement was nevertheless broad;
* this demonstrates workflow portability while preserving dataset-specific semantics.

Do not claim pooled clinical replication.

---

# Numerical audit requirement

Every rendered number must be entered into the P5 report’s numerical audit.

For each number record:

```text
Audit ID
Manuscript location
Rendered value
Full-precision source value
Unit or scale
Dataset
Estimator/model
Exposure, where applicable
Sampling condition
Source file
Source row or selection predicate
Source status
Rounding rule
Cross-check source
Qualification
```

The audit must include:

* all 32 predictive metric cells;
* both cohort counts;
* both exposure counts if rendered again;
* every causal value displayed in prose;
* all 57 values plotted in Figure 2;
* 19/19;
* 18/19;
* matching success counts;
* 14/15;
* 55/57;
* every named exception.

Do not use manuscript prose or this prompt as a numerical source.

---

# Evidence-map update

Update:

```text
thesis-writing/paper-aaai/paper_evidence_map.md
```

For every Results claim:

* connect it to an existing claim ID or create a new claim ID;
* record the exact manuscript location;
* record the authoritative checked source;
* record the full result-selection predicate;
* classify support status;
* record the local qualification;
* link the claim to Table 2, Figure 2, or the relevant paragraph;
* preserve existing TODO gates.

At minimum map:

* resource counts;
* all predictive metric rows;
* dataset-specific model leadership;
* MIMIC-III primary sign pattern;
* PhysioNet primary sign pattern;
* largest primary values;
* 19/19 DML agreement;
* 18/19 three-estimator agreement;
* CausalPFN interpretation;
* PhysioNet-shock disagreement;
* matching availability and direction agreement;
* 55/57 downsampling stability;
* cross-dataset interpretation.

---

# Main-paper space budget

Target for Section 5:

```text
1.45–1.75 AAAI technical-content pages
```

Target:

* 7–9 substantive paragraphs;
* Table 2;
* Figure 2;
* no additional main-paper table unless Figure 2 cannot be generated.

The figure and table may float across nearby pages, but the combined manuscript architecture must remain viable for a seven-page technical-content limit.

Do not compress by changing:

* font size;
* margins;
* line spacing;
* column width;
* float spacing;
* AAAI style settings.

Do not use `\resizebox`.

If the section exceeds the target, shorten repetitive interpretation before removing checked findings.

---

# Manuscript integration

Preserve:

```text
3 Constructing the CliniCause Datasets
4 Empirical Evaluation
5 Results
6 Discussion and Limitations
7 Conclusion
```

Section 5 should follow directly from the Evaluation section.

Do not revise Sections 3 or 4 except for one strictly necessary transition sentence. Record any such edit in the report.

Do not write:

* the Abstract;
* the Introduction;
* Related Work;
* Discussion;
* Conclusion.

---

# Mandatory P5 report

Create:

```text
thesis-writing/paper-aaai/reports/P5_results_report.md
```

The report must be complete enough for independent validation without Codex chat output.

Include:

```text
Stage
Model
Reasoning effort
Current HEAD before work
Branch
Last two commits inspected
Accepted P4 commit
Worktree state before work
Nested revisions
Canonical plan read
Checked result sources inspected
Files changed
Section structure
Paragraph count
Approximate prose word count
Measured PDF page footprint
Table 2 structure
Table 2 numerical source
Figure 2 generation method
Figure 2 input source and filter
Figure 2 row-count validation
Figure 2 estimator-count validation
Figure 2 visual inspection
Primary CausalForestDML findings
LinearDML agreement result
CausalPFN agreement result
PhysioNet-shock result
Matching result
Downsampling result
Sensitivity/permutation summary
Cross-dataset result
Citation keys used
Evidence-map changes
Complete numerical audit
Rounding policy
Source conflicts
TODO-EVIDENCE items
TODO-RUNTIME items
TODO-RELEASE items
Build command
Build result
PDF page count
PDF page size
Font validation
LaTeX warnings
Visual inspection findings
Protected-file verification
Repository-cleanup deferral
No-commit/no-push verification
Readiness decision
```

The report must explicitly state:

* every rendered number was traced to a checked source;
* no number was copied from the prompt without verification;
* no favorable exposure or estimator subset was selected;
* all 19 original-cohort comparisons appear in Figure 2 or its accepted fallback;
* CausalPFN is presented prominently;
* no unsupported CausalPFN methodological citation was added;
* matching failures are not interpreted as zero effects;
* no experiment or model run was performed;
* no commit or push was performed;
* repository cleanup remains deferred by user decision.

End with exactly one of:

```text
READY FOR STAGE P6 — RESULTS DRAFTED AND NUMERICALLY MAPPED
```

or:

```text
NARROW REPAIR REQUIRED BEFORE STAGE P6 — <EXACT DEFECT>
```

or:

```text
BLOCKED BEFORE STAGE P6 — <EXACT REASON>
```

---

# Allowed edits

You may edit or create only:

```text
thesis-writing/paper-aaai/paper.tex
thesis-writing/paper-aaai/paper.pdf
thesis-writing/paper-aaai/paper_build_report.md
thesis-writing/paper-aaai/paper_evidence_map.md
thesis-writing/paper-aaai/reports/P5_results_report.md
thesis-writing/paper-aaai/figures/figure2_estimator_agreement.pdf
thesis-writing/paper-aaai/figures/generate_figure2_estimator_agreement.py
```

If an existing canonical figure-generation location exists, use it instead of creating a duplicate path and record that decision.

Tracked LaTeX auxiliaries may be regenerated during compilation.

Do not clean or reorganize them.

---

# Protected paths

Treat as read-only:

```text
prompt.txt

thesis-writing/paper-aaai/clinicause_aaai27_paper_operational_plan.md
thesis-writing/paper-aaai/aaai_structure_notes.md
thesis-writing/paper-aaai/reports/P3_dataset_construction_validation_report.md
thesis-writing/paper-aaai/reports/P4_empirical_evaluation_report.md
thesis-writing/paper-aaai/AnonymousSubmission2027.tex
thesis-writing/paper-aaai/CameraReady2027.tex
thesis-writing/paper-aaai/ReproducibilityChecklist.tex
thesis-writing/paper-aaai/aaai2027.sty
thesis-writing/paper-aaai/aaai2027.bst

thesis-writing/thesis/
thesis-writing/results/
thesis-writing/reproducibility/
thesis-writing/literature/
thesis-writing/logs/
thesis-writing/planning/
thesis-writing/audit/

router.py
tests/
STraTS/
causal-irregular-time-series/
clinicause_debug_operational_plan.md
data/
logs/
processed/
runs/
```

Do not edit the bibliography.

---

# Forbidden actions

Do not:

* invent or estimate numbers;
* use this prompt as a numerical authority;
* select exposures based on favorable results;
* omit PhysioNet shock;
* hide estimator disagreements;
* reduce CausalPFN to a footnote;
* invent a CausalPFN citation;
* claim statistical significance;
* claim clinical-label validation;
* call model estimates treatment recommendations;
* write the Abstract;
* write the Introduction or Related Work;
* write Discussion or Conclusion;
* modify Sections 3 or 4 beyond a necessary transition;
* modify source data or checked results;
* access patient-level values;
* run experiments;
* run models;
* install packages;
* modify code;
* modify thesis prose;
* clean the repository;
* alter `.gitignore`;
* reset, restore, revert, stage, commit, or push.

---

# Build and validation

Build from:

```text
thesis-writing/paper-aaai/
```

Use:

```bash
env TEXINPUTS=AuthorKit27: \
    BSTINPUTS=AuthorKit27: \
    BIBINPUTS=../literature/metadata: \
    latexmk -pdf -interaction=nonstopmode -halt-on-error paper.tex
```

Run sufficient passes to resolve references and citations.

Then run:

```bash
git diff --check
git status --short
git diff --stat

git diff -- thesis-writing/paper-aaai/paper.tex
git diff -- thesis-writing/paper-aaai/paper_evidence_map.md
git diff -- thesis-writing/paper-aaai/paper_build_report.md
git diff -- thesis-writing/paper-aaai/reports/P5_results_report.md
```

Validate:

* the anonymous author block remains intact;
* the PDF remains US Letter;
* the PDF remains in official two-column AAAI format;
* no prohibited package was added;
* no undefined citation or reference remains;
* no duplicate labels remain;
* Table 2 fits and is readable;
* Figure 2 fits and is readable;
* all 19 comparisons are represented;
* the zero line and PhysioNet-shock exception are visible;
* no figure label is clipped;
* no text crosses margins or gutters;
* no Type 3 font appears;
* Section 5 remains within or close to its page budget;
* Results do not become a limitations section;
* all numerical claims match the audit;
* no unsupported causal or clinical claim appears.

Render and visually inspect every page of `paper.pdf`.

Inspect Figure 2 separately at its final embedded size.

---

# Completion standard

P5 passes only if:

* Section 5 is complete polished Results prose;
* Table 2 contains the checked predictive results;
* Figure 2 contains all 19×3 estimator summaries or an explicitly justified complete fallback;
* the 19/19 DML agreement is prominent;
* the 18/19 CausalPFN finding is prominent;
* PhysioNet shock is visible and accurately described;
* matching and downsampling results are included;
* cross-dataset findings are stated without pooling;
* all numbers are independently audited;
* the evidence map is current;
* the manuscript compiles;
* the PDF remains AAAI compliant;
* the durable P5 report exists;
* no commit or push was performed.
