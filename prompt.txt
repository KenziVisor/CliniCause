Stage: Narrow Scientific Repair After Thesis Compression

Model/reasoning level:
Use the strongest available reasoning mode. This is a narrow post-review repair of a substantially completed thesis-compression stage.

Repository:
KenziVisor/CliniCause

Current expected HEAD:
26b639cf3da6551e0e2aee388c8b2bfd7368d238
commit message: aaai authors kit

Thesis-compression baseline:
ddbf52b489bc7bffc0b17f39d54aca5d766fe8e3
commit message: thesis compression

Reviewed parent thesis state:
d77fada7b63c2b0b37746302fcda1c6351fa3e9b
commit message: SAnD removal

Do not commit or push.

======================================================================
1. CONTEXT AND SCOPE
======================================================================

The current HEAD contains one commit after the thesis-compression commit. That later commit adds the AAAI 2027 author kit under:

thesis-writing/paper-aaai/AuthorKit27/

It is unrelated to the thesis manuscript repair.

Continue from the current HEAD. Do not reset, detach, revert, rewrite, or remove the AAAI commit.

Treat:

- `26b639cf` as the repository baseline;
- `ddbf52b4` as the current thesis-manuscript baseline;
- `d77fada7` as the useful pre-compression reference for material that may need to be restored.

The AAAI author kit is protected and outside this task.

Do not:

- edit anything under `thesis-writing/paper-aaai/`;
- apply AAAI formatting requirements to the thesis;
- interpret AAAI prohibitions on `setspace`, margins, page size, fonts, or XeLaTeX as thesis requirements;
- include the AAAI templates in the thesis build;
- search the AAAI kit as if it were active thesis prose;
- modify the conference-paper work.

The active thesis remains:

thesis-writing/thesis/main.tex

with its chapters, front matter, appendix, bibliography, and generated `main.pdf`.

======================================================================
2. REVIEW DECISION
======================================================================

The thesis-compression commit is broadly successful.

Preserve its principal improvements:

- complete active SAnD clearance;
- four-model predictive scope:
  STraTS, GRU, GRU-D, and TCN;
- five-source aggregate:
  four predicted-label sources plus one rule-derived source;
- merged experimental and validation chapter;
- removal of duplicative contribution, audit, research-question, limitation, and status tables;
- preservation of all checked numerical Results;
- existing approximately 1.5-spaced body prose;
- improved total length and narrative flow.

Do not reopen the full compression stage.

A narrow repair is required in three areas:

1. restore sufficient operational detail for the proxy-state definitions;
2. restore a compact executed-configuration and provenance specification;
3. resolve the inactive Chapter 9 compatibility stub.

The author prioritizes scientific completeness over a particular page count.

The current 94-page PDF is not a target that must be preserved. It is acceptable and expected for the repair to add several pages. Do not cut other strong material merely to compensate for restored content.

A final length around 99–105 pages may arise naturally, but no page number is mandatory.

======================================================================
3. BASELINE VERIFICATION
======================================================================

Before editing, run and record:

git status --short
git branch --show-current
git log -10 --oneline
git show --stat --oneline 26b639cf3da6551e0e2aee388c8b2bfd7368d238
git diff --stat ddbf52b489bc7bffc0b17f39d54aca5d766fe8e3..26b639cf3da6551e0e2aee388c8b2bfd7368d238
git show --stat --oneline ddbf52b489bc7bffc0b17f39d54aca5d766fe8e3
git diff --stat d77fada7b63c2b0b37746302fcda1c6351fa3e9b..ddbf52b489bc7bffc0b17f39d54aca5d766fe8e3

Confirm that the commit after `ddbf52b4` changes only the AAAI author-kit area and does not change active thesis source.

Read all applicable `AGENTS.md` and thesis-local instructions.

Record:

- current HEAD;
- current thesis baseline;
- pre-existing dirty files;
- nested-repository state;
- current PDF page count;
- existing build command;
- existing body-spacing configuration;
- protected paths;
- exact files expected to change in this repair.

Do not reset, clean, normalize, stage, or overwrite unrelated work.

======================================================================
4. PROTECTED MATERIAL
======================================================================

Protect and verify unchanged:

- all checked numerical result CSVs;
- result manifests and checksums;
- result tables and their values;
- result figures and figure data;
- DAG artifacts;
- active research code;
- raw and processed data;
- literature PDFs;
- abstracts;
- title and administrative metadata;
- institutional forms;
- estimator hierarchy;
- model rankings;
- signs, counts, sample sizes, percentages, and effect estimates;
- citation keys except where an actual citation repair is required;
- the AAAI author-kit commit and all files under `thesis-writing/paper-aaai/`;
- all unrelated pre-existing dirty files.

The repair must not change:

- the four learned prediction models;
- the five-source aggregate lineage;
- the SAnD-clearance result;
- primary CausalForestDML status;
- secondary LinearDML status;
- exploratory CausalPFN status;
- original versus downsampled population hierarchy;
- any checked numerical result.

Create an appropriate protected-content snapshot before editing and compare it after editing.

======================================================================
5. TASK A — RESTORE OPERATIONAL PROXY DEFINITIONS
======================================================================

This is the highest-priority repair.

The compressed Chapter 5 currently names and broadly summarizes the proxy families, but it no longer gives enough operational information for a thesis reader to determine what each downstream prediction target and causal exposure represents.

The proxy definitions are scientifically central because they define:

- the supervised prediction targets;
- the predicted voter columns;
- the mixed-source aggregate;
- the proxy-state exposures used by matching and effect estimation;
- the interpretation of every proxy-specific result.

A reader must not be required to inspect Python source or a Git repository merely to understand the operational meaning of each `LAT_*` variable.

----------------------------------------------------------------------
5.1 Sources of authority
----------------------------------------------------------------------

Inspect and reconcile:

- the current compressed Chapter 5 at `ddbf52b4`;
- the pre-compression Chapter 5 at `d77fada7`;
- the active PhysioNet tagger;
- the active MIMIC tagger;
- the existing proxy-rule citation matrix;
- existing evidence reports describing rule behavior;
- current bibliography entries.

Use this hierarchy:

1. active tagger source defines implemented behavior;
2. validated source/schema evidence supports the reported implementation;
3. the pre-compression chapter provides an already reviewed presentation baseline;
4. prompt records explain design provenance but do not override active code;
5. clinical literature provides bounded conceptual grounding, not automatic validation.

Do not invent or simplify a threshold in a way that changes rule meaning.

----------------------------------------------------------------------
5.2 Required main-text content
----------------------------------------------------------------------

Restore two compact but operational tables in Chapter 5:

1. PhysioNet proxy-state definitions;
2. MIMIC-III proxy-state definitions.

Every active proxy state must have its own row.

PhysioNet must cover all eleven active states.

MIMIC-III must cover all ten active states.

Each row should communicate, as applicable:

- exact `LAT_*` identifier;
- construct represented;
- principal input measurements or helper flags;
- temporal or aggregation basis;
- main threshold or score logic;
- important sufficient conditions;
- important missingness or input-mode behavior;
- principal clinical/construct-validity boundary.

The tables need not reproduce every source-code branch verbatim, but they must be operationally informative.

For example, it is not sufficient to say only:

“Shock combines hemodynamic and perfusion evidence.”

The table should communicate the principal implemented evidence and decision form, such as the relevant pressure, lactate, heart-rate, urine, acid-base, vasopressor, or support conditions and whether the rule uses a score, conjunction, or sufficient critical marker.

Likewise, it is not sufficient to say only:

“Renal dysfunction combines renal measurements.”

The table should communicate the relevant creatinine, change-from-baseline, BUN, urine, electrolyte, dialysis, or sufficient-condition logic implemented for that dataset.

----------------------------------------------------------------------
5.3 Balance detail and readability
----------------------------------------------------------------------

Do not blindly restore the former tables unchanged.

The old tables may be used as source material, but edit them for thesis readability.

Preferred structure for each table:

- Proxy state;
- Operational evidence;
- Decision form;
- Principal boundary.

A fifth column may be used only when it materially improves clarity.

Keep globally shared behavior in surrounding prose instead of repeating it in every row, including:

- non-missing numeric comparisons;
- general interpretation of negative labels;
- broad score-versus-sufficient-condition pattern;
- active-code authority;
- lack of chart-adjudicated validation.

Retain row-specific details when they differ materially.

Use landscape `longtable` only if needed. Tables may use conventional compact spacing locally; do not force 1.5 spacing inside large tables.

Do not:

- use unreadably small text;
- shrink the page margins;
- abbreviate thresholds beyond recognition;
- remove clinically important conditions merely to keep a table on one page;
- restore verbose provenance columns;
- restore prompt-file paths in every row;
- turn the tables back into repository-audit matrices.

----------------------------------------------------------------------
5.4 Main-text independence and supplement boundary
----------------------------------------------------------------------

The main thesis should stand on its own scientifically.

The technical evidence package may retain:

- exact clause-by-clause source mappings;
- hashes;
- prompt-to-code lineage;
- full source paths;
- detailed citation-status matrices;
- superseded versions.

The main thesis must retain:

- enough operational information to understand each construct;
- the active number and identity of states;
- the major thresholds and score/sufficient-condition form;
- dataset-specific differences;
- missingness and input-mode implications;
- construct-validity limitations.

Keep a concise reference to the repository evidence package for exhaustive clauses, but do not use that reference as a substitute for the operational definitions.

----------------------------------------------------------------------
5.5 Preserve the successful compression
----------------------------------------------------------------------

Do not restore:

- the full prompt inventory table;
- the prompt-to-source traceability matrix;
- the proxy-source-type table if prose already explains it;
- repeated artifact-path inventories;
- repeated version-history narrative;
- repeated full disclaimers;
- inactive or superseded rules;
- SAnD content.

Preserve the compressed chapter’s clearer structure and the existing section on predicted and aggregated proxy labels.

======================================================================
6. TASK B — ADD A COMPACT EXECUTED-CONFIGURATION SPECIFICATION
======================================================================

The merged “Experimental and Validation Design” chapter is structurally sound, but it now needs a compact specification of the executed settings required to interpret or attempt reproduction of the reported experiments.

Do not restore the old audit-heavy Chapters 8 and 9.

Add one concise subsection, for example:

“Executed Settings and Provenance Status”

or:

“Confirmed Experimental Settings and Unresolved Lineage”

Place it where it best connects the experiment-family description to result admission.

----------------------------------------------------------------------
6.1 Required content
----------------------------------------------------------------------

Create one compact table with approximately these columns:

- Analysis component;
- Confirmed or execution-supported settings;
- Unresolved provenance or interpretation boundary.

Cover, where supported by current evidence:

Prediction:

- PhysioNet 2012 and MIMIC-III;
- STraTS, GRU, GRU-D, and TCN;
- multi-label prediction of rule-derived proxy labels;
- archived dataset–model combinations;
- prediction threshold of 0.5;
- exported probability and binary fields;
- exported split setting where verified;
- seed, run identifier, and training fraction where directly supported by archived logs or checked evidence;
- checkpoint-selection metric;
- STraTS pretraining distinction.

Causal analysis:

- two datasets;
- CausalForestDML, LinearDML, and CausalPFN;
- original and outcome-downsampled conditions;
- twelve archived run families;
- original cohorts as primary;
- outcome-downsampled populations as robustness analyses;
- five-source mixed aggregate;
- estimator hierarchy;
- exposure-specific analysis;
- matching and diagnostic roles.

Permutation and sensitivity:

- ten permutation trials where verified;
- seed 42 where verified;
- treatment-versus-outcome permutation distinction;
- CausalPFN diagnostic limitations;
- sensitivity evidence-status distinctions.

Provenance gaps:

- missing processed-pickle hashes;
- missing split manifests or identifiers;
- incomplete checkpoint-to-export mapping;
- missing numbered final causal configurations;
- incomplete producing commands or archive-copy records;
- incomplete source/environment lineage.

Only include a setting as “confirmed” when current checked evidence supports it.

Classify uncertain settings explicitly as:

- source-defined;
- log-supported;
- artifact-supported;
- unresolved;
- not applicable.

Do not infer historical execution merely from current defaults.

----------------------------------------------------------------------
6.2 Keep it compact
----------------------------------------------------------------------

The subsection should add approximately one or two pages, not recreate the previous four experiment/reproducibility tables.

Do not restore:

- full environment-field inventories;
- hardware wish lists;
- ten-item admission checklists;
- repeated source-status columns;
- full artifact path matrices;
- operational configuration precedence unless needed to interpret a reported setting.

The purpose is to make the experiment sufficiently specified, not to reproduce the audit package.

======================================================================
7. TASK C — RESOLVE THE INACTIVE CHAPTER 9 STUB
======================================================================

The file:

thesis-writing/thesis/chapters/09_experimental_design.tex

currently contains only an inactive compatibility comment and is no longer included by `main.tex`.

Search the repository for:

- direct references to this path;
- scripts that expect it;
- validation tools that inspect it;
- planning files that rely on its existence;
- local instructions requiring chapter placeholders.

If no active dependency requires the path:

- delete the file.

If an active dependency requires it:

- retain it;
- expand the comment to identify the exact dependency;
- state that active content is in `08_robustness_sensitivity_validation.tex`;
- ensure it cannot accidentally define duplicate labels or be included in the build.

Do not re-enable the old chapter.

Do not split the merged chapter again.

======================================================================
8. LINE SPACING AND TYPOGRAPHY
======================================================================

The thesis already uses:

\usepackage{setspace}
\onehalfspacing

This provides approximately 1.5 spacing for ordinary body prose.

Preserve this configuration unchanged unless direct rendered evidence shows it is not functioning.

Do not:

- add another spacing package or command;
- stack `\setstretch{1.5}` on top of `\onehalfspacing`;
- tighten line spacing to offset the restored pages;
- reduce margins;
- reduce body font size;
- shrink figures;
- globally change table row spacing;
- apply AAAI formatting rules to the thesis.

Use context-appropriate compact spacing for:

- long tables;
- captions;
- bibliography;
- contents and lists;
- equations;
- title material;
- footnotes;
- dense technical entries.

The page increase must reflect restored scientific content, not typographic manipulation.

======================================================================
9. ALLOWED EDITS
======================================================================

Expected thesis edits are limited primarily to:

- `thesis-writing/thesis/chapters/05_proxy_state_construction.tex`;
- `thesis-writing/thesis/chapters/08_robustness_sensitivity_validation.tex`;
- `thesis-writing/thesis/chapters/09_experimental_design.tex`, only for deletion or clarified compatibility handling;
- `thesis-writing/thesis/main.pdf`;
- LaTeX auxiliary/index effects generated by a clean build;
- one narrow repair evidence report in the established logs directory.

Edit `main.tex` only if required by deletion or integration, and do not change its spacing, margins, fonts, language setup, front matter, or chapter order.

Do not create multiple overlapping reports.

Do not modify `prompt.txt` except when the local workflow explicitly uses it as Codex’s current instruction file.

======================================================================
10. FORBIDDEN EDITS
======================================================================

Do not modify:

- `thesis-writing/paper-aaai/`;
- active research code;
- model outputs;
- causal outputs;
- checked result CSVs;
- result manifests;
- raw or processed data;
- figures or figure source values;
- DAG definitions or artifacts;
- abstracts;
- title pages;
- institutional metadata;
- bibliography entries unless a directly necessary citation repair is identified;
- unrelated planning or audit history;
- unrelated dirty files.

Do not run:

- preprocessing;
- model training;
- prediction;
- matching;
- causal estimation;
- sensitivity regeneration;
- permutation regeneration;
- new experiments.

Static inspection and thesis build/validation are authorized.

======================================================================
11. REQUIRED VALIDATION
======================================================================

11.1 Scientific validation

Verify that Chapter 5 contains:

- all eleven PhysioNet proxy states;
- all ten MIMIC proxy states;
- no extra or obsolete states;
- exact active identifiers;
- operationally meaningful evidence and decision logic;
- important sufficient conditions;
- dataset-specific distinctions;
- correct missingness/input-mode qualifications;
- no statement upgrading a proxy to a diagnosis;
- no SAnD content.

Compare the restored descriptions against active tagger source.

Create a row-level validation ledger:

- proxy identifier;
- source function or rule;
- thesis row;
- main thresholds/conditions checked;
- status;
- discrepancy, if any.

Any unresolved rule discrepancy is blocking.

11.2 Experimental-setting validation

For every setting placed in the compact configuration table, record its evidence class and source.

Verify that:

- a current default is not presented as a historical producing value;
- an archived path is not presented as a portable command;
- requirements are not presented as producing-environment proof;
- original and downsampled populations remain distinct;
- estimator hierarchy remains correct;
- the permutation count and seed are reported only where verified;
- prediction threshold and export schema remain correct;
- the five-source aggregate remains exact.

11.3 Protected-content comparison

Confirm:

- zero checked-result value changes;
- zero result-figure changes;
- zero abstract changes;
- zero title/admin changes;
- zero research-code changes;
- zero AAAI author-kit changes;
- zero SAnD reintroduction;
- zero changes to the four-model scope;
- zero changes to estimator hierarchy.

Explain all citation, label, reference, and numeric-token changes.

11.4 LaTeX build

Run a clean build using the existing documented process:

latexmk -C
latexmk -xelatex main.tex

from the thesis directory.

Check:

- build exit status;
- XeLaTeX and Biber completion;
- undefined citations;
- undefined references;
- multiply defined labels;
- missing files;
- missing characters;
- table overflow;
- overfull/underfull warnings;
- bibliography errors;
- PDF syntax.

Run:

qpdf --check main.pdf

or the established equivalent.

11.5 PDF and visual inspection

Record:

- starting page count;
- final page count;
- pages added;
- where the added pages occur.

Render and inspect the complete PDF because pagination will change.

At minimum, inspect individually at adequate resolution:

- contents;
- List of Tables;
- all Chapter 5 pages;
- both restored proxy-definition tables;
- Chapter 8;
- the compact configuration table;
- chapter transitions;
- Results opening;
- bibliography;
- final covers.

Check:

- table readability;
- repeated longtable headers;
- clipping;
- overlap;
- broken identifiers;
- detached headings;
- excessive whitespace;
- blank or nearly blank pages;
- RTL integrity;
- line-spacing consistency;
- correct table numbering;
- correct page references.

The restored tables may be compactly spaced, but ordinary English and Hebrew prose must remain approximately 1.5-spaced.

======================================================================
12. REQUIRED DELIVERABLES
======================================================================

Produce:

1. revised Chapter 5;
2. revised merged experimental/validation chapter;
3. resolved Chapter 9 stub;
4. rebuilt `main.pdf`;
5. one narrow evidence report, for example:

thesis-writing/logs/thesis_compression_scientific_repair_report.md

Do not create redundant Markdown or CSV files when the evidence can be included in the single report.

The report should contain:

# Thesis Compression Scientific Repair

## Baseline
- current HEAD;
- thesis baseline commit;
- unrelated AAAI commit classification;
- worktree state;
- protected scope;
- initial PDF page count;
- spacing status.

## Review findings addressed
- insufficient proxy operational detail;
- insufficient compact executed-setting specification;
- inactive Chapter 9 stub.

## Proxy-definition repair
- source hierarchy;
- table design;
- all 21 proxy rows;
- row-level source validation;
- important details restored;
- audit detail deliberately not restored.

## Experimental-setting repair
- confirmed settings;
- evidence classes;
- unresolved provenance;
- material deliberately kept in external evidence package.

## Stub resolution
- dependency search;
- deletion or retention decision;
- justification.

## Scientific preservation
- results unchanged;
- figures unchanged;
- abstracts unchanged;
- model and estimator scope unchanged;
- SAnD remains absent;
- five-source lineage unchanged;
- 1.5 body spacing preserved.

## Validation
- clean build;
- references and citations;
- PDF check;
- page count;
- visual review;
- protected comparison.

## Length outcome
- starting pages;
- final pages;
- pages restored;
- why the added material is scientifically necessary;
- confirmation that no compensating cuts or spacing manipulation were used.

## Unresolved issues

## Readiness decision

Use exactly one:

READY FOR AUTHOR REVIEW

READY FOR AUTHOR REVIEW WITH NON-BLOCKING PRESENTATION NOTES

BLOCKED FOR PROXY-DEFINITION ACCURACY — <reason>

BLOCKED FOR DOCUMENT INTEGRITY — <reason>

BLOCKED FOR SCIENTIFIC CONSISTENCY — <reason>

======================================================================
13. FINAL SUCCESS CRITERIA
======================================================================

The repair is successful when:

- the thesis remains concise and coherent;
- the successful compression is preserved;
- each proxy exposure is operationally understandable from the thesis itself;
- Chapter 5 no longer depends on source-code access for core construct meaning;
- the experimental chapter contains a compact but adequate specification of confirmed settings and unresolved provenance;
- the inactive Chapter 9 file is cleanly resolved;
- all checked Results remain unchanged;
- active SAnD clearance remains complete;
- ordinary body prose remains approximately 1.5-spaced;
- no AAAI author-kit file or rule affects the thesis;
- the PDF builds cleanly and is visually professional;
- the final page count is determined by scientific quality rather than an arbitrary target.

Do not commit or push.