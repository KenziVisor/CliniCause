# Stage 4.3R — Repair LLM, Prompt-Engineering, and Method-Provenance Coverage

## Authoritative Task Declaration

Implement this prompt as a corrective task after Stage 4.3 and before Stage 4.4.

The active task is:

```text
Stage 4.3R — LLM and Prompt-Provenance Repair
```

This is not Stage 4.4.

Do not draft the full causal-methodology chapter yet.

The purpose of this repair is to incorporate newly committed evidence under:

```text
thesis-writing/prompts-and-documents/
```

These materials were not inspected during Stage 2, Stage 3, or Stage 4.3 and materially change the documented provenance of:

* proxy-state ontology design;
* rule elicitation;
* causal-DAG design;
* clinical and causal reasoning;
* prompt engineering;
* research-document generation.

The corrected thesis must distinguish:

```text
LLM-assisted design proposal
human or project review
source-code implementation
executed artifact
clinical validation
```

These are separate evidence levels.

---

# 1. Context

The latest approved repository commit is expected to be:

```text
0009d91 step 4.3 + prompts
```

This commit contains:

* the completed Stage 4.3 Chapter 5 draft;
* the Stage 4.3 evidence report;
* updated placeholder and deferred-fix logs;
* the generated thesis PDF;
* a newly committed `thesis-writing/prompts-and-documents/` directory.

The Stage 4.3 evidence report states that this directory was outside Stage 4.3 scope. Therefore, Chapter 5 and the existing planning documents do not yet incorporate its methodological implications.

The exact final prompt template appears to include:

```text
general-purpose-latent-causal-model-prompt.txt
physionet-prompt.txt
mimic-prompt.txt
```

Historical variants include:

```text
old-physionet-prompt.txt
old-mimic-prompt.txt
```

The directory also includes generated reports, manager summaries, research-question documents, and validation/reference documents in PDF or DOCX form.

Do not infer the relationship among these files from their filenames alone. Audit the relationships directly.

---

# 2. Core Interpretation

The LLM is not an inference-time component of the executable CliniCause pipeline.

It does not:

* process ICU records during normal pipeline execution;
* generate STraTS predictions during execution;
* estimate matching or CATE outputs during execution;
* replace the active tagger scripts;
* replace the active DAG scripts;
* provide chart-adjudicated clinical labels.

However, the newly committed prompt evidence indicates that an LLM was used as a **method-design and expert-elicitation instrument** to propose or refine:

* candidate latent clinical states;
* observed-to-latent mappings;
* proxy-state decision rules;
* clinical threshold families;
* missingness and measurement-process interpretation;
* candidate causal edges;
* mortality pathways;
* adjustment implications;
* rejected alternatives;
* implementation specifications.

This use is methodologically relevant and must be documented.

Use conservative terms such as:

```text
LLM-assisted expert elicitation
LLM-assisted evidence synthesis
LLM-generated candidate ontology
LLM-proposed rule specification
LLM-proposed causal structure
project-reviewed design artifact
source-code operationalization
```

Do not use:

```text
AI-discovered clinical truth
validated LLM diagnosis
LLM-proven causal graph
automatically learned DAG
clinical expert consensus
ground-truth latent state
```

unless discussing why such wording is inappropriate.

---

# 3. Mandatory Initial Inspection

## 3.1 Git State

From the repository root, run:

```bash
git status --short
git branch --show-current
git log -6 --oneline
git show --stat --oneline 0009d91
```

Verify:

* commit `0009d91` exists;
* its message is `step 4.3 + prompts`;
* it follows `c9f6b12 step 4.2`;
* Chapter 5 and the Stage 4.3 evidence report are present;
* `thesis-writing/prompts-and-documents/` is tracked in this commit.

Record the initial and final working-tree states.

Do not:

* reset;
* clean;
* checkout another branch;
* stage;
* commit;
* push;
* discard unrelated changes.

## 3.2 Applicable Instructions

Inspect all relevant `AGENTS.md` files.

Also inspect:

```text
final-results/AGENTS.md
thesis-writing/thesis/README.md
```

Do not let stale instructions override current source evidence.

## 3.3 Baseline Thesis Build

Run:

```bash
cd thesis-writing/thesis
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

Record:

* return status;
* page count;
* citation and reference status;
* duplicate-label status;
* fatal errors;
* existing nonfatal warnings.

---

# 4. Complete Prompt-and-Document Inventory

Inspect every file under:

```text
thesis-writing/prompts-and-documents/
```

Create an inventory containing:

```text
path
file type
size
SHA-256
apparent role
dataset
prompt/output/internal-document classification
version status
relationship to another file
author or generator metadata, when available
creation/modification metadata, when available
text extraction status
provenance confidence
```

Use commands such as:

```bash
find thesis-writing/prompts-and-documents -maxdepth 1 -type f -printf '%f\t%s bytes\n' | sort
sha256sum thesis-writing/prompts-and-documents/*
file thesis-writing/prompts-and-documents/*
```

Do not edit the prompt or document artifacts.

---

# 5. PDF and DOCX Inspection

## 5.1 PDF Files

For each PDF, run:

```bash
pdfinfo "<file>"
pdftotext -layout "<file>" -
```

Save temporary extracted text outside tracked thesis directories when useful.

Determine:

* title;
* page count;
* whether it appears to be an LLM output;
* whether it contains a model/product identifier;
* whether it contains citations;
* whether it contains prompt text or only generated results;
* whether it is duplicated by a DOCX;
* whether it is an old or final report;
* whether it directly informed source code.

Do not treat PDF formatting as scientific validation.

## 5.2 DOCX Files

Inspect DOCX contents without modifying them.

Use an appropriate method such as:

```bash
python - <<'PY'
from pathlib import Path
from docx import Document

for path in sorted(Path("thesis-writing/prompts-and-documents").glob("*.docx")):
    doc = Document(path)
    print(f"\n===== {path} =====")
    for paragraph in doc.paragraphs:
        text = paragraph.text.strip()
        if text:
            print(text)
PY
```

Also inspect document metadata when available.

Determine whether PDF/DOCX pairs contain equivalent content.

When they differ, identify which appears newer and do not silently choose one as authoritative.

---

# 6. Prompt-Version Audit

Compare:

```text
general-purpose-latent-causal-model-prompt.txt
physionet-prompt.txt
mimic-prompt.txt
old-physionet-prompt.txt
old-mimic-prompt.txt
```

Establish:

1. Whether the final dataset-specific prompts are derived from the general template.
2. Which global substitutions were made.
3. Whether the final PhysioNet and MIMIC prompts differ only in dataset variables or have substantive differences.
4. How each final prompt differs from its `old-*` version.
5. Which version was actually used to produce each generated report.
6. Whether follow-up prompts or manual edits occurred.
7. Whether the exact prompt text is sufficient to reproduce the documented output.

Generate unified diffs for audit purposes:

```bash
diff -u \
  thesis-writing/prompts-and-documents/old-physionet-prompt.txt \
  thesis-writing/prompts-and-documents/physionet-prompt.txt || true

diff -u \
  thesis-writing/prompts-and-documents/old-mimic-prompt.txt \
  thesis-writing/prompts-and-documents/mimic-prompt.txt || true
```

Do not paste the full 700-plus-line prompt into the main thesis chapter.

Instead, document its structure and preserve the exact files as versioned supplementary artifacts.

---

# 7. Extract the Actual Prompt Protocol

From the final prompt template, document the verified protocol.

At minimum identify:

* assigned expert role;
* declared research mission;
* dataset variables and official dataset-documentation link;
* dataset audit;
* deep-research requirements;
* candidate latent-state discovery;
* observed-to-latent measurement model;
* mortality mechanism;
* missingness and irregular-sampling analysis;
* latent-state selection;
* rule-based binary proxy construction;
* leakage restrictions;
* causal-DAG construction;
* confounder, mediator, and collider analysis;
* iterative validation;
* adversarial critique;
* required report structure;
* implementation specification.

Also record risky wording inside the prompt, including language such as:

```text
true hidden clinical states
most clinically plausible DAG
causal correctness
clinically justified latent DAG
```

Explain that these are **prompt objectives**, not evidence that the returned constructs are true, identified, clinically validated, or causally correct.

---

# 8. Determine the Actual LLM Execution Provenance

Search all new documents and repository history for:

```text
model provider
product or interface
model name
model version
run date
conversation date
deep-research mode
browsing mode
temperature
reasoning setting
system prompt
follow-up prompts
human edits
reviewer names or roles
clinical reviewer
output export method
```

Do not infer these values.

When undocumented, create precise placeholders:

```text
[VALIDATION REQUIRED: identify the LLM provider, product, and exact model/version used for the final PhysioNet prompt run]
```

```text
[VALIDATION REQUIRED: identify the LLM provider, product, and exact model/version used for the final MIMIC-III prompt run]
```

```text
[VALIDATION REQUIRED: record run dates, research/browsing mode, available generation settings, follow-up prompts, and export procedure]
```

```text
[SUPERVISOR DECISION REQUIRED: document the human and clinical review applied to LLM-proposed ontology, rules, and DAG edges]
```

A prompt file alone does not prove which model executed it.

A generated PDF alone does not prove that all recommendations were accepted into the implementation.

---

# 9. Prompt-Output-to-Code Traceability

Create a traceability matrix mapping:

```text
prompt requirement
generated-report recommendation
implemented source location
implemented proxy/DAG element
agreement status
difference
human-review evidence
artifact evidence
confidence
```

Cover at minimum:

* PhysioNet latent-state set;
* MIMIC latent-state set;
* PhysioNet rule families;
* MIMIC rule families;
* PhysioNet DAG nodes and edges;
* MIMIC DAG nodes and edges;
* measurement-process or missingness nodes;
* mortality pathways;
* confounder/mediator/collider recommendations;
* rejected prompt recommendations;
* recommendations that were never implemented;
* implementation elements absent from the LLM reports.

Use statuses:

```text
DIRECTLY ALIGNED
PARTIALLY ALIGNED
IMPLEMENTATION MODIFIED
PROMPT RECOMMENDATION NOT IMPLEMENTED
IMPLEMENTATION NOT FOUND IN PROMPT OUTPUT
RELATIONSHIP UNCLEAR
```

Do not assume that textual similarity proves direct provenance.

The active source code remains authoritative for what the executable pipeline implements.

The LLM output remains authoritative only for documenting the design proposal it contains.

---

# 10. Correct the Literature Scope

The current literature README and citation plan state that LLM literature is out of scope.

That statement is now incomplete.

Update:

```text
thesis-writing/literature/README.md
thesis-writing/planning/citation_plan.md
```

The corrected distinction should be:

```text
No LLM is used as an execution-time predictive or causal estimator in the CliniCause pipeline. However, an LLM-assisted research and expert-elicitation process was used during proxy-ontology, rule, and DAG design. Literature concerning LLM use as a domain expert, clinical reasoning aid, prompt engineering, reliability, hallucination, and human oversight is therefore relevant to the methodology and limitations discussion.
```

Do not automatically add new BibTeX records from generated reports.

First inspect whether the current corpus already contains suitable sources.

When it does not, create:

```text
thesis-writing/literature/llm_methodology_candidate_additions.md
```

The candidate-additions file should specify the literature roles needed:

* LLMs as medical or domain-expert assistants;
* evaluation of LLM medical reasoning reliability;
* prompt-engineering methodology;
* reproducibility and reporting of LLM-assisted research;
* human oversight and hallucination risk;
* LLM-assisted causal or ontology construction, when supported by appropriate literature.

Add explicit citation gaps to the citation plan, for example:

```text
CIT-GAP-004 — Evidence for using LLMs as domain-expert or evidence-synthesis assistants in medicine.
CIT-GAP-005 — Prompt-engineering and reproducibility guidance for the exact multi-phase elicitation protocol.
CIT-GAP-006 — Limitations, hallucination, bias, and human-validation requirements for LLM-generated clinical and causal structures.
```

Do not invent citation keys.

Do not add a paper merely because it appears in an LLM-generated bibliography.

---

# 11. Correct the Stage 2 and Stage 3 Evidence Base

## 11.1 New Audit File

Create:

```text
thesis-writing/audit/llm_prompt_provenance_audit.md
```

It must contain:

1. Artifact inventory.
2. Prompt version comparison.
3. Prompt execution metadata.
4. Output-document relationships.
5. Prompt-output-to-code traceability.
6. Scientific role.
7. Clinical and causal limitations.
8. Missing provenance.
9. Citation needs.
10. Recommended thesis wording.

## 11.2 Existing Audit Files

Update only where necessary:

```text
thesis-writing/audit/repository_map.md
thesis-writing/audit/evidence_inventory.md
thesis-writing/audit/terminology_map.md
thesis-writing/audit/unresolved_questions.md
thesis-writing/audit/claim_evidence_ledger.csv
```

Add a distinct evidence class for:

```text
LLM-DESIGN-ARTIFACT
```

Do not classify an LLM report as:

```text
clinical validation
execution evidence
ground truth
result-confirmed
causal identification
```

Recommended wording-strength categories include:

```text
LLM-PROPOSED
PROJECT-REVIEWED
IMPLEMENTATION-CONFIRMED
CLINICAL-REVIEW-INCOMPLETE
PROVENANCE-INCOMPLETE
```

---

# 12. Correct the Thesis Story and Planning Files

Update:

```text
thesis-writing/planning/thesis_story.md
thesis-writing/planning/thesis_outline.md
thesis-writing/planning/chapter_evidence_map.md
thesis-writing/planning/terminology_and_notation.md
thesis-writing/planning/citation_plan.md
thesis-writing/planning/table_plan.md
thesis-writing/planning/figure_plan.md
thesis-writing/planning/writing_order.md
thesis-writing/planning/stage4_prompt_queue.md
```

## 12.1 Thesis Story

Replace unqualified claims such as:

```text
clinician-authored DAG
```

with wording supported by the new evidence, such as:

```text
project-specified, LLM-assisted DAG
```

or:

```text
dataset-specific project DAG developed through LLM-assisted evidence synthesis and subsequently encoded in source code
```

Do not claim clinical authorship or approval unless a named clinical-review process is documented.

The main thesis story should now include:

* LLM-assisted ontology and DAG elicitation as a methodological component;
* source-code implementation as the operational layer;
* evidence tracking and human review as safeguards;
* explicit separation from automated causal discovery and clinical validation.

## 12.2 Research Questions

Consider adding or refining a secondary research question such as:

```text
How can a structured LLM-assisted elicitation protocol be used to propose clinically interpretable proxy states and causal structures, and how do those proposals differ from the final source-code implementation?
```

Do not make this a research question when the available evidence cannot answer it. In that case, classify it as a methodology or reproducibility question.

## 12.3 Chapter Allocation

Plan LLM-related material across:

### Chapter 5

* prompt-assisted proxy-state ontology and rule elicitation;
* distinction between LLM proposal and implemented rule;
* prompt-output-to-code traceability;
* clinical-validation limitations.

### Chapter 7

* prompt-assisted causal ontology and DAG elicitation;
* distinction from causal discovery or graph learning;
* final source-code graph as the implemented object;
* human/clinical review status;
* prompt wording risks and causal assumptions.

### Chapter 9

* exact prompt version;
* prompt hashes;
* model/provider/version;
* run date and settings;
* report files;
* prompt-version history;
* human review;
* reproducibility protocol.

### Chapter 11

* hallucination risk;
* anchoring and confirmation bias;
* opaque model updates;
* non-determinism;
* clinical expertise limitations;
* source-selection bias;
* causal-direction errors;
* need for independent review.

### Appendix

* exact final generic prompt;
* exact final PhysioNet prompt;
* exact final MIMIC prompt;
* prompt hashes;
* old-versus-final change summary;
* report and manager-summary inventory.

Do not insert the entire prompts into the main methods narrative.

---

# 13. Add Figure and Table Plans

Add a planned non-result methods figure:

```text
F-LLM-ELICITATION-01
```

Suggested purpose:

```text
Show the traceable workflow from general prompt template, through dataset-specific prompt execution and generated candidate report, to human/project review, source-code implementation, artifact validation, and thesis claims.
```

The figure must make clear that:

```text
LLM output ≠ implemented code ≠ clinical validation
```

Do not generate the figure during this repair unless the existing planning policy clearly permits it.

Add planned tables:

```text
T-LLM-PROVENANCE-01
    Prompt, model, run, output, hash, and review provenance.

T-LLM-TRACEABILITY-01
    Mapping from LLM-proposed ontology/rules/DAG elements to active source implementation.

T-PROMPT-VERSION-01
    Old versus final prompt differences and methodological implications.
```

Do not insert invented model metadata.

---

# 14. Repair Chapter 3

Edit narrowly:

```text
thesis-writing/thesis/chapters/03_problem_definition_study_design.tex
```

Do not rewrite the chapter.

Add a concise study-design disclosure explaining:

* candidate proxy-state definitions and DAG structure were informed by a structured LLM-assisted elicitation process;
* the LLM did not learn labels or a graph from patient-level data;
* the active tagger and DAG source files define the implemented objects;
* LLM-generated recommendations are design evidence, not validation;
* final causal interpretation still depends on assumptions and review.

Revise this type of wording:

```text
project-authored DAG
```

to reflect LLM assistance without overstating provenance.

A suitable form is:

```text
project-specified DAG developed through an LLM-assisted clinical and causal elicitation protocol and subsequently encoded in source code
```

Use a more conservative alternative when human-review evidence is incomplete.

Do not add a literature citation until an approved key exists. Use a precise citation placeholder when necessary.

---

# 15. Repair Chapter 5

Edit:

```text
thesis-writing/thesis/chapters/05_proxy_state_construction.tex
```

Do not discard the verified rule tables already drafted.

## 15.1 Add an LLM-Assisted Elicitation Subsection

Within C5.1, add an appropriately titled subsection such as:

```text
LLM-Assisted Ontology and Rule Elicitation
```

Explain:

1. A reusable general-purpose prompt template was prepared.
2. Dataset-specific prompts configured the dataset name and official dataset-description source.
3. The prompt required a staged process involving dataset audit, literature-guided clinical reasoning, candidate-state selection, rule design, DAG construction, missingness analysis, and adversarial critique.
4. Generated reports were treated as candidate methodological design documents.
5. Proposed states and rules were subsequently represented in repository source code.
6. The active source code, not the generated prose, defines the implemented rules.
7. No claim of chart-adjudicated validation follows from LLM generation.
8. Mismatches between prompt outputs and code must be documented.
9. Model/provider/version and human-review provenance remain explicit limitations when not recoverable.

## 15.2 Add a Prompt-Artifact Table

Create a compact table such as:

```latex
\label{tab:llm-prompt-artifact-provenance}
```

Suggested columns:

```text
Artifact
Dataset
Role
Version status
Relation to implementation
Provenance status
```

Do not include unsupported metadata.

## 15.3 Correct Causal and Clinical Language

The exact prompt includes aspirational language concerning:

* “true hidden clinical states”;
* the “most clinically plausible” graph;
* “causal correctness.”

The chapter must explain that these phrases directed the LLM’s search objective but do not establish:

* latent-state identifiability;
* clinical validity;
* graph correctness;
* causal identification;
* clinician agreement.

## 15.4 Preserve Existing Rule Evidence

Keep the current source-code-derived rule tables unless the new traceability audit finds a concrete discrepancy.

When a prompt recommendation and implemented rule differ:

* describe the implemented rule in Chapter 5;
* document the LLM recommendation in the evidence report or appendix plan;
* do not merge the two;
* record the discrepancy.

---

# 16. Do Not Repair Chapter 7 Yet

Do not draft:

```text
thesis-writing/thesis/chapters/07_causal_methodology.tex
```

during this repair.

Instead, update its Stage 4.4 planning packet so the next prompt requires inspection of:

```text
general-purpose-latent-causal-model-prompt.txt
physionet-prompt.txt
mimic-prompt.txt
the corresponding generated reports
the active DAG source files
the generated DAG artifacts
the LLM prompt-provenance audit
```

Stage 4.4 must distinguish:

```text
LLM-proposed DAG
project-reviewed DAG
source-code DAG
rendered run artifact
clinically approved DAG
```

Do not assume those objects are identical.

---

# 17. Placeholder Updates

Update:

```text
thesis-writing/logs/unresolved_placeholders.md
```

Add all unresolved LLM-provenance fields, including:

```text
[VALIDATION REQUIRED: identify the exact LLM provider, product, and model/version used for the final PhysioNet prompt run]
```

```text
[VALIDATION REQUIRED: identify the exact LLM provider, product, and model/version used for the final MIMIC-III prompt run]
```

```text
[VALIDATION REQUIRED: record the execution dates, research/browsing mode, settings, follow-up prompts, and output-export procedure]
```

```text
[VALIDATION REQUIRED: map every implemented proxy state and DAG component to the corresponding generated-report recommendation or mark it as independently added]
```

```text
[SUPERVISOR DECISION REQUIRED: document the human and clinical review applied to the LLM-generated design proposals]
```

```text
[CITATION REQUIRED: LLM-assisted domain-expert elicitation and prompt-engineering methodology]
```

```text
[CITATION REQUIRED: limitations and validation requirements for LLM-generated medical and causal structures]
```

Every entry must include:

* file;
* section;
* reason;
* evidence needed;
* expected resolution stage;
* status.

---

# 18. Deferred-Fix Updates

Append a new section to:

```text
thesis-writing/logs/deferred_fixes.md
```

Use IDs:

```text
DF-4.3R-001
DF-4.3R-002
...
```

At minimum assess:

### LLM execution provenance

Model/provider/version, dates, modes, settings, and follow-up prompts may be missing.

### Human-review provenance

The identity and qualifications of reviewers may be undocumented.

### Prompt-output-to-code provenance

The mapping between generated recommendations and active source code may be incomplete.

### Clinical-validation status

LLM generation must not be treated as clinical validation.

### Causal-graph authorship wording

“Clinician-authored” may be unsupported and must be corrected unless evidence exists.

### Prompt-version provenance

The producing old/final prompt version for each report may be unclear.

### Literature coverage

The corpus lacks an approved LLM methodology and reliability evidence packet.

### Reproducibility

The exact prompts are available, but model behavior may not be reproducible without provider/version/date/settings.

For every item include:

```text
ID
area
path
issue
evidence
impact
recommended later action
urgency
```

---

# 19. Repair Evidence Report

Create:

```text
thesis-writing/logs/stage_4_3_llm_prompt_repair_report.md
```

It must include:

## 19.1 Git State

* verified commit;
* initial status;
* final status;
* files changed.

## 19.2 Artifact Inventory

All files in `prompts-and-documents/`, with classifications and hashes.

## 19.3 Prompt Architecture

The exact phases and expected outputs of the final template.

## 19.4 Version Comparison

* general versus dataset-specific;
* old versus final;
* producing-version conclusions.

## 19.5 LLM Execution Metadata

Known, unknown, and conflicting metadata.

## 19.6 Output-Document Relationships

Which prompt appears related to which report or manager summary.

## 19.7 Prompt-to-Code Traceability

Summary of aligned, modified, rejected, missing, and unclear elements.

## 19.8 Scientific Interpretation

What the LLM contributed and what it did not contribute.

## 19.9 Planning Changes

Every modified audit, literature, and planning file.

## 19.10 Thesis Changes

Exact Chapter 3 and Chapter 5 additions or corrections.

## 19.11 Citation Gaps

Existing sources and new candidate needs.

## 19.12 Placeholders

All new LLM-related placeholders.

## 19.13 Deferred Fixes

All new `DF-4.3R-*` entries.

## 19.14 Build Validation

Final clean-build result and PDF metadata.

## 19.15 Readiness

Use one:

```text
READY FOR STAGE 4.4
READY WITH NON-BLOCKING LLM-PROVENANCE WARNINGS
BLOCKED BEFORE STAGE 4.4
```

Missing model/version metadata need not block methodological drafting when the limitation is visible, but it must block claims of full prompt reproducibility.

---

# 20. Validation Requirements

## 20.1 Search for Incorrect Authorship Claims

Search the thesis, audit, and planning files for:

```text
clinician-authored
clinically validated
expert-validated
ground-truth DAG
true latent state
causal discovery
learned DAG
LLM-discovered
AI-validated
```

Correct or qualify every unsupported occurrence.

## 20.2 Prompt-versus-Implementation Validation

Confirm that:

* the prompt is described as a design protocol;
* generated reports are described as candidate proposals;
* source code defines implemented rules and graphs;
* artifact files support execution or schema only according to their provenance;
* LLM output is not called clinical evidence;
* prompt citations are not automatically added to the thesis bibliography;
* old and final prompts are not conflated.

## 20.3 Chapter Validation

Confirm that:

* Chapter 3 receives only a concise provenance disclosure;
* Chapter 5 retains all verified rule details;
* Chapter 5 gains an explicit LLM-assisted method description;
* Chapter 6 is unchanged;
* Chapter 7 remains undrafted;
* no numerical result is added.

## 20.4 Citation Validation

Check all existing citation keys.

Do not introduce an unresolved key into LaTeX.

Use visible placeholders until an approved LLM-methodology citation is added to the canonical bibliography.

## 20.5 LaTeX Validation

Run:

```bash
cd thesis-writing/thesis
latexmk -C
latexmk -xelatex main.tex
test -f main.pdf
pdfinfo main.pdf
```

Scan for:

```text
LaTeX Error
Undefined control sequence
Citation ... undefined
Reference ... undefined
There were undefined references
multiply defined
Biber error
Emergency stop
Fatal error
```

Record nonfatal layout warnings separately.

## 20.6 Scope Validation

Confirm:

* no prompt artifact was edited;
* no PDF or DOCX was modified;
* no research code was modified;
* no result artifact was modified;
* no experiment was run;
* no other substantive thesis chapter was drafted;
* no commit or push occurred.

---

# 21. Completion Criteria

This repair is complete only when:

* the prompt-and-document directory has a complete inventory;
* old and final prompts have been compared;
* the relationship between generic and dataset-specific prompts is documented;
* LLM execution metadata is recorded or visibly missing;
* the prompt-output-to-code relationship is audited;
* literature scope no longer incorrectly excludes LLM-assisted methodology;
* new LLM citation gaps are visible;
* “clinician-authored DAG” language is corrected unless supported;
* Chapter 3 discloses the LLM-assisted design process;
* Chapter 5 explains the prompt-assisted ontology and rule elicitation;
* Chapter 5 retains source code as the operational authority;
* Chapter 7 planning is updated for Stage 4.4;
* prompt reproducibility is planned for Chapter 9 and the appendix;
* all new placeholders and deferred issues are logged;
* the thesis compiles successfully.

---

# 22. Final Response

At the end, report:

1. Verified commit.
2. Prompt-and-document files inspected.
3. Prompt versions and relationships.
4. LLM provider/model/run metadata found.
5. Missing LLM metadata.
6. Prompt-output-to-code mapping summary.
7. Audit files changed.
8. Planning files changed.
9. Literature-scope correction.
10. Chapter 3 correction.
11. Chapter 5 correction.
12. New tables or figure plans.
13. Citation gaps.
14. New placeholders.
15. New deferred fixes.
16. Final build command and result.
17. PDF path and page count.
18. Stage 4.4 readiness status.

Do not commit or push.

Do not proceed to Stage 4.4.
